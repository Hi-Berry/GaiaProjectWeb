# 봇 전략 실험 이력 (DECISIONS)

새 실험을 시작하기 **전에 먼저 읽어** — 같은 실패를 반복하지 말 것. 모든 head2head는 가중치 격리(`weightsDiffer=false`) 기준. 형식: 날짜 · 플래그 · 판수 · 승률(B=챌린저) · VP마진(도전자−챔피언) · p · 판정.

## 🌙 야간 작업 요약 (2026-06-18)

**누적 검증 240판** (옛 베이스라인=채택4개 OFF vs 현재봇): **현재봇 71.25 vs 옛 70.54 → +0.70 VP, 승률 53.2%(125:110), t≈0.58.**
작지만 일관 양성, 봇 퇴보 없음. 개별 채택(+1~2)이 winner's curse+상호겹침으로 과대추정이었고 누적이 정직한 값.

**채택 4 (기본 ON, 커밋됨)**: `geodensNewType`(★+3.52) · `fedEndgameVp`(+1.73) · `shipEntryByAction`(+1.98) · `qicVpGate`(+0.95).
**기각 3**: `resourceActionSetup`(−1.25) · `taklonsPowerPos`(★−2.33) · `lantidsParasiticPush`(★−1.83).
**되돌림 1**: `fedZoneStrategy`(미검증 채택이었으나 유효측정서 ON이 −1.22) → 기본 OFF.
**인프라/버그**: 래퍼 JSON 버그 수정(과거 다수 측정 무효였음) · faction-forcing 추가 · qicVpGate naive 행성유형 수정 · 채택기준 "음수만 거른다" 합의.
**메타교훈**: 종족 빌드-타깃 튜닝은 신뢰 불가(3중1 성공) — geodens만 통한 건 봇이 못 보던 크고 확정적 보상(+3K)이라서. 포지셔닝/우선순위 가점은 확장 휴리스틱을 왜곡해 −.

---

## ⚠️ 측정 인프라 버그 (2026-06-18 발견) — 일부 과거 측정 무효

`run-h2h.sh`의 `FLAGS="${1:-{}}"`가 bash에서 `${1:-{}` + 리터럴 `}`로 파싱돼 `challenger.flags.json`을
`{"x":true}}` 로 깨뜨림 → `readJson(...) ?? {}` 가 파싱실패를 **조용히 `{}`로 폴백** → **플래그 미적용 = 챔피언끼리 측정**.
리포트 `challengerFlags {}` / `flagsDiffer false`로 드러남. (수정: `FLAGS="$1"; [ -z "$FLAGS" ] && FLAGS='{}'`)
- **무효(재측정 필요)**: `fedZoneStrategy`(49.2%), `shipEntryByAction`(48.3%), `qicVpGate`(38.8%) — "거의 50% 무해"는 챔피언끼리라 당연했던 것. 특히 **fedZoneStrategy는 유일 채택인데 검증된 적 없음**.
- **유효(영향 없음)**: `fedSatHumanCap`(−2.99), `fedCompactBuild`(−4.22) — 강한 음성신호 = 플래그가 실제 적용됨(스킬 래퍼 이전 수동 실행). 기각 유지.
- 이후 모든 측정은 리포트 `flagsDiffer:true` 확인 필수.

## 채택됨 (기본 ON)

| 날짜 | 플래그/변경 | 판수 | 종족 측정(★) | 판정 |
|---|---|---|---|---|
| 06-18 | **`qicVpGate`(기본 ON 전환)** — 2QIC 우주선 VP액션(Eclipse=행성유형+2, TFMars=기술타일+2)을 실제 VP로 평가(≥6/R6만 적극, 아니면 QIC 아껴 확장). + naive 행성유형(lost_planet·가상광산 누락) 수정. **래퍼버그 무효(이전 38.8%) 후 유효 재측정.** | 120 | **VP +0.95, 승률 53.6%**(둘다 양성, p≈0.45~0.61) | ✅ **채택**(작지만 양성+룰옳음). |
| 06-18 | **`shipEntryByAction`(기본 ON 전환)** — 우주선 입장 가치를 '그 배에서 쓸 최고 액션'으로 산정. **래퍼버그 수정 후 유효 재측정**(이전 48.3%는 플래그 미적용 무효). | 120 | **VP +1.98, 승률 56.7%**(둘다 양성, p≈0.14~0.30) | ✅ **채택**. 무효였던 게 사실 양성이었음 — 래퍼버그 수정의 직접 성과. |
| 06-18 | **`geodensNewType`(기본 ON 전환)** — geodens가 PI 보유 시 '미보유 행성유형' 빌드에 +100(빌드 타깃을 새 유형으로 유도 → +3K 연쇄 + 후반 이클립스2Q 유형 다양성). **faction-forcing 첫 적용**(geodens 위치 회전, ON120/OFF120). | 240 | **ON 63.72 vs OFF 60.20 → Δ+3.52 (t≈1.47, p≈0.14)**. OFF=60.2가 기존 평균(61)과 일치. 240판 부호 일관 양성·기준선 안정. | ✅ **채택**(새 기준: 음수 아님+일관양성+도메인옳음). winner's curse로 +4.57→+3.52 안착. expansionResearch(전종족 +0.72)와 대조 — "연구 아닌 **빌드타깃 유도**"가 핵심. **첫 종족별 채택 사례.** |

## 되돌림 — 구현 미흡 (전략 자체는 유효, 더 나은 구현으로 재시도할 것)

| 날짜 | 플래그/변경 | 판수 | 결과 | 판정 |
|---|---|---|---|---|
| 06-18 | `fedZoneStrategy` — 구역 내부 티어업글(TS→의회/연구소→아카데미)로 7파워 닫기 **+ 초반 위성 ≤4 소프트예산** 묶음. | 120(유효 재검증) | B=OFF 승률 53.8%, **VP +1.22(OFF가 나음, p=0.54)**. 중간 −1.58↔+1.44 진동. | ⚠️ **기본 ON→OFF 되돌림**. 단 **전략은 사람플레이 검증된 good** — 음성은 *구현 탓*. **유력 원인: 묶인 "위성 ≤4 소프트예산"이 이미 실패한 `fedSatHumanCap`(−2.99)과 같은 실패모드**(위성 막으면 연방 자체를 못 만듦). **분리 재구현 필요**: 위성예산 빼고 "구역 티어업으로 연방 닫기" 의사결정만 깔끔히 → 재측정. |

## 기각됨 (기본 OFF / 되돌림) — 다시 시도 말 것

| 날짜 | 플래그/변경 | 판수 | 승률 | VP마진 | 왜 실패 |
|---|---|---|---|---|---|
| 06-18 | `resourceActionSetup` — 2O/7C 파워액션을 의회/아카데미 셋업 가점 | 120 | 43.6% | −1.25 | 셋업만 우대하고 실제 건설 안 따라오면 좋은수 밀어냄(expansionResearch와 동일 실패). |
| 06-18 | `taklonsPowerPos` — taklons 상대건물 인접 가점 강화(+55, 파워 수급 포지셔닝) | 120(forcing) | — | **★split Δ−2.33** | taklons를 2.33 약화. 파워 받겠다고 상대옆에 짓느라 좋은 확장·템포 희생. (전체마진 +3.32는 forcing모드 비대상좌석 아티팩트 — ★split만 유효.) |
| 06-18 | `lantidsParasiticPush` — lantids 밀집지역(주변 상대건물多) 기생 우대(+30/건물, ≤120) | 120(forcing) | — | **★split Δ−1.83** | lantids를 1.83 약화. 밀집지역 기생 강제가 더 나은 확장/타이밍 왜곡. |

### ★★ 메타 교훈 (2026-06-18): 종족 빌드-타깃 튜닝은 신뢰 못 함 (3개 중 1개만 +)
- geodens(+3.52)만 성공, taklons(−2.33)·lantids(−1.83) 실패. **차이**: geodens의 "+3K 새유형"은 **봇이 못 보던 크고 즉각적·확정적 보상**이라 통함. taklons/lantids의 "포지셔닝/우선순위 가점"은 **이미 잘 작동하는 확장 휴리스틱을 왜곡**해 손해.
- **결론**: 블라인드 휴리스틱 종족튜닝 금지. 종족 보강은 ①봇이 명백히 놓치는 **구체적·확정적 보상**이 있을 때만, ②사용자 행동검증과 함께. 막연한 "이 종족은 X를 선호" 가점은 대부분 −.
| 06-17 | `fedSatHumanCap` — 연방 위성 수를 라운드 스케일 하드캡(R≤3:2…R6:5) | 48 | 35.4% | −2.99 | 봇은 집이 흩어져 캡 걸리면 연방 자체를 못 만들어 VP 통째 손실. "위성만 막기"는 증상치료. |
| 06-18 | `fedCompactBuild` — 빌드 위치에 '비연방 건물과 멀수록 감점 + 연방 인접 흡수 감점' | 60 | 39.0% | −4.22 | 빌드를 막으니 확장(좋은 행성 선점) 가치를 죽여 손해. **빌드 페널티 접근 = 실패 패턴.** |
| 06-18 | `powerActionValue` — 우주선 입장 가치 산정 + 본판 자원액션 셋업가치 묶음 | 60→120 | 62.7%→52.9% | +3.32→**−1.45** | 60판 양성이 노이즈였음(평균회귀). 묶음은 중립~약음. 분리 측정 권장. |
| 06-18 | ~~`shipEntryByAction` 48.3%~~ → **래퍼버그 무효였음.** 유효 재측정(아래 채택표 참조). | — | — | — | 무효 → 재측정함 |
| 06-18 | `resourceActionSetup` — 2O/7C 파워액션을 '다음 턴 의회·아카데미 건설 셋업'이면 가점 + 스텝우선 페널티 완화. **래퍼버그 수정 후 유효 재측정**(flagsDiffer true). | 120 | 43.6% | −1.25 (p=0.44) | ❌ **음성 → 기각**. expansionResearch와 동일 실패: 셋업만 우대하고 *실제 그 건물 건설로 안 이어지면* 좋은 수를 밀어내는 우회=손해. "셋업→건설" 묶어야. (powerActionValue 무효측정의 유효 재측정 결론) |
| 06-18 | `expansionResearch` — terra/nav/gaia 연구점수 +(5-level)*14 (확장연구 우선순위 상향). **래퍼버그 수정 후 첫 유효 측정**(flagsDiffer true 확인). | 120 | 51.3% | +0.72 (±1.97, p=0.71) | ➖ 유의차 없음. **"최대 발견"이 기대만큼 안 먹힘** — 가설대로 *연구만 올리고 빌드가 안 따라와서* 변환 실패(또는 400ms 깊이서 미포착). 연구↑+그 트랙으로 열린 **행성 건설↑을 묶어야** 효과 가능성. 단독은 채택 보류. |

## 교훈 (위 실험들에서)

- **연방 sprawl/토큰소진은 "둔한 페널티/캡"으로 못 고친다.** 빌드를 막으면 확장이 죽어 손해(2번 실패). 효과 본 유일한 접근은 **빌드는 그대로 두고 "연방 형성+티어업글" 의사결정만** 바꾸기(`fedZoneStrategy`, 무해).
- **60판 유망 결과는 믿지 말 것.** 평균회귀로 뒤집힌다(powerActionValue 실증). 경계면 120판+.
- **가중치 격리 안 하면 결과 오염.** 초기 측정들이 candidate 가중치 때문에 +로 잘못 나왔다가 격리 후 −로 뒤집힘.

## 잡은 봇 버그 (대칭 — 양쪽 봇 공통, 측정 방향엔 영향 없으나 절대점수 낮췄음)

- 06-18 `pendingTerraformSteps` 남았는데 지을 데 없으면 후보 0개 반환 → 강제패스 + 연구블록 미도달로 **4지식이 묶임**. (2026-03-23 도입, 우리 세션 전부터 존재) → 고침. **이후 종족 평균 VP가 60대 초반→70대로 상승.**
- 06-18 사거리 부스터(트왈라잇+3/보너스+3/글린+2) **낭비 + "부스터 후 사거리 액션만" 룰 위반**(봇이 소켓 가드 우회) → 후보 필터 + 발동 가드.
- 06-17 글린/보너스 사거리 부스터를 필요 없는데 켜서 낭비.
- 06-18 가상광산 인공물(asteroid/proto) 'build_mine' 라운드미션 +2 누락 / 고급타일 adv-vp-research(+2/연구전진) 일부 경로 누락 / 다카니안 소행성 의회 보너스 누락.
- 06-28 **연구 전진 충전 낭비**(사용자 지시 후속): advance_research가 충전을 유발하는 두 지점(아무 트랙 L3 도달 +3PW, 경제 L5 도달 +6PW, applyTrackLevelBonus)에서 bowl 수용량 부족 시 충전이 버려지던 것 교정. 헬퍼 `advanceResearchAction`이 newLevel로 충전량을 산정해 `chargeDrainPreActions`로 bowl3 선환수. 후보 생성 2곳(nav QIC절약, pickResearchTracks)에 적용. waste≥2 strictly dominant(p3 부족 시도 "회수 가능한 만큼"이라 무해), `chargeDrainBowl3` 플래그 공유. h2h 불필요. 남은 미해결: 상대 leech 예측 선환수(휴리스틱→h2h 필요), 기술타일 구동 연구전진의 충전(드물어 보류).
- 06-28 **우주선 입장 충전 낭비**(사용자 관찰): 2·3번째 입장 +2PW·4번째 +3PW 충전(executeEnterSpaceship)이 bowl 수용량(2·p1+p2) 부족 시 버려짐. 기존 tech-act-4p의 충전낭비 대비 인라인 로직을 헬퍼 `chargeDrainPreActions(playerId,player,chargeAmount)`로 추출해 우주선 입장에도 적용. waste≥2에서만 bowl3를 1P→1C로 비워(strictly dominant: 비운 토큰이 충전으로 bowl3 복귀+크레딧) — 기존 `chargeDrainBowl3` 플래그(기본ON) 공유. itars/nevlas(입장 시 토큰 선소모)·타클론 제외. h2h 불필요(낭비수정). 미해결: 연구L3(+3)·eco L5(+6) 충전, 상대 leech 예측 선환수는 별도(후자는 휴리스틱이라 h2h 필요).
- 06-28 **인공물 take 시 bowl3 토큰 낭비**(사용자 관찰): 6파워는 `spendPowerTokens`로 그릇1→2→3 순 소모 → bowl1+bowl2<6이면 부족분만큼 bowl3 토큰이 그냥 제거됨. `findTwilightArtifactActions` totalPower≥6 분기가 preActions 없이 바로 소모하던 것 교정 → 제거될 bowl3 토큰 `d=min(p3,6-p1-p2)`개를 먼저 1P→1C 환수(토큰은 bowl1로 옮겨졌다가 어차피 소모 → 최종 파워 동일, 크레딧 d 이득 = strictly dominant). 헬퍼 `doomedBowl3CashoutPreActions`. 타클론(브레인스톤 특수회계) 제외. h2h 불필요(낭비수정).

## 2026-06-28 startPlacementFuturePlayers (플래그 기본 OFF, 미검증)
- 사용자 관찰: 봇이 초기 광산 놓을 때 '내 턴 뒤에 놓을 사람'(미래 배치자) 위치 고려 안 함. 스코어러(findStartingMineAction)가 **이미 놓인** 상대 광산(+5/dist≤2)만 봄.
- 구현: 상대 홈 행성 타입의 빈 타일이 dist≤2면 +2(캡 +6) — 상대가 거기 지을 확률↑→미래 리치 기대. 중앙·접점 자리를 자연히 선호.
- ★검증 한계: placement/contention은 self-play 재현 불가(placementPolicy −7.10 기각 전례). do-no-harm으로 '손해 없음'만 확인 가능, 진짜 판정은 실게임 1:3. 그래서 **기본 OFF**로 두고 사용자가 켜서 관찰.
- 확장(사용자 추가 관찰 "우주선 주변+맵 중앙 가점 없음"): 같은 플래그에 (b)우주선 인접(dist≤2:+4, ≤4:+2 — 입장 비용↓) (c)맵 중심 근접(최대 +6, 전체 타일 q/r 평균=중심) 추가. 사전계산은 루프 밖.
- 미구현(v2 후보): contested 확장군집(여러 상대 사거리 동시 노출) 약한 감점.

## 2026-06-28 pendingStepsQicPenalty (플래그 기본 ON)
- 사용자 관찰: 봇이 "Nav를 레벨1로만 올리고(=range 안 늚) 2거리에 1QIC 던져 짓는다, 늘 1QIC 손해". 실게임 로그(b3lhu1ks R1 ivits) 확인: terra/volcanic에 1QIC씩 빌드 + nav→1(range 그대로 1).
- 근본: 빌드 생성기 2개 중 `findBuildActions`엔 강한 QIC 억제(R1 −300/QIC, nav절약 skip, navBeforeJump 검증됨)가 있는데, **`findBuildActionsWithPendingSteps`(펜딩 테라폼스텝 경로)엔 QIC 페널티가 전혀 없었음**(score=dist*15만). 테라폼 보너스/삽 액션 쓰면 이 경로만 타서 QIC 낭비.
- 픽스: 펜딩스텝 경로 regular 타일 score에 `−neededQic*(R≤4?220:120)`. 순서만 바꿈(인레인지 우선, 없으면 최소QIC) → 빌드 자체는 안 막아 삽 낭비 없음. 이미 검증된 navBeforeJump 억제의 누락분 보충이라 기본 ON.
- 검증: ✅ do-no-harm 120판(챔피언ON vs 도전자OFF, 가중치격리). 평균VP 챔피언71.6 vs 도전자70.7 → 신동작 +0.86VP(p=0.65 노이즈), 승률 신66:구53. 음수아님+방향일관 → 채택유지. 행동: 신ON이 광산 +0.10/좌석(QIC아껴 인레인지확장↑)=의도대로. 수익스킵 게임 0(측정 클린).

## 종족별 평균 점수 (최근 측정 — 약한 종족부터 보강 대상)

06-18 (버그픽스 후, 120판, ~27샘플/종족):

```
56.1 nevlas   ← 최하위 (1순위 보강)
61.4 geodens
62.3 lantids
62.7 itars
63.7 taklons
68.3 bescods
68.9 moweyip   69.5 darkanians  69.7 firaks  70.3 terran  70.9 ivits
72.2 space_giants  73.1 xenos  74.9 tinkeroids  77.0 bal_tak
81.8 ambas  84.0 hadsch_hallas  87.7 gleens ← 최상위
```

## ★ 최대 발견 (2026-06-18): 봇 전반의 확장 연구 과소투자

저장 게임들에서 **확장 연구 트랙 합(terraforming+navigation+gaiaProject)**을 봇 vs 사람으로 비교:
- **봇: 4~7** (nevlas 1, firaks/ivits 4, ambas 4.8, itars/geodens 5~6.7) → 점수 40~70
- **사람: 9~14** (ambas 12, firaks 12.5, ivits 12.3, geodens 12, tinkeroids 14) → 점수 180~270

**모든 종족에서 봇이 확장 연구를 사람의 ~절반만** 올린다 → 테라포밍/사거리/가이아 못 풀어 행성 못 늘림 → 작은 엔진 → 저득점. **nevlas(exp 1.0)는 극단 사례, 진짜 문제는 전 종족 공통.** 종족별 미세튜닝보다 **글로벌 확장연구 우선순위 상향**(calculateResearchScore의 terra/nav/gaia 가중치)이 1순위 레버. → 가설로 측정 예정(flag).
주의: 단 연구만 올리고 빌드(새 행성 건설)가 안 따라오면 효과 없음 — 연구↑ + 그 트랙으로 열린 행성 건설↑이 같이 가야. 측정으로 확인.

추가 버그(수정함): 이클립스 액션1(행성유형+2VP)이 **잊혀진 행성·가상광산을 미카운트**(naive types가 space타일의 lost_planet_mine 누락) → getPlayerPlanetTypesForGeodens 사용으로 수정. (봇 qicVpGate 추정치 eclipse i1도 같은 naive — qicVpGate 측정/채택 시 같이 정리.)

## 🔄 재측정 백로그 (래퍼버그/빡빡기준으로 무효였던 것 — 새 기준 "음수만 거른다"로 다시)
순차 실행(포트/플래그파일 충돌 방지). 각 리포트 `flagsDiffer:true` 확인 필수.
1. **`resourceActionSetup`** (파워액션: 2O/7C를 의회·아카데미 셋업에 가점) — 진행중. powerActionValue(+3.32→−1.45)는 0주변진동=무효 의심.
2. **`fedZoneStrategy` 재검증 (🔴시급)** — 기본 ON인데 미검증. 챌린저 `{"fedZoneStrategy":false}`로 끄고 챔피언(ON)과 비교. 음수면 기본 OFF로 되돌려야(몰래 해로울 위험).
3. **`shipEntryByAction`** (우주선 입장 가치) — 48.3%는 무효.
4. **`qicVpGate`** — 38.8% 무효. naive 행성유형(eclipse i1) `getPlayerPlanetTypesForGeodens`로 고친 뒤 측정.
5. (옵션) **expansionResearch 재시도** — "연구↑ + 그 트랙으로 열린 빌드↑" 묶음으로.

기각 유지(명확한 음수, 재측정 불필요): `fedSatHumanCap`(−2.99), `fedCompactBuild`(−4.22).

## 연방 후반 로직 (구현됨, 측정 대기)
- ✅측정완료 **`fedZoneUpgrade`** (06-18, 120판) — 위성예산 떼고 "구역 티어업으로 연방 닫기"만 분리. **VP +0.02(완전중립), 승률 44%(p=0.19)**. 깨끗이 구현해도 점수 중립 → **채택 안 함(OFF 유지)**. 시사점: 분리해보니 원래 fedZoneStrategy 음성은 위성예산 묶음/노이즈였고, 티어업 전략 자체는 무해중립. 구현(+480/+520 R4+) 튜닝 여지 있으나 우선순위 낮음.
- ✅**채택(기본 ON)** **`fedEndgameVp`** (06-18, 120판) — R6 연방: 보상 선택지를 전부 후보로 펼쳐 MCTS가 다운스트림 총VP(자원→연구5/고급타일/미션)로 선택 + 위성페널티≈0(토큰 sunk). **VP +1.73, 승률 55.9%(둘다 양성, p≈0.2~0.38)** → 채택. 현 구조는 `finalizeFederation`이 face-value로 보상 1개 미리 찍어 MCTS가 비교 못 했던 것(사용자 관찰)을 해결.
- ✅**채택(기본 ON, 버그/룰 수정)** **`tfStepBuildSameTurn`** (06-22, 120판, 격리 OK) — TF-Mars 액션3(3C→1TF스텝) 후보 게이트에서 3C 차감까지 시뮬 → 3C 쓰고도 그 턴에 광산을 실제로 지을 수 있을 때만 TF-3C 허용. **head2head 중립(VP −0.25±1.65, 승률 47.5%, p≫0.05)** 이지만, 사용자 지적대로 "3C 쓰고 다음 턴 건설"은 전략이 아니라 **버그(TF 스텝은 같은 턴에 써야 함)** → 룰/낭비 수정이라 점수 무관 채택. 중립인 이유는 챔피언·챌린저 둘 다 스텝-이연 가능 엔진에서 측정돼 차이가 안 난 것. (별도 backlog: 엔진이 TF-Mars 스텝의 턴-넘김을 애초에 막아야 — fed-reward 3tf-mine 등 영구 스텝과 구분 필요.)
- ✅**채택(기본 ON, 강한 양성)** **`fedMinTrim`** (06-19, 120판, 격리 OK) — 연결 컴포넌트가 7+α면 통째로 안 묶고 "딱 7 넘는 최소 부분집합"만 연방에 넣어 초과 건물을 다음 연방 씨앗으로 보존. **승률 59.8%(70:47, p=0.033 유의), VP +2.60±1.81(p=0.151)** → 채택. 사용자 모델("사람은 전체파워 7 넘어도 다 안 묶고 작게 자주 만든다") + 사람로그 프로파일(연방수 4.2 vs 봇 1.3, 위성 1.55 vs 5.93)이 둘 다 가리킨 방향. 블런트 캡(기각 −2.99)과 반대로 형성을 막지 않고 "작게 자주"를 유도해서 성공. [[human-federation-profile]] 참조.
- ✅**채택(기본 ON)** **`fedRewardResourceAware`** (06-19, 120판, 격리 OK) — 돈연방(fed-7vp-6c)/광물연방(fed-7vp-2o)이 둘 다 플랫 250이라 돈이 남아도(또는 돈 수익이 많아도) 돈연방을 절반 집던 문제(사용자 관찰). '현재 크레딧 + 향후 돈 수익×min(3,남은R)'을 '돈 여유'로 보고 ≥25→−25, ≥45→−50, 광물은 +2. **승률 50%(58:58), VP +1.27±1.78(p=0.478)** → 무해+양수+도메인 정합("음수만 거른다") → 채택. 둘 다 7VP라 신호 약한 건 예상대로. getNextRoundIncomePreview로 교역소 패스 시나리오까지 반영(현재 잔고만 보던 1차 시도는 사용자가 "교역소 짓고 패스하면 담 라운드 돈 많다"고 지적해 개선).

## 큐 (다음에 할 것)
- [x] ~~`expansionResearch`~~ → 51.3%/+0.72 유의차 없음(2026-06-18). 단독 보류. 다음: **연구↑ + 빌드 변환↑ 묶어서** 재시도.
- [ ] **`fedZoneStrategy` 재검증 (최우선)** — 유일 채택인데 래퍼 버그로 검증된 적 없음. 별도 OFF 플래그 필요(현재 기본 ON).
- [ ] `qicVpGate` 재측정 — 이전 38.8%는 무효(플래그 미적용). 2QIC 우주선 VP액션 실제 VP 평가, ≥6 또는 R6만.
- [ ] **nevlas** 전략 보강 (최하위) — 파워토큰 경제 종족, 봇이 특수 메커니즘 미활용 의심. (종족 강제 배정 필요)

## 2026-06-23 contention prior — mcts.ts PUCT(강한 레버) → 무해(노이즈), 미채택
- mcts.ts bestUCT에 use_power_action 대상 getGrabUrgency PUCT 항 추가(flag useContentionPrior, contentionPriorW 기본2). HEAD mcts.ts 기준(유저 WIP은 stash).
- head2head 120판(가중치격리): 도전자 승률 51.3%(61:58, p=0.783), VP +0.60±1.73(p=0.731). 음수 아님이나 노이즈.
- 결론: 경쟁자원 선점 편향은 do-no-harm이지만 측정 이득 없음(봇이 이미 gain-2-ore 고평가). 가설(선점이 VP 이득) 미지지. mcts.ts 훅은 유저 파일이라 미커밋, 채택 보류. contention 데이터/모듈은 커밋되어 보존(향후 '의존 회피' 등 다른 적용 여지).

## 2026-06-23 자원형 파워액션 선점 (preferResourcePowerAction) — 행동검증 통과, VP 음수 → 기각
- 발견: 봇은 테라포밍스텝 파워액션(gain-1-step/2-steps)은 잘 쓰나 자원형(2O/7C)은 거의 안 씀. 계측(자가대국): gain-2-ore 0.25/게임. (참고: 사람게임 로그 봇=0/205는 구버전/탐지 이슈로 현재 자가대국과 불일치 — 자가대국 계측이 현재 행동의 정답.)
- 계측 방법: bot.ts performAction use_power_action에 AI_POW_COUNT env-gated 카운터(data/pow-pick-count.txt). 주의: ESM이라 require('fs') 실패 → import * as nodeFs 로 해결. head2head 워커는 BotLogic.executeAction 경로를 탐(확인됨).
- 레버: 결정경로에서 ore<3 & p3>=5 & gain-2-ore available이면 MCTS 건너뛰고 2O 강제(7C도 유사).
- 행동검증(8판, B좌석만): gain-2-ore 2→7, 7C 5→10 — 발화 확인. 단 VP −10.13±7.60(p=0.18) → 강한 음수.
- 결론: 자원형 파워액션 강제는 메인액션 템포 손실이 파워환율 이득을 압도해 손해. 봇의 기피가 대체로 옳음. 사용자 가설("무조건 이득") 미지지. 단 레버가 무뎌(좋은 건설도 무시) 음수 일부는 그 탓일 수 있음 → 외과적 버전(3p→O 치환 시점만) 여지.
- 교훈: bestUCT prior(이전)는 최종선택 못 바꿈. 결정경로 강제 return은 결정적 발화(검증됨). "행동 검증 후 VP" 절차 정착.

## 2026-06-23 자원형 파워액션 — 외과적 버전(3p→O 프리변환 시점만 2O 치환) → 중립~약음수, 미채택 (확정 120판)
- 블런트(ore<3 강제) −10은 좋은수 무시 아티팩트였음(사용자 지적). 외과적 버전으로 격리: MCTS가 3p→O를 고른 순간에만 gain-2-ore로 치환.
- 행동검증: gain-2-ore 발화 베이스 0.25/게임 → 외과 0.5~0.6/게임(B좌석). 발화 확인.
- VP 추세: 8판 +0.06 → 60판 −4.54(p=0.09) → 120판 −2.50(유의없음). 음수 근처 진동=중립~약음수.
- 120판 액션수(좌석당): 총건설 A 15.20 vs B 14.97(−0.23), research −0.19. 30판의 "B 건설 +0.97"은 소표본 노이즈였고 120판이 교정 → B가 개발을 더 하지 않음.
- breakdown: 손실 researchTracks/techTiles/pass, 이득 finalMissions. remainingResources ≈ 동일.
- 결론: 2O 효율(2파워/광석) 이득이 메인액션 비용과 상쇄(wash). 음수방향이라 미채택. 사용자 자원효율 논리는 옳으나 봇 실효는 중립.
- 방법론 정착: bestUCT prior는 최종선택 못 바꿈 / 결정경로 강제는 결정적 발화 / 소표본(8~30) 액션수는 노이즈 큼 → 120판 필요 / "행동검증→VP→큰표본 확인" 절차.

## 2026-06-23 야간 자율 실험 큐 (engineBlend 계측 결과 기반)
- 계측: 휴리스틱 evaluateState score 평균 ~4956, engineValueNet raw출력 평균 ~77(VP스케일). weight 50 → 엔진망이 평가의 77% (압도). 
- engineBlend=50(77%압도): VP −0.8 중립 → 망 ≈ 휴리스틱 동급. engineBlend=13(20%블렌드) 40판 +1.5 약양수(노이즈), 120판 확인중.
- 핸드오프 문서 재확인: 가치망은 사람데이터 부족으로 노이즈. 실제 VP격차 = 우주선0/기술타일0/라운드미션/연구편식.
- 야간 큐(전부 플래그 default OFF, 음수면 미채택): researchBreadth(연구분산 상태보상, evaluator.ts), ivitsFedAwareStation(우주정거장 연방연결성 우선, bot.ts findIvitsSpaceStationAction). 둘 다 tsc 통과, head2head 대기.
- 임시계측 잔존: evaluator.ts engineBlend 블록의 AI_EVAL_SCALE 덤프(env-gated, 무해) — 정리 예정.

## 2026-06-23 engineBlend(학습 엔진 가치망 블렌드) → 기각 (전 비중 중립)
- engineBlend=50(망 77% 압도): VP −0.8 중립. engineBlend=13(20%): 40판 +1.5(노이즈) → 120판 VP −0.11(p=0.96) 완전 중립, 승률 44.9%.
- 결론: 학습 엔진 가치망 ≈ 그리디 휴리스틱(어느 비중이든 중립). 블렌드만으론 개선 불가. 가치망 재학습(더 많은 자가대국+실제최종VP) 필요. 단 자가대국 학습데이터 저장 인프라 없음. → value-net 경로 보류, 구체적 격차(연구분산/연방) 표적으로 전환.

## 2026-06-23 researchBreadth(연구 분산 상태보상) → weight 50 강한 음수
- researchBreadth=50 60판: VP −5.44±1.85 (p=0.003 유의), 승률 39%. 강하게 해로움.
- 해석: 봇은 지식수입 부족 → 강제 분산이 '얕고 넓게'(L2 여러개)가 돼 깊은트랙 L5보너스(12VP) 상실. 사람의 L4+ 넓고깊게는 지식이 받쳐줘야 가능. 봇엔 mono-deep이 분수에 맞음.
- weight 15로 약하게 재확인 예정.

## 2026-06-23 researchBreadth=15 (약한 분산 nudge) → +2.3 약양수 (120판 확인중)
- weight 15 60판: VP +2.30±2.37 (p=0.33), 승률 53.3%. weight 50(−5.4)과 대조 → 스윗스팟 존재: 강제 분산은 해롭고, 살짝 유도는 이득.
- 해석: mono-track에서 살짝 벗어나게만(L2+/L3+ 폭 가벼운 보상) 하면 +, 강제하면 얕은분산으로 −. 120판 확인 후 양수 유지면 채택.

## 2026-06-23 ivitsFedAwareStation (우주정거장 연방 연결성 우선 배치) → +5.7 Ivits (120판 확인중)
- bot.ts findIvitsSpaceStationAction에 전략0 추가: 빈 우주를 '내 건물 인접(거리1) 수' 많은 곳 우선(연방 닫기). Ivits 전용이라 타 종족 무해.
- Ivits 강제 60판: 전체 VP +4.01(p=0.098), factionSplit Ivits onAvg 73.8 vs offAvg 68.1 = +5.70. 사용자 관찰("자유분방 배치") 교정.
- 120판 강제Ivits로 확정 예정.

## 2026-06-23 researchBreadth weight 최적화 종료 → 15 유지
- 25 vs 15: 60판 +2.41(노이즈) → 120판 −0.75(p=0.71). 25는 15보다 안 나음. 15(검증된 +3.85 p=0.022) 유지.
- 야간 자율 최종: 채택 2건(researchBreadth=15 유의, ivitsFedAwareStation do-no-harm) + 버그수정 4건(소행성QIC, qicVpGate실효화, 충전낭비cleanup, 채팅). engineBlend/researchBreadth50/breadth25 기각.

## 2026-06-23 researchBreadth=15 복제 확정 (robust ~+3 VP)
- 1차: 15 vs off 120판 +3.85 (p=0.022). 복제: off vs 15 120판 −3.02 (p=0.073, = 15가 +3.02 우세). 두 독립 샘플 일치 → researchBreadth=15는 robust ~+3 VP 개선 확정.
- (패키지 둘다OFF vs 둘다ON 120판은 VP +1.52 모호했으나 단일샘플 노이즈; 직접 2회 측정이 일관 → 채택 신뢰.)
- 야간 자율 최종 결산: 채택 researchBreadth=15(검증된 +3VP, clusterFedBonus 이후 첫 유의 강화), ivitsFedAwareStation(do-no-harm). 버그수정 6건(소행성QIC 서버+클라, qicVpGate실효화, 충전낭비cleanup, 채팅 낙관적+unread). 기각 engineBlend/breadth50/breadth25/2O자원파워액션.

## 2026-06-23 planetTypeBreadth(행성유형 다양성 nudge) → 기각 (60판 +3.41 → 120판 −0.78 회귀)
- researchBreadth와 동형 시도. 60판 VP +3.41(p=0.19) 유망했으나 120판 −0.78(p=0.66) 중립. 노이즈. evaluator.ts 코드 되돌림(HEAD).
- 야간 종합 패턴: gentle eval nudge 6개 시도 → researchBreadth=15만 진짜(120판+복제 통과). 나머지(engineBlend/breadth50/breadth25/planetTypeBreadth/2O) 전부 60판 양수→120판 회귀 or 음수. 120판+복제 규율이 가짜 다 걸러냄. 결론: 고립된 nudge로 얻을 진짜 강화는 희소(이 효과 누적 ~2건: clusterFedBonus, researchBreadth). 추가 큰 도약엔 1:3 플레이 데이터 / 계획엔진 / RL 필요.

## 2026-06-24 oreScarcity (광석 희소 페널티) — ❌ 기각
- **가설(사용자 지목 2대 약점):** ①TP 과다건설+조기패스→크레딧풍선·광석고갈 ②2O/7C 파워액션 미사용. 평가기가 잉여자원을 선형·미미(ore×0.08, cred×0.025)하게만 쳐서 "2O 결과상태(+0.16)"가 "TP업글(+90구조)"에 항상 짐 → 평가기에 광석희소 페널티 추가로 2O를 결정경로에서 이기게.
- **v1(escapable):** credit≥6 & ore<4면 부족분 페널티. **결함:** 게이트가 credit≥6이라 TP에 크레딧 써서 6밑으로 떨구면 페널티 소멸 → 봇이 2O 대신 TP로 도망(B 2O 442 < A 512). 24판 VP +2.75 중립.
- **v2(non-escapable):** ore===0 & power3≥4 & round≤4일 때만 -120. 2O만 유일 탈출구(ore 0→2). **메커니즘 작동확인:** B 2O 497 > A 462(7%↑). **그러나 24판 VP −9.0 (p=0.012) 유의하게 약화 → 채택 금지.**
- **핵심 교훈:** ①**자가대국에선 봇이 2O를 이미 풍부하게 누른다**(A 462~512회/24판). 사용자가 "실전에서 한번도 안 누른다"고 본 건 **강한 상대(본인) 앞에서만 나타나는 현상** → self-play로 재현·검증 불가. ②2O를 TP보다 강제 우선시키면 self-play 점수 하락 = 봇의 TP건설이 self-play에선 틀린 게 아님. [[value-net-blend-neutral]]의 "2O 중립~해로움" 재확인. → 이 2대 약점은 **사용자 1:3 실전 데이터로만** 진단·검증 가능.
- 코드 전량 revert, 플래그 OFF, 계측(AI_FLOW_COUNT) 제거.

## 2026-06-25 ★ head2head 행동믹스 계측 추가 (방법론 업그레이드)
- **계기(사용자):** "VP 무해로 넘어가면, 의도한 행동대체(교역소↓→광산/연구소↑)가 *실제로 일어났는지* 아니면 그냥 안 하고 패스↑인지 구분이 안 된다." → 정당한 지적. VP마진은 결과지표라 메커니즘을 못 봄.
- **구현(headToHead.ts):** 게임 종료 시 emit된 `gameLog`를 playerId별로 분류 카운트(광산/교역소/연구소/의회/업글/연구/연방/파워액션/기술타일/가이아/우주선입장/우주선액션/Nav+1/패스/총행동) → 리포트+stdout에 **A(챔피언) vs B(도전자) 좌석당 평균 차이표**. `classifyAction` 순서 주의: "Upgraded to Trading Station/Research Lab/Academy"를 일반 'Upgraded'보다 **먼저** 매칭(안 그러면 교역소가 일반업글로 새서 0으로 나옴 — 1차 버전서 실제 발생).
- **즉시 성과:** ①shipTechEntryValue가 "목적 미달성(Nav+1 0.40→0.50)+전반 감소"인 헛수고임을 정량 포착. ②self-play가 실전격차(우주선 contention) 재현 못 함 정량 확인(Nav+1 self-play 0.40 vs 실전 0.09). ③tpDiminish 기존 메커니즘 설명("교역소↓→광산↑")이 틀림 포착(교역소 불변, 차이는 광산/총행동에 분산). → 앞으로 모든 A/B는 VP + 행동표 같이 본다.

## 2026-06-25 shipTechEntryValue (우주선 기술타일 가치) — ❌ 기각 (−9.5)
- **가설:** 실전 Nav+1 획득 봇 0.09 vs 사람 4.59(50배), Rebellion 관여 0.70 vs 3.71. 입장가치를 estimateBestShipActionValue(액션)으로만 매겨 Rebellion이 주는 Nav+1(영구 +사거리=확장 약점 직격) 가치가 0 → 안 탐. 입장가치+선택기점수에 우주선 기술타일 실가치 반영(Nav+1 +170/110 등).
- **결과(40판, 격리 OK):** 승률 37.5%, **VP −9.51 (p=0.003)**. 행동표: Nav+1 0.40→0.50(거의 안 늚), 우주선입장 −0.20, 광산 −0.63, 총행동 −1.81 → **목적 미달 + 전반 악화.**
- **핵심:** 챔피언이 self-play선 이미 Nav+1 0.40 먹음 → **self-play가 우주선 격차를 재현 못 함.** 실전 0.09는 봇 평가문제가 아니라 **강한 사람이 우주선을 먼저 차지하는 contention**일 가능성. oreScarcity·researchLikeHuman과 동일 경계 — 1:3만 검증가능. flag OFF 유지(코드 보존, 1:3 실험용).

## 2026-06-25 potentialEval (다턴 잠재력: ore·지식=선불행동) — ➖ 무해, default OFF
- **가설:** 행동믹스상 봇 라운드당 행동수가 사람의 절반(research −9·upgrade −8·mine −7) = 라운드 중반 ore/지식 고갈로 일찍 패스. ore·지식을 '잉여 0.1×'가 아니라 '다음 행동가치의 작은 일부'로 평가(크레딧은 0.1× 유지=풍선 억제), hoarding 방지 위해 소비≫보유 되게 분수 작게.
- **결과(40판, 격리 OK):** 승률 52.5%, VP +0.96 (p=0.757) = 무해. 확실한 이득 아니라 **default OFF, 1:3 판정용 보존.**

## 2026-06-25 tpDiminish 역검증 (챔피언 ON vs 도전자 OFF) — ON 유지
- 현재구성에서 끄면(40판, 격리 OK) 승률 27.5%/**VP −8.45(p=0.005)**. 원래 채택시 "무해(+0.08)"와 모순 → 다른 채택 fix들과 상호작용으로 현재 +가 된 듯. **행동표: 교역소 거의 불변(4.60 vs 4.51)** → 기존 "교역소↓→광산↑" 설명 틀림, 실제론 OFF가 광산(−1.16)·우주선액션(−0.86)·총행동(−2.44) 전반 감소. 120판 확인은 "결정 안 바뀜(어차피 ON 유지, 음수만 아니면 됨)"이라 사용자 판단으로 생략. **tpDiminish ON 유지.**

## 2026-06-25 earlyOreBonusTile (R1-3 보너스타일 광석우대) — ➖ 무해+메커니즘 미발화, default OFF (1:3 대기)
- **연구(사용자: "사람 초반 따라해 R1-3 건물수/수입 늘려라"):** R1-3 데이터 — ①봇은 자원 쟁여두고 안 지음(R3 보유자원 봇 9.8 vs 사람 5.3, R3까지 건물행동 8.71 vs 11.43). ②보너스타일 선택 정반대: 사람=1O 확장타일(1o-mine/1o-2tokens/1o-ts), 봇=4c-gaia(순수 크레딧). 봇 점수 4c-gaia(10)>1o-mine(8.5)라 크레딧타일을 위로 침 → 크레딧 풍선↑·광석 굶음.
- **fix:** calculateBonusTileScore에서 round≤3 광석 가중치 3→5(지식 3→4) → 1O 확장타일이 4c-gaia를 이기게(픽이 결정적으로 광석쪽으로 바뀜=사람 따라하기).
- **결과(40판, 격리 OK):** 승률 53.8%, VP +2.29 (p=0.497) 무해. **그러나 행동표 광산 Δ−0.18(안 늚)·총행동 Δ−0.71** → self-play에선 메커니즘(광석↑→광산↑) 미발화. 우주선·2O와 동일 벽: self-play가 실전 격차(크레딧타일 편향=사람이 광석타일 선채감 contention + 쟁여두기) 재현 못 함. 광석 더 줘도 self-play 봇은 안 지음(진짜 병목=쟁여두고 안 짓기).
- **판정:** default OFF(신뢰 채택 안 함). 단 실전선 픽을 광석타일로 바꿈+무해 → **1:3 직접 검증 대기.**

## 2026-06-25 ★ 광산 미건설 진단 — "평가 저평가" 아님, "도달능력(reach)" 한계 (expandDrive로 판별)
- **사용자 교정(중요):** "광산 적게 짓는 건 강한 상대 앞에서만이 아니라 봇끼리(self-play)도 그렇다" → 맞음. 행동표상 self-play 봇 광산 ~8 vs 사람 16.7. **이건 contention(2O·Nav+1)과 달리 self-play에 존재하는 진짜 갭** = 측정·수정 가능. (2O/Nav+1을 광산과 한 묶음으로 본 건 오류였음.)
- **R1-3 자원 분해(라운드말 c/o/k):** R3 봇 7.0/0.8/1.9 vs 사람 3.7/0.9/0.8. **광석은 동일(둘 다 0.8=다 씀), 봇은 크레딧(+3.3)·지식(+1.1)을 쟁임** → 봇=새 광산 대신 제자리 업글(mine→TP=크레딧, →lab=지식)로 tall, 사람=새 광산 wide.
- **판별 실험 expandDrive(확장 +28/건물, 40판 격리):** VP **−6.10(p=0.039)**, **광산 Δ−0.11(안 늚!)**. → 확장 가치를 올려도 광산이 안 생김 = **평가기가 확장을 저평가하는 게 아니라, 지을 타일에 도달을 못 하는 것(reach 한계).** 봇은 이미 닿는 광산은 다 짓고 있음.
- **진짜 병목 = 도달능력**: 가이아포머(transdim→가이아 새 타일) 봇 0.03 vs 사람 0.43, 확장연구(nav/terra/gaia) 봇 4-7 vs 사람 9-14(★기존 최대발견). 사람은 사거리/포머로 새 타일을 열어 광산을 더 깜.
- **왜 안 고쳐지나:** expandDrive(−6)·expansionResearch(+0.72 무의차) 둘 다 단일 조각만 건드려 wash. **"연구↑ + 사거리↑ + 그 결과 건설↑"이 묶인 복리 엔진 부트스트랩** 문제 → 단일 eval nudge로 안 풀림. **path2(학습정책)/계획엔진이 필요한 핵심 근거**(1턴 평가기는 "지금 nav 올려 다음턴 가이아행성 따기"를 못 봄). expandDrive OFF 유지.

## 2026-06-25 earlyTerraExpand (R1-3 terraforming 연구 강제) — ❌ 기각 (−7.9, 광산 ↓), reach-coupling 확정
- **연구(사람 R1-3 연구트랙):** 봇 terra 0%·gaia 2%(nav58·econ41만) vs 사람 terra20·gaia29%. 가이아포머 봇 0.06 vs 사람 1.24. 가설: terra↑→개척비용↓→findBuildActions 빌드후보↑→광산 따라옴. 결정경로 강제(humanRule2O 방식, 평가기 nudge는 wash라).
- **측정 함정(중요):** bot.ts `log()`는 head2head stdout에 안 찍힘 → "발화 횟수 grep" 무용(0으로 오판). **발화 여부는 행동표 변화로 판단해야.** 초기 가드판(terraTarget/canTerra)은 행동표 거의 불변=거의 발화 안 함 → +1.15/−5.54는 **노이즈**(같은 사실상-no-op이 40판 변동만으로 ±6 흔들림 = 40판 불신 실증).
- **무조건 발화판(R≤3·terra<2·지식≥4):** 행동표 크게 변함(총행동 −2.77) = 실제 발화. **VP −7.89(p=0.011), 광산 Δ−0.31(줄음!).** → **terra를 강제로 올려도 광산이 안 늘고 오히려 줆**(낮은 nav로는 terra해도 닿을 non-home 행성 없음 + 강제 terra가 실제 빌드/템포 밀어냄).
- **★결론(반복확인):** 단일 레버 3종 전멸 — expandDrive(−6, 광산 안 늚)·earlyOreBonusTile(무해, 광산 안 늚)·earlyTerraExpand(−7.9, 광산 줆). **확장 병목 = nav+terra+reach+build 묶인 다턴 계획이라 단일 hand-rule로 절대 안 풀림.** path2(학습정책)/계획엔진이 유일한 길. 코드 revert(HEAD), flag 제거.

## 2026-06-26 buildOrderPlanner (연방 형성 강제, 라운드별 목표) — ❌ 기각 (연방 0증가 + −4.14)
- **목표(사용자):** 봇 연방 1.39 → 2.5+. 데이터: 봇 연방 1.39·위성 5.93 vs 사람 4.21·위성 1.55(봇=흩어진 큰연방 적게, 사람=촘촘한 작은연방 많이). 고급타일 0.00도 연방부족 탓(초록연방 부족).
- **레버:** buildOrderPlanner flag — myFeds<fedTarget(R3:2·R4:3·R5:4)이면 getBestFederationAction(품질게이트)로 강제 형성.
- **결과(40판, 격리 OK):** 승률 30.8%(p=0.016), VP −4.14. **연방 A 1.39 vs B 1.38 — 전혀 안 늚!** 파워액션 −1.24·광산 −0.60·총행동 −1.85.
- **★핵심:** 강제했는데 연방 0증가 = getBestFederationAction이 목표미달 시에도 null = **봇 구조물로는 2번째 7파워 클러스터를 못 만듦.** 연방 수 병목은 '형성 결정'이 아니라 **구조물 연결성/배치**(흩어져 지어 위성 5.93개로 큰연방 1개만, 나머진 2번째 클러스터 안 됨). 강제는 같은 연방을 나쁜 타이밍에 형성해 다른 액션만 밀어냄 → −VP.
- **결론:** 연방 ≥2.5는 "형성 강제"로 불가(증명됨). 진짜 레버 = **타이트한 빌드 배치**(인접 클러스터로 지어 위성 적게 작은연방 여럿). 단 findStartingMineAction은 오히려 2번째 광산을 1번째서 멀리(−100) 배치 = 연방엔 역효과(초반 확장/다양성과 트레이드오프). 연방은 결국 구조물 수/배치(=확장 엔진) 다운스트림 → path2/배치 재설계 영역.

## 2026-06-26 mineFirstExpansion (업글 억제→광산 우선) — ❌ 기각, 광산 오히려 줆
- 광산<5·R≤4에 비-첫TS·비할인 업글 -120 → 새 광산 우선 의도. 40판: VP -2.92(노이즈), **광산 8.65→8.13(줄음)**, 교역소 +0.15, 연방 1.51→1.50(무변). 업글 막아도 광산 대체 안 되고 다른 행동으로 샘.
- ★**건물수 단일레버 4종 전멸 확정**: expandDrive(가치↑)·earlyOreBonusTile(광석↑)·earlyTerraExpand(terra강제)·mineFirstExpansion(업글억제) 전부 광산 못 늘림(안 늚 or 줆). 봇 ~8광산 = reach 천장. reach는 nav+terra+gaiaformer 초반 coordinate 투자(다턴)라 단일 hand-rule 불가. 연방(구조물 다운스트림)도 따라서 불가. → 남은 길: 좌표된 오프닝 스크립트(사용자 전략) 또는 path2.

## 2026-06-27 ★ R1 오프닝 데이터분석 (35 사람로그 vs 봇 자가대국) — "사람 로그로 학습" goal
- **사람 R1 오프닝(66명):** 첫 액션 업글 65%, 지배적 시퀀스 **→교역소→연구→기술타일**(변형 다수). 연구트랙 nav/gaia 위주.
- **★ 사람 R1 우주선 탑승 63/66 = 95%!** 첫 연방 R2:20·R3:28명.
- **봇 자가대국(59명):** 오프닝 흩어짐(업글 26%·광산15%·보너스15%, 지배패턴 없음). **★ 58%가 게임 내내 우주선 한번도 안 탐**, 초반탑승 8%.
- **진단:** 봇 오프닝에 coherent 다턴계획 없음(사용자 관찰 확증). 우주선 격차는 (a)입장점수(50+best*0.5)<빌드(~300)라 후순위 + (b)우주선=내부정션이라 reach부족시 후보 미생성 → **reach 약점(문서화된 근본벽)과 직결**.
- **r1ShipPriority(flag OFF, 실험):** R1-2 우주선 입장 +180(빌드 경쟁권), Rebellion Nav+1 +90(reach 직격=사용자 "거의 이기는 액션"). boarding률 self-play검증 + VP는 1:3(self-play는 contention 못 재현, shipTechEntryValue −9.5 전례).
- 주의: 단일 nudge 5종 전멸 전례 → 이것도 self-play VP는 중립/음수 가능. 핵심은 boarding률 발화 확인 + 사용자 1:3 판정.

## 2026-06-27 ✅ 채택 r1ShipPriority (기본 ON) — R1-2 우주선 입장 우선 (사람 R1 95% 탑승 패턴)
- 레버: R1-2 우주선 입장 score +180(빌드 ~300과 경쟁권), Rebellion Nav+1 미보유시 +90(reach 약점 직격=사용자 "거의 이기는 액션"), 기타 미보유 기술타일 +50.
- **검증: 28판 +3.39(63%) → 60판 +2.70(55%), 88판 일관 양성**(winner's curse로 3.39→2.70 안착). 행동검증: 우주선액션 ▲(3.77→4.91 1차, 4.49→4.71 2차), 광산 약간↓(템포 일부 양보). flagsDiffer/weightsDiffer OK.
- 판정: 음수 아님+일관양성+도메인옳음(사람 R1탑승 95% vs 봇 58% 미탑승)+메커니즘 발화 → **채택**. shipTechEntryValue(−9.5)와 대조: 그건 tech-only+mis-calibration, 이건 R1-2 전체우선+실제 액션사용 동반이라 성공.
- ★ self-play에서도 +2.7 — "우주선 격차는 contention뿐"이 아니라 **봇이 애초에 R1 우주선을 안 타는(58%) 진짜 행동격차**였음(과거 진단 일부 교정). 사람 오프닝 모방이 self-play로도 측정 가능한 첫 사례.

## 2026-06-27 알파고 정책망 경로 — imitation 신호 게이트 근접 (+7.1%p)
- imitationProbe(사람 35게임 3385샘플): base 20.2% → v1 스칼라 kNN 25.8%(+5.6%p) → v2 +엔진누적피처 27.3%(+7.1%p). 추이: 8게임 +3.9 → 35게임 +7.1 (데이터+피처로 상승).
- 게이트(+8%p, "정책망 풀빌드 정당화") 근접. **맵 위치피처(인접·reach) 없이 7.1%p** → replay 파이프라인으로 맵피처 넣으면 넘을 듯. 사용자 1:3 데이터 성장도 기여.
- 다음 브릭: replay 파이프라인(사람게임 엔진 재생→결정시점 맵상태 피처) → 정책망 학습 → MCTS PUCT prior(알파고 구조). mcts.ts는 사용자 영역이라 후보 prior는 bot.ts에 구현.

## 2026-06-27 알파고 정책망 — 맵 피처 replay 파이프라인(imitationProbe3) 빌드 + 측정
- **인프라:** 사람게임 actionJournal을 forward 재생해 결정시점 소유타일 복원(최종 g.map geometry 사용) → 맵피처(소유 행성유형 다양성·섹터분포·우주선/프로토 거리·인접클러스터). 21게임 맵보유(06-25+), 2147 결정샘플.
- **결과:** v2(스칼라+엔진) +6.9%p → v3(+맵) +7.4%p. 맵효과 +0.5%p(marginal). **게이트(+8%p) 미달.**
- **해석:** 정책 신호 추이 8게임+3.9 → 35게임 스칼라+5.6 → +엔진+7.1 → +맵+7.4. **데이터로 꾸준히 상승, 게이트 근접하나 kNN으론 못 넘음.** kNN은 약한 학습기(하한) → 트레인드 net/더 많은 데이터로 넘을 여지. 맵피처가 kNN서 약한 건 local 거리기반의 한계일 수 있음.
- **알파고 경로 현 위치:** ①MCTS ✅ ②데이터 자가대국124MB+사람35(맵21) ③정책망 신호 +7.4%p(게이트근접, 데이터성장 의존) ④가치망 ≈그리디. **다음 레버: (a)사람 1:3 더(신호는 데이터로 자람-입증) (b)트레인드 정책망(kNN아닌 softmax/MLP)으로 같은피처서 더 짜기 (c)그게 게이트 넘으면 bot.ts 후보 prior로 연결(mcts.ts는 사용자영역).**
- 즉효 병행: 사람패턴 직접주입(r1ShipPriority +2.7~3.4 채택)이 정책망 성숙 전 실득점. 정책망이 결국 이런 패턴을 자동학습하는 게 목표.

## 2026-06-27 ★★★ 알파고 정책망 viability 게이트 돌파 (트레인드 softmax +10.1%p)
- **kNN(+7.4%p)은 약한 하한이었음. 트레인드 softmax 분류기(5-fold by game, 21게임 2147샘플, v3피처)= 31.8% = base+10.1%p → 게이트(+8%p) 돌파.** (imitationProbe4.mjs, 순수JS GD)
- **의미:** 사람 actionJournal+맵피처로 학습한 정책망이 다음 행동타입을 base보다 +10%p 예측 = MCTS PUCT prior로 쓸 신호 충분. **그리디→다중탐색(알파고) 전환의 데이터 게이트 공식 통과.**
- **다음 빌드(green-lit):** ①정책망 학습+직렬화(softmax 가중치 → policyNet.json) ②bot.ts getCandidateMoves에서 상태피처→행동타입 prior 계산해 후보 점수에 prior 가산(mcts.ts는 사용자영역이라 후보단계 prior) ③head2head VP검증(prior가 실제 도움인지 — useContentionPrior 등 prior 워시 전례 있어 검증 필수) ④사람 1:3 데이터 누적시 정책망 재학습(신호는 데이터로 자람: 3.9→10.1).
- 한계: 정책은 행동'타입'(광산/연구/연방..) 예측이라 coarse(특정 타일/트랙 아님) → 후보 카테고리 prior로만. 그래도 탐색 좁히기엔 유효.

## 2026-06-27 ❌ policyPrior (정책망 후보 재정렬 통합) — 기각 (−10.75 p=0.003)
- 정책망(+10.1%p viable)을 bot.ts getCandidateMoves에서 후보를 행동타입 정책확률 순으로 안정정렬(mcts.ts 미수정).
- **30판: 승률 20%(6:24), VP −10.75(p=0.003) 강한 음성.** 행동: 광산 −0.82·연구 −0.27·총행동 −1.17(우주선만 +0.6).
- **원인:** 정책은 행동'타입'만 예측(특정 수 아님). 타입확률 재정렬이 **휴리스틱이 고른 타입내 최선수를 롤아웃 top-N(8) 창 밖으로 밀어냄** → 롤아웃이 나쁜 수 평가. 휴리스틱 cross-type 우선순위 파괴.
- **결론:** 정책망은 viable 신호지만, **싼 후보재정렬 통합은 실패.** 제대로 된 알파고 통합 = MCTS 선택식 PUCT prior(Q + c·P·√N/(1+n))로 휴리스틱 Q와 *블렌드*(순서 교체 아님) — 이건 mcts.ts(사용자 영역) 수정 필요. policyPrior flag OFF 유지(코드/정책망 보존). 
- **다음:** 사용자 mcts.ts 동의 시 PUCT prior 통합(정책 P를 selection에 블렌드, 휴리스틱 Q 보존). 또는 정책망 신호 더 키우기(데이터↑/특정수 예측으로 정밀화).

## 2026-06-27 알파고 PUCT 통합 — 빌드+클린 검증 OK, 타입레벨 정책은 prior로 불충분 (default OFF)
- mcts.ts bestUCT에 PUCT 정책 prior 항(Q보존+blend, root자식만, 가중치 policyPUCTw). 사용자 mcts.ts 승인+diff확인+커밋(flag OFF=무영향).
- **검증: w=150 VP −0.03(완전중립, prior가 raw-eval Q ~5000에 묻힘) / w=800 VP −2.85(prior 영향시작-우주선액션+0.92-하나 VP 음성).** 후보재정렬(−10.75)과 달리 **catastrophic 실패는 없음**(제대로 blend) but 도움도 없음.
- **핵심:** 정책망이 행동'타입'(11종)만 예측 → 휴리스틱이 이미 타입내 최선수 아는데 타입쪽 nudge는 무익~해. **타입레벨 정책은 PUCT prior로 불충분.** 진짜 알파고 이득엔 **특정 수 예측(per-candidate) 정책** 필요(풍부한 피처+더 많은 데이터).
- **인프라 가치:** 정책망 학습→직렬화→PUCT 통합 파이프라인 완성+클린 검증. 정책을 per-candidate로 정밀화하면 바로 재사용 가능. policyPUCT/policyPrior flag OFF 유지(코드 보존).
- **다음 알파고 레버:** (1)정책 라벨 granular화(행동타입+서브타깃: 연구트랙·행성유형) 또는 per-candidate 피처 정책 (2)사람 1:3 데이터 계속(신호 데이터로 자람 3.9→10.1) (3)그 위에 PUCT 재검증.

## 2026-06-27 ★★★ 채택 policyPUCT (기본 ON, w=500) — 알파고 정책-가이드 MCTS 첫 성공
- granular 정책망(연구 트랙별 분할, 15클래스)을 mcts.ts PUCT prior로. **타입레벨(coarse) PUCT w800=−2.85 였으나, 연구트랙 분할(granular)로 정밀화하니 w500=양성 전환.**
- **검증: w500 30판 +2.12(50%) → 60판 +1.47, 승률 61%(36:23, p=0.091 거의유의). 90판 일관 양성**(winner's curse 2.12→1.47 안착).
- 판정: 음수아님+일관양성(90판)+승률61%+도메인옳음(정책-가이드 탐색, 연구트랙 약점 직격)+메커니즘(PUCT blend, Q보존) → **채택.** 
- **★ 알파고 마일스톤:** 봇이 그리디 휴리스틱 Q + 사람 모방 정책망 prior를 MCTS PUCT로 결합해 탐색 → 점수 향상. "그리디 탈피 다중탐색"(사용자 goal) 구조적 첫 실득점. 후보재정렬(−10.75)→타입PUCT(−2.85)→granularPUCT(+1.47)의 진화로 도달.
- **다음 증폭:** (1)빌드도 행성유형/new-type 분할 등 정책 더 정밀화 (2)가중치 미세튜닝(300~700 스윕) (3)사람 1:3 데이터로 정책 재학습(신호 자람) (4)per-candidate replay로 특정-수 정책. 각 단계가 PUCT 이득 키움.

## 2026-06-27 ❌ 정책망 빌드분할(Build:new/old, 16클래스) — 기각, research-split 복원
- 연구트랙 분할이 통했으니(+1.47) 빌드도 'Build:새유형 vs 기존'으로 분할 시도(683샘플 데이터풍부+빌드타깃 약점).
- **검증(40판, challenger=policyPUCT OFF vs 챔피언=빌드분할 ON): VP +4.39(OFF가 강함!) = 빌드분할 PUCT가 −4.39로 악화.** research-split(+1.47)과 정반대.
- **원인:** 16클래스로 과분할 → train acc 27.3%(research-split)→23.8% 하락 = 정책 noisier → prior 악화. **교훈: 정책 granularity는 sweet spot 있음 — research-split(15클래스)이 최적, 더 쪼개면 데이터분산으로 역효과.**
- 조치: trainPolicyNet/bot.ts/policyNet.json을 0edc8e6(research-split +1.47)로 복원. policyPUCT 기본 ON 유지.
- **세션 누적(측정): 현재봇 75.0 vs 세션시작(r1ShipPriority+policyPUCT OFF) 70.7 = +4.36VP.** 알파고 구조전환 + 검증된 향상.
- **다음(데이터 자라면): 사람 1:3↑ → 정책망 재학습(신호 3.9→10.1로 자람), 가중치 미세튜닝, per-candidate replay(특정수).** 

## 2026-06-27 policyPUCTw 가중치 스윕 — w=500 유지 (튜닝 이득 없음)
- 1번(가중치튜닝): challenger w vs 챔피언 w=500. w=300 30판 +1.33, w=800 30판 +3.97(유망) → **w=800 60판 −1.70**(회귀). 30판 +3.97은 winner's curse. 300~800 노이즈범위 평평 → **w=500 유지.**
- 2번(per-candidate 정책): **정확한 후보집합 복원=엔진replay 불가**(사람JSON에 중간 풀/기술타일/파워액션 상태 미저장, getCandidateMoves 20+필드 필요). 빌드타깃 랭커는 reach약점에 묶여 비추천(빌드분할 −4.39 전례). **→ 진짜 증폭경로 = research-track처럼 reach-무관+데이터충분 결정의 granular 정책**(다음 후보: 연방보상/기술타일 선택). 과분할 주의(한번에 하나).
- 현 알파고 채택 상태 확정: policyPUCT(w=500, research-split 15클래스 net) + r1ShipPriority. 세션누적 +4.36VP.

## 2026-06-27 ★★ 채택 power-action granular 정책 (자율 실험 성공)
- 정책망에 power선택 6범주(Pw:ore/credits/knowledge/tokens/tf/qic) 추가 → 19클래스. (research-split 유지)
- **검증(challenger=OFF vs 챔피언=power+research ON): 40판 −3.30, 60판 −2.81 (100판 일관, 57%승) = PUCT 효과 ~+2.8~3.3. research-only(+1.47) 대비 +1.4 개선.** → 채택(net 커밋 유지).
- **★★ 핵심 원칙 발견:** granular 정책 성패 = **"클래스 수"가 아니라 "features가 그 결정을 설명하느냐"**. 
  - research트랙(자원/지식기반)·power선택(자원기반) → features有 → 통함(+1.47/+2.8)
  - 빌드타깃(맵위치기반) → features不足(좌표 미사용) → 실패(−4.39)
  - **다음 granular 후보: features로 설명되는 결정** — 연방보상(자원), 기술타일(연구상태), 업글타깃(자원). 맵위치 결정(빌드/우주선배치)은 per-candidate 맵피처 필요(replay 막힘).
- 현 알파고 상태: policyPUCT(w500, power+research 19클래스 net) + r1ShipPriority. PUCT 자체효과 +2.8(↑ from +1.47).
- 데이터: 사람게임 무제한 보존(61MB/35게임, 자동삭제X) → 1:3 누적이 정책 직접강화. 용량 무문제.

## 2026-06-27 야간 자율: 봇 good-vs-bad 게임 대조 (사용자 지시)
- 봇 자기게임 8788명 상위25%(VP94) vs 하위25%(VP43): 연구합+2.9·구조물+2.6·광산+1.9·기술+1.2·연방+0.8.
- **★엔진통제(구조물9-11, 4222명): 같은 엔진인데 VP +45.7 격차. 정책 차이 = 연구합+2.5(최대)·기술타일+1.0·최고연구레벨+0.6·연방+0.5·잔여파워−1.7(하위 파워낭비). 우주선/크레딧잔여는 동일.**
- 해석: 같은 엔진이라도 좋은 게임은 연구 더 깊게+기술타일 더+파워 덜 쟁임. 단 연구깊이(L5)는 fed게이트, 연구합은 일부 lab조성. 우주선·크레딧은 차이없음(r1Ship·ore-credit fix로 이미 다룸).
- 후보 레버: 연구(researchBreadth=15 이미채택, 추가여지?)·잔여파워 활용(power-forcing 과거 −10/−2.5 실패, 단 R6한정은 미시도)·기술타일(엔진게이트).

## 2026-06-27 야간: 세션 누적 120판 확정 + 종족 피처 추가
- **누적 120판: 현재봇(r1Ship+policyPUCT power-split) 73.3 vs 세션시작 70.5 = +2.83VP, 55%(66:54), p=0.104.** 작은표본 노이즈(+0.29/−4.36)가 120판서 +2.83 안착 = 세션 진짜 이득 ~+2.8VP 확정(winner's curse 부풀림 제거).
- **종족 피처 추가(사용자 지시):** 사람 종족별 플레이 뚜렷이 다름 — nevlas 파워9.4(vs~5)·terran 가이아9.0·ambas 연방11.7·firaks 연구소6.3·lantids 광산8(최저)/연구10.5. 정책망에 종족 one-hot(18) 추가 → featDim 26→44, train acc 27.8%. 검증중(working-dir, git HEAD는 power-split 폴백).

## 2026-06-27 야간: ❌ 종족 피처(one-hot 18) — 과적합으로 악화, 되돌림
- 사용자 지시로 종족 one-hot 18개를 정책망 피처에 추가(featDim 26→44). 분석상 종족별 플레이 뚜렷이 다름(nevlas파워·terran가이아·ambas연방).
- **검증(challenger OFF vs 종족net ON, 60판): VP +1.12(OFF 이김) = 종족피처가 PUCT를 악화.** power-split(+2.8 ON유리)에서 뒤집힘.
- **원인: 종족당 ~120샘플(2147/18)로 18피처 과적합.** 종족차이는 실재하나 현 데이터론 학습 불가. **데이터 더 모이면(사용자 1:3) 재시도 가치O** — 종족피처 코드는 git에 남기지 않되 이 교훈 기록(데이터 게이트).
- 되돌림: bot.ts/trainPolicyNet/policyNet.json → power-split(검증된 +2.8) 복원.
- ★교훈 보강: feature 추가도 "데이터 대비 과다하면 과적합"(클래스 과분할과 같은 한계). 종족(18) > 데이터 여력. research/power 분할은 OK였던 건 클래스가 적고 feature증가 없어서.

## 2026-06-27 ⚠️★ 정정: policyPUCT 정책망 — 120판서 winner's curse 드러남, 기본 OFF로 되돌림
- **policyPUCT 격리: 40판 −3.30 → 60판 −2.81 → 120판 +1.45(OFF 유리로 부호 뒤집힘).** 220판 풀링 ≈ +0.58 = 중립. 소표본 +2.8은 winner's curse였음(스킬 1원칙: 120판이 진실, 소표본 양수 회귀).
- **정책망(오늘 "알파고 성과")은 큰 표본서 중립~약음성.** 과대평가였음. 정직 정정.
- **누적 120판 +2.83의 실제 동력 = r1ShipPriority**(역산 ~+4.3, 직접검증 88판 +2.7~3.4). r1Ship은 진짜 강한 채택(유지). policyPUCT는 중립이라 연산비용만 → **기본 OFF.**
- 인프라(정책망 학습/PUCT/per-candidate 캡처)는 보존 — **데이터 부족이 근본**(2147샘플, 종족피처도 과적합). 1:3 데이터 크게 모이면 정책망 재학습→PUCT 재검증 가치O.
- ★교훈: 이번 세션 정책망 채택들(research-split +1.47, power-split +2.8, w튜닝)이 다 40-60판 기반이었음 → winner's curse 취약. **policyPUCT 계열은 전부 120판 미달 = 신뢰 보류.** 진짜 검증된 세션 성과 = r1ShipPriority + 버그수정들(ship배치·3오레·오레기아·ship-fed).

## 2026-06-27 ⚠️★★ 밤샘 정직 결산: flag 채택들 120판서 전부 ~중립 (소표본 winner's curse)
- **r1ShipPriority 120판 격리: −0.25 (중립).** 88판 +2.7~3.4도 회귀. policyPUCT 120판 −1.45와 함께 → **오늘 세션 flag 채택(r1Ship·policyPUCT)이 큰표본서 전부 중립.**
- 누적 120판 +2.83도 노이즈 높은 draw였음(개별 격리가 중립인데 합이 +2.83일 수 없음 = ±1.9 노이즈가 셋 다 흔듦).
- **★결정적 교훈(스킬 1원칙 재확인): self-play A/B는 120판+ 아니면 신뢰 불가. 40-88판 양수는 거의 회귀.** 이번 세션 내 모든 정책 채택이 이 함정에 걸렸음.
- **진짜 검증된 세션 성과 = flag튜닝이 아니라 버그/룰 수정**(self-play VP로 안 잡히나 명백히 옳음): 맵생성 우주선 거리>3, R1-2 3오레 차단, 오레기아 교역소나선, ship-fed 연방, proto, nevlas/taklons 조기패스. + 인프라(candidate캡처·정책망·dev:nowatch).
- **채택 처리:** policyPUCT → OFF(중립+연산비용). r1ShipPriority → **ON 유지**(중립이나 do-no-harm + 도메인옳음[사람 R1우주선95%] + 실전 우주선경쟁 가치 — self-play는 contention 못 재현). 버그수정 전부 유지.
- **근본 결론: self-play 튜닝으로 짜낼 VP는 사실상 없음(전부 노이즈/포화). 봇 강화의 유일한 실레버 = 사람 1:3 데이터**(정책망 재학습·종족·per-candidate 다 데이터 게이트). 인프라는 다 깔림.

## 2026-06-27 ❌ 종족조건부 policyPUCT — 기각 (per-faction 효과 대부분 노이즈, forcing 2/3 회귀)
- 사용자 통찰("평균이 종족효과 가림") 검증: 120판 종족분해서 policyPUCT/r1Ship이 종족별 ±10~15로 갈려보임.
- **forcing 확인(n~30): taklons+policyPUCT +10.8→+5.9(홀로 유지) / geodens+policyPUCT +8.0→−1.7(회귀) / nevlas+r1Ship +15.7→−1.3(회귀).** 3중 2 회귀.
- **결론: per-faction "효과"도 대부분 소표본 노이즈**(종족당 n~10이 per-game ±20 분산으로 가짜패턴). 120판 종족사인으로 게이트하면 false-positive(geodens)로 손해. taklons 1종족(1/18, 우연일 수도)만 위해 게이트 = winner's curse 함정 → 미채택, 코드 revert.
- ★★ 최종 확정: **self-play VP 튜닝은 평균이든 종족별이든 신뢰가능한 이득 없음(완전 포화).** 정책망(평균 중립, 종족별 노이즈)·flag(r1Ship/policyPUCT 120판 중립)·가중치 전부. **봇 강화 유일 실레버 = 사람 1:3 데이터.** 인프라(정책망·PUCT·candidate캡처) 다 보존, 데이터 게이트.

## 2026-06-27 ★ 종족별 망 질문 — 데이터가 근본 병목 (종족당 평균 126샘플, 일부 1게임)
- 사용자: "좋은 종족(taklons) 있으니 종족별 망 가져야?" — 개념상 맞으나 데이터 불가.
- **종족별 학습 샘플: taklons 61(1게임)·itars 56(1게임)·darkanians 46(1게임)·bal_tak 41(1게임) ... 최대 gleens 234(5게임). 평균 126.**
- 종족별 softmax(26피처×19클래스 ≈500+파라미터)는 수천 샘플 필요 → **126샘플론 과적합 확정.** taklons +5.9도 1게임 데이터 기반 → 노이즈 의심.
- **공유망+종족one-hot이 데이터효율적 종족조건부인데 그것도 2147서 과적합**(앞 실험). 종족별 분리망은 데이터 더 적어 더 심함.
- ★결론: 종족별/조건부 학습 전부 **데이터 게이트**(종족당 1-5게임, 필요 ~10-50). 모든 정책 시도가 같은 벽. **유일 해법 = 사람 1:3 더(특히 종족 다양/반복).** candidate캡처 인프라로 플레이=자동적립.

## 2026-06-28 ❌ fedBridge 묘수(연방 브리징 정확탐색) — 기각, 그러나 핵심 통찰
- 묘수: getBestFederationAction what-if로 "빈타일 빌드가 연방 가능케 하나" 정확탐색(1+2빌드). 사람 R3 묘수(2채→7파워→연방) 재현.
- **1빌드판: 연방 +0.00(케이스 드묾). 2빌드판: 연방 +0.09·광산 +0.54인데 VP −3.59.** 강제가 손해.
- **★핵심 원인 2가지:** ①브리징 케이스 자체가 드묾(클러스터 흩어짐=reach 상류문제) ②강제로 tile1 짓고도 **다음 턴 tile2+연방을 안 이어감**(광산+0.54 vs 연방+0.09=대부분 미완성 낭비) → **봇이 다턴 계획을 commit 못 함**(매턴 재결정).
- **★★ 통합 결론:** 연방(봇#1약점) = reach/흩어진확장 + 다턴 commit불가의 하류. 정확탐색은 *한 시점* 푸나 *여러 턴 실행*을 못 함. buildOrderPlanner(−4.14)와 같은 실패모드(강제 빌드 disruption). **튜닝·학습·LLM·정확탐색 전부 이 "다턴 좌표계획+commit" 벽에 막힘** — 이게 greedy 1-ply 아키텍처의 진짜 천장. 돌파엔 다턴 plan을 *상태로 유지하며 실행*하는 구조(또는 충분한 학습데이터) 필요.
- fedBridge 기본 OFF(코드 보존). what-if 헬퍼(buildEnablesFederation)는 재사용 가능.

## 2026-06-28 오프닝 배치 (사용자 관찰: 광산위치 엉망/외각 2O6C TS)
- startPlacementExpansion 기본 ON: 시작광산을 싸고 가까운 확장지 우선. **do-no-harm −0.21(중립)**, ON 유지(placement는 self-play 못판정, 1:3 판정).
- ❌ buildNearShipOpp(우주선+55/상대dist2 근처 빌드보너스) 기각: **VP −1.76 + 우주선액션 −0.84·총행동 −1.47(역효과).** 원인: "우주선 옆에 짓기 ≠ 타기" — 옆 나쁜타일 빌드로 액션낭비→엔진약화→오히려 우주선 덜 탐. 단순 proximity 보너스가 backfire. flag OFF.
- ★사용자 통찰(같은비용→상대인접[파워+TS할인]·우주선근처 우선, 외각기피)은 옳음. 단 placement는 scoring 한 항 튜닝이 다른 걸 왜곡 → fedCompactBuild(−4.22)·buildNearShipOpp(−1.76) 다 실패 패턴. 진짜 해법: 외각 *페널티*(proximity 보너스 아니라) 또는 placement를 다턴 계획으로(같은 벽) 또는 사용자 특정 bad-case 외과수정.

## 2026-06-28 ✅ buildNearShip 기본ON (데이터 교정판) — 사람 첫집 패턴 기반
- 사람 R1 첫광산 분석(39건): 우주선 dist≤2에 51%(평균2.2)·자기건물 dist≤3에 80%, 상대는 무관(26%/분산).
- 교정: 우주선 proximity만(상대항 제거=이전 buildNearShipOpp −1.76의 원인), 약하게(30/20/8 ← 55/30/12), R1-2 한정.
- **do-no-harm −0.38(중립, 이전 −1.76서 교정)**, 우주선입장 +0.09. placement는 self-play 못판정 → do-no-harm+데이터기반이라 ON, **1:3 판정.**
- 현 오프닝 배치 채택 2: startPlacementExpansion(−0.21) + buildNearShip(−0.38). 둘 다 do-no-harm+사용자관찰/사람데이터 기반. 사용자 1:3서 "외각 2O6C TS↓ + 우주선/확장지 근처↑" 확인 대기.
- 교훈: placement 보너스는 약하게+데이터범위로(강하면 엔진왜곡 backfire). 상대 proximity는 첫집 무관(데이터). 자기건물 군집은 기존 adjacencyBonus가 담당.

## 2026-06-28 ★ 다턴 계획엔진 brick1 (persistent 연방완성 plan) — 중립, 핵심 아키텍처 발견
- multiTurnPlan: 7파워 빌드set을 game에 저장→여러턴 commit(fedBridge −3.59의 "commit 안함" 교정).
- **50판: VP −0.29, 연방 1.48→1.43(−0.05 안늚!), 광산 +0.67(브리징 지음), 총행동 −0.79.**
- ★발견: **commit해서 브리징 광산을 실제 지었는데도 연방 안 생김** → "다턴 commit 부족"이 문제가 아니라, **봇 클러스터가 흩어져 2채 브리징해도 7파워 연결 불가(위성비용 폭발)**. 사람 R3는 클러스터가 *가까워서*(좋은 배치) 2채로 1위성 연방됨. 봇은 흩어진 배치라 브리징해도 연방 안 됨.
- **★★ 결론: 연방/계획 다 *배치(placement/reach/흩어짐)*의 하류.** 계획엔진의 자연스런 타깃(연방)은 증상. 진짜 병=상류 배치 좌표. 그걸 hand-code하는 placement-coordination planner는 (a)가장 어려운 핵심 (b)self-play 검증불가 (c)reach 벽 그대로일 위험. **→ 더 유망한 아키텍처 = 학습(정책/가치망이 배치좌표를 *암묵* 학습, 알파제로식). 데이터 게이트(사용자 1:3).** multiTurnPlan/fedBridge flag OFF, 코드보존.

## 2026-06-28 ★ isolatedTSPenalty 채택(기본 ON) — 외곽 고립 빌드 강한 패널티
- 사용자 라이브 관찰: 스자(Space Giants)가 2Nav로 외곽 가이아 점프, 왼쪽(상대옆 3원 TS+leech) 무시.
- 룰(calculateAdjacencyBonus): 상대건물 인접 아님(=TS 2ore/3credit 할인 불가) AND 내건물 dist≤2도 아님 = 진짜 외곽 → bonus −150. 내클러스터/상대옆 빌드는 예외. R1-4 한정.
- **do-no-harm 50판: VP +0.94(중립 p=0.72), 광산 −0.49(외곽빌드 실제 억제), 가이아포밍 +0.14.** 행동변화 확인 + 비음수 → 채택.
- ※ leech/TS할인/포지션 가치는 self-play로 안 잡힘(과거 buildNearShipOpp −1.76) → **진짜 판정은 사용자 1:3.** flag로 끄면 구동작.

## 2026-06-28 ★ humanResearchPrior 채택(기본 ON) — 첫 연구를 사람처럼 nav/gaia로 (데이터 직접 박음)
- 사용자 지적: "사람 로그 보면 아무도 지식 안 올리는데 왜 안 고쳐지나". → 사람 35판 직접 분석.
- **사람 첫 연구 분포: nav 41% / gaia 32% / terra 11% / eco 9% / science 6% / AI 2%. 전체 누적도 science 8%(최하위).**
- 봇 공식은 science ×22(L0R1 162점)로 첫 연구를 지식에 몰아 사람과 정반대였음 → 교정: science ×22→×12 환원+초반보너스축소, R1-2 nav+95/gaia+85/eco−40. 새 ordering nav155>gaia133>terra127>eco115>science82.
- **do-no-harm 50판: 도전자(구 science몰빵) +2.08 VP(p=0.43 노이즈). 즉 self-play는 구를 살짝 선호.** 가이아포밍+0.19/광산−0.38(확장연구로 이동 확인).
- ★ 채택 근거: (1)사람 데이터 명백(science 6% 첫/8% 전체) (2)실게임 봇 연구VP 이미 1/3(13.5 vs 37)→science먼저가 실전가치 아님, +2.08은 봇끼리 인공물 (3)self-play는 확장/연구전략 가치 판정 불가(합의됨) (4)research 단독효과 없음=배치(isolatedTSPenalty)와 함께 가야. **진짜판정=사용자 1:3. 실게임 악화 시 flag OFF로 즉시 복원.**

## 2026-06-28 ★ advTechChain 채택(기본 ON) — L3+초록연방이면 고급타일 자격(L4) 만들러 3→4 강하게
- 사용자 라이브관찰: 봇이 연방보유+기술L3+4지식+연구소/아카 자원 있는데 R4에 일반 수익타일 먹음. "4지식으로 L4 가서 고급타일 먹어야".
- 갭: calculateResearchScore의 초록연방 ×2.0 폭발가중이 level===4(4→5)에만 → "3→4로 *자격 자체를 만드는*" 결정적 수를 +55로만 약평가. 고급타일은 ~30VP.
- 수정: level===3 && 초록연방≥1 && 그 트랙 위 미보유 고급타일 → score +110.
- **do-no-harm 50판: VP +0.43(중립 p=0.85), 기술타일 +0.36·연구소 +0.20(기술쪽 이동 확인).** 
- ★ **self-play 고급타일 선택 = 0건(50판 전체)** — 봇끼리는 초록연방+L4 자격을 거의 못 갖춰 고급타일 경로가 안 돌아감(=techTiles 3.3 vs 40.5의 정체: 못 고르는 게 아니라 자격을 못 만듦). **사용자 실게임은 그 상태(연방+L3+4지식)가 실제로 생김** → self-play는 일반기술 do-no-harm만 확인, **진짜판정=1:3.** 악화시 flag OFF.

## 2026-06-28 ★ placementPolicy(학습 배치랭커 통합) 기각 — 가치피처 없어 역효과
- 사람22판 656결정서 학습한 8-피처 선형랭커(top1 17.8% vs random 5%)를 빌드타일 스코어에 +Σw*feat×60 추가.
- **do-no-harm 40판: VP −7.10 (p=0.007 유의!), 광산 +0.61(뭉치긴 함).** 명확한 손해 → 기각.
- ★원인: 랭커는 *상대위치*만 학습, *타일가치*(가이아/proto/미션/테라폼) 모름 → ×60으로 가치 무시하고 가까운 나쁜타일에 뭉쳐 +80VP짜리 먼 가이아/미션 버림.
- ★★교훈: (1)position-only 모방을 value-driven 봇에 강하게 박으면 역효과(humanResearchPrior −2.08과 같은 family, 더 큼). (2)**추측이지만 surgical한 isolatedTSPenalty(극단고립만 −150, 가치보존)=+0.94가 오히려 정답.** data probe는 "밀집이 압도적 신호"라는 *방향*만 확증(isolatedTSPenalty 설계 정당화). (3)학습정책을 쓰려면 가치피처 포함 재학습 or 전체 정책망(후보생성 자체 대체)이어야. placementPolicy flag OFF 코드보존.

## 2026-06-28 ★ advTileOverL5 채택(기본 ON) — green을 L5 대신 고급타일에 (3관문 + value-aware)
- 사용자 관찰: 봇 R6에 고급타일(10+VP) 안 먹고 광산 짓다 패스. 데이터 확증: 봇 고급타일 0건 vs 사람 95, green 21개 미사용, L5도달 15.
- 사용자 의문 "왜 L5가 고급타일보다 늘 높나" → calculateResearchScore의 level4 green ×2.0이 L5를 ~200으로 띄워 둘 다 green쓰는 고급타일(~75-120) 압도. 주석은 "고급위해 L5"였지만 정반대(L5가 green 소모).
- 수정 3관문(전부 value-aware, scoreAdvancedTechTile≥85 좋은타일만): ①green1개뿐+좋은고급 있으면 L5 ×2.0 부스트 끔(green 양보) ②green+L4+좋은고급이면 연구소/아카 빌드 +130(tech-gain 트리거 생성) ③findTechTileAction에서 좋은고급(≥85)이면 표준보다 우선 선택.
- **do-no-harm 40판: VP +4.35(p=0.14 노이즈). ★단 고급타일 선택 여전히 0** — 강제선택까지 넣어도 0 = 봇이 self-play서 전제상태(green+L4+좋은고급) 자체에 도달 안함(엔진 벽, advTechChain과 동일). 
- 채택 근거: 논리적 정확+value-aware(나쁜고급 안 먹음, 사용자 우려)+do-no-harm+확인된 최대갭(0vs95). **VP증명은 self-play 불가(전제 희박)→사용자 1:3 유일 판정.** 1:3서도 안 먹으면 threshold 85가 높은 것→낮추기. flag OFF로 구동작.

## 2026-06-28 밤 자율 배치 (사용자 취침, 미검증분 일괄 do-no-harm)
- **fedTrimSatPath 채택(ON)**: 위성경로 연방 과포함 트림(모웨이드 첫연방 +1위성·8파워 관찰). BFS 연결컴포넌트 통째병합→트림. 40판 VP-1.11(노이즈), 연방 1.44vs1.46(안깨짐), 에러0(폴백작동). 버그수정+안전+논리적 → ON. 진짜판정 1:3.
- **gaiaformPreSpend 되돌림(OFF)**: 무검증 "순이득"이라 ON했는데 40판 ON이 -5.24(p=0.11). 추정원인: 변환을 *크레딧*으로 해서 크레딧과잉(죽음의나선) 악화 — 저가치 자원추가가 역효과. 자기교정→OFF. (deficit 1엔 3P→1ore 불가라 credit밖에 못썼던 게 한계.)
- **academyTypeChoice 유지(ON)**: 도전자OFF +0.49(중립), 연방 1.54vs1.48. 사용자룰(기본 left지식, right QIC는 리벨리온/R5+) 도메인옳음 → 유지.
- 전부 에러0(game_error 없음 — 폴백/가드 안전).

## 2026-06-29 deepRollout (사용자 아이디어 "내 액션만 깊게" 직접 검증) — 기각
- **deepRollout+realRolloutIncome 기각(OFF)**: 테란고정 80판 VP **−7.45 (p=0.001)**, 승률 33%. 명확 악화.
  행동표: 파워액션 3.21→1.87(Δ−1.34), 우주선액션 4.37→5.81(Δ+1.44), 광산 −0.41(QIC 안고쳐짐).
  원인 ①깊이5배→400ms내 MCTS반복 급감(트리 노이즈↑) ②가상턴 사이 수입이 근사라 5수 fantasy가
  "우주선액션 반복=좋음"으로 오도→일회성/경합 파워액션 버림. 현실 안 맞는 시퀀스 최적화.
  교훈: 자기-only 다턴 롤아웃은 방향은 맞으나 **정확한 forward모델+충분연산 없이 근사로 깊게 파면 환상 추종**.
  → 라운드 예산 플래너(명시적 자원계산)가 deepRollout(fuzzy 롤아웃)보다 나은 근거.
- **navLookaheadTier 코드 인입(미검증, OFF)**: calculateResearchScore 항해평가의 tier-skip 버그 수정
  (getRange 0·1→1 묶임 탓 nav0에서 level+1만 보면 range 불변→항해 저평가→QIC점프 후 뒤늦게 nav2).
  willNavResearchSaveQIC가 이미 고친 로직을 점수기에 이식. deepRollout 기각 후 별도 검증 대기.

## 2026-06-29 deepRollout 정련 3단계 — 최종 기각 (구조적 한계 확인)
- 사용자 아이디어("상대 빼고 내 액션만 깊게 = 이번라운드 X+Y+Z 계획")를 정련:
  - 원판 deepRollout+realRolloutIncome: 테란80판 **−7.45**(진범=applyRolloutIncome을 체인 매-액션마다 호출→4라운드치 수입환상).
  - +deepRollCurRes(수입환상 제거, 현재자원으로만 연쇄): 테란80판 **−1.89**(무해 회복).
  - +deepRollCap=3(체인 단축→반복회복+그리디증폭완화): 테란80판 **+1.38**(광산+0.44/파워−0.07, 건강).
  - **120판 종족혼합 확정: −1.39 (p=0.44, 승률45%)** → 테란 +1.38은 평균회귀로 뒤집힘. 광산+0.03(테란한정),
    파워액션 −0.59 부활, 우주선액션 +0.97 — **그리디 자기-연쇄가 평가기의 우주선액션 편향을 증폭하는 구조적 한계(종족불문).**
- **판정: deepRollout 계열 전부 기각(OFF 유지).** 깊게 팔수록 평가기 편향만 증폭, 더 똑똑해지지 않음.
- **산출물**: ① 매-액션 수입환상 버그 규명(realRolloutIncome 쓰면 누구나 영향, 현재 default OFF라 휴면).
  ② **명시적 라운드 예산 플래너 > fuzzy 롤아웃**의 데이터 근거 확보 — 플래너는 번들 종료상태 평가라 스텝별 그리디 증폭 없음.
- deepRollCurRes/deepRollCap 플래그는 문서화된 실험으로 코드 잔존(default OFF, 무해).

## 2026-06-29 fedMax5Buildings (R6 외 연방 건물 5개 캡) — 사용자 채택(ON), 단 목표 미달
- 120판 종족혼합: VP −1.27 (p=0.49, 노이즈), 승률 43%. **연방 1.39→1.44 (Δ+0.05, 사실상 그대로).**
- 결론: 건물 캡(sprawl 억제)으론 연방 수가 안 늘었다. 봇 연방 부족 원인은 sprawl이 아니라 '2번째 클러스터 형성 불가'(getBestFederationAction 2번째 null) — 기존 reach 벽 재확인. 건물 캡은 그 집들을 2번째 연방으로 전환 못 시킴.
- 판정: 측정상 무해(VP 노이즈)지만 점수/연방 이득 없음. **사용자가 플레이 스타일(연방 좁게)로 원해서 기본 ON 채택**(getPlayerFlag 기본값 false→true). 연방 수 증대는 별도 '2번째 클러스터 형성' 작업 필요.

## 2026-06-29 Ivits 약점(사용자 "하이브 특출나게 못함") — 두 수정 다 중립/기각
- 진단: Ivits 68.5(18종족 7번째 바닥). ability-usage: 봇 우주정거장 4.5/게임 vs 사람 11.3. calculateFederationScore가 Ivits엔 평면 +20만(tightCluster/fedCompletion 스킵).
- **ivitsStationBeforePass(OFF 유지)**: 패스 직전 미사용 우주정거장 배치. forced-ivits 80판 VP +0.70(p=0.75), 연방 1.31→1.30 그대로. "더 놓기"는 연방 증대 X.
- **ivitsTightCluster(OFF 유지)**: Ivits 빌드에 dist1+100/dist2+50/fedCompletion commit 부여. forced-ivits 80판(둘 다 ON) VP +0.45(p=0.83), **연방 1.41→1.32(노이즈, 안 늘어).**
- 결론: 사용자 관찰("과점프→산개→연결불가→연방불가")은 정확하나, **빌드 클러스터링은 후보점수라 MCTS 평가기가 덮어써 실제 연방으로 안 이어짐**(mcts-candidate-score-vs-final-pick 벽 재확인). 연방 증대는 평가기 레벨 또는 결정경로 강제 필요(둘 다 큰 작업·기존 buildOrderPlanner도 2번째 클러스터 null로 실패).

## 2026-06-29 greenForAdvTile — 채택(ON): 초록연방을 L5 대신 고급타일에 아끼기
- 진단(실게임 6경기 추적): 봇 L4/L5+초록연방 보유(조건 충족)인데 고급타일 0개. 원인=고급타일은 '건물 트리거+L4+초록 동시'여야 하는데, 초록이 늦게(R5-6, 연방 늦음) 와서 ①트리거(연구소/의회) 소진 후 도착 ②오자마자 L5 승급에 초록 소모. scoreAdvancedTechTile은 70-90으로 충분(점수 문제 아님).
- 수정: L4→L5 승급 점수에 -250(좋은 고급타일≥70 청구가능 + 초록 보유 + R<6) → 초록을 L5에 안 태우고 고급타일에 보존. advTileOverL5의 아카/연구소 트리거 우대와 짝.
- 80판 혼합: VP +0.82(p=0.71 노이즈), **의회/아카데미 +0.14·기술타일 +0.09·연구진행 +0.18·총행동 +0.49**(트리거 건물↑→타일↑ 의도 방향). do-no-harm + 사용자 강한 요청 → 기본 ON. (harness log가 stdout 미포착이라 고급타일 수 직접확인 불가 → 사용자 실게임 로그로 검증 예정.)

## 2026-06-29 greenForAdvTile 직접계측 → 기각(채택 철회), + headToHead 고급타일 계측 추가
- headToHead에 '고급타일' 행 추가(최종 techTiles 중 adv- 개수). 봇끼리 ON(A) vs OFF(B) 80판 직접 측정:
  **고급타일 A(ON) 0.09 vs B(OFF) 0.06 — 둘 다 사실상 0.** 수정해도 안 늘었다. VP도 이 판은 B(OFF) +2.51승(이전 ON판 +0.82와 합쳐 노이즈/중립).
- 결론: 초록연방을 L5 대신 아껴도, 그걸 현금화할 트리거(아카데미 건설+L4+초록 동시)를 봇이 여전히 안 만들어 초록이 놀음 → 고급타일 0 유지. **연방과 동일한 다조건 타이밍 코디네이션 벽.**
- 판정: greenForAdvTile **기본 OFF로 철회**(80c286a 채택 번복). 교훈: proxy(아카데미↑)만 보고 채택했다가 직접계측(봇끼리)으로 무효 확인 — 행동 계측은 결과지표를 직접 봐야. 고급타일 늘리려면 '아카데미 트리거 강제'(결정경로) 필요(buildOrderPlanner류, 기존 실패 전력).

## 2026-06-30 고급타일: 저평가 가설 반증 + 데드락 수정
- **데드락 버그(오늘 자작) 수정**: 우주선 무료광산 비용 부족 시 거부→면제(3f6b52b가 거부해 pendingSpaceshipFedMine 영구 미해소→게임 hang, 80판 중 59에서 멈춤 실측). 무한거리 무료광산은 어디든 건설 가능해야 함.
- **advTileValueBoost(기각, OFF)**: 사용자 가설 "MCTS가 고급타일 가치를 못 봐서 안 먹는다" 직접검증. 평가기 고급타일 준비보상 30→90 + 보유가치 ×2. 40판 직접계측: **고급타일 0.05(OFF)→0.06(ON), 변화 없음.** VP −1.85(노이즈), 아카데미 −0.29(오히려 왜곡). → **저평가가 원인이 아님이 측정으로 확정.**
- 결론: 고급타일 0의 원인은 평가가 아니라 **기회(초록연방+L4+트리거 동시 정렬)** 부족. 초록연방이 늦고 적어(연방 1.45) 정렬이 안 생김. 고급타일 ⊂ 초록연방 ⊂ 연방 타이밍 벽. 단독 교정 불가 — 연방을 일찍/많이 만드는 게 고급타일·L5·점수를 동시에 푸는 진짜 레버.
- 계측 자산: headToHead에 '고급타일' 행 상시 추가됨(adv- 보유수).

## 2026-06-30 고급타일 저평가 가설 — 극단부스트로 최종 확정(기회 문제)
- 사용자 반론 타당("2~3배로 안 되면 아직 저평가일 수도"). → 극단 테스트: advTileValueMax(준비보상 400×ready=L4시 +800, 보유 ×6, 획득직전 +2000).
- 40판 직접계측: **고급타일 0.04→0.10(겨우 +0.06), VP −8.26(p=0.02 유의 악화).** 의회/아카 −0.19.
- 판정: **저평가 아님 확정.** 저평가였다면 ×10에서 폭증(0.5~1+)해야 하나 0.10에 머묾 = 딸 기회(초록+L4+트리거 동시)가 거의 없음. 과대평가는 못 딸 걸 쫓아 봇을 유의하게 악화(−8.26). 가치는 드문 기회를 조금 더 잡게 할 뿐 병목 아님.
- 결론 재확정: 고급타일·L5·Ivits 모두 연방 타이밍 벽으로 수렴. 진짜 레버 = 초록연방 일찍/많이(2번째 클러스터 형성).

## 2026-06-30 advTileAlways 채택(ON) — 고급타일 0.03→0.51, 사용자가 옳았음
- 반전: 평가기 부스트(advTileValueBoost/Max)가 고급타일 수를 못 올린 진짜 이유 = **타일 선택은 MCTS/평가기를 안 거치고 findTechTileAction이 직접 결정**(getNextMove 441 pending이면 즉시 반환). 평가기 건드린 게 결정에 무관했음.
- 사용자 관찰("초록+L4+트리거 가능한 상태를 1:3에서 수없이 봤다")이 정답: 기회는 충분, 결정부 임계(scoreAdvancedTechTile≥70)가 보수적이라 '딸 수 있는데 기본 집음'.
- advTileAlways: 청구 가능한 고급타일(초록+L4+미보유)이면 무조건 기본보다 우선. 40판: **고급타일 0.03→0.51(+0.49)**, VP +0.66(p=0.84 중립), 기술타일 +0.38. do-no-harm + 목표동작 달성 → 기본 ON.
- 교훈: "어느 함수가 실제로 그 행동을 결정하는가"를 먼저 확인할 것. 평가기 가중치/상태보너스는 MCTS 경유 행동에만 영향 — pending 처리(타일선택·연방보상 등)는 직접 함수가 결정. 결과지표(고급타일 행) 직접 계측이 결정적이었음.

## 2026-06-30 hadschHallasConvert 채택(ON) — HH PI 무료변환(4C→1QIC 등) 봇 구현
- 갭: 봇 HH 시그니처 사용 1.0/게임 vs 사람 44.7(봇 코드 자체가 없었음). HH PI = 매턴 반복 가능 무료변환 4C→1QIC/4C→1K/3C→1O(once-per-round 아님 — 소켓핸들러가 isUsed 미체크).
- 구현: executeUseHadschHallasPIAction(서버, 소켓과 공용 DRY) + 봇 findHadschHallasConvert(QIC 최우선, 부족자원 보충, 크레딧풍선 드레인) 메인액션·패스 직전 연결.
- forced-HH 40판: 보수적(버퍼6/qic<5) → HH변환 0.40, VP −3.67(빡빡해 나쁜 타이밍만). 적극(버퍼3/qic<8/풍선≥10) → **HH변환 0.70, VP +0.33(중립), 연방 +0.10.** do-no-harm + 시그니처 사용 + QIC가 연방에 보탬 → 기본 ON.
- 단 0.70은 사람 44.7보다 훨씬 적음(봇이 self-play에선 크레딧 부자가 아님). vs사람 실전서 더 체감 예상.
- 계측 자산: headToHead에 'HH변환'·'다운그레이드' 행 추가.

## 2026-06-30 종족능력 갭 4종 — tinkeroids 채택, darkanians/bal_tak 기각, taklons 무레버
- **tinkeroids(채택, 무플래그 룰개선)**: executeBotTinkeroidSpecial이 무조건 options[0] 고르던 것 → 라운드별 우선순위(1-3R: 4파워>1QIC>1TF광산, 4-6R: 3K>2QIC>3TF광산). 명백한 개선·저위험이라 적용(이미 최강 종족 91점이라 A/B 생략).
- **darkanians(기각, OFF)**: PI 신섹터 +1K+2C를 빌드점수 +35로 유도. forced 40판 VP −0.49, paired 65.3(ON)vs66.8(OFF). 신섹터는 멀어(QIC점프) 보너스가 reach 벽/점프페널티에 막힘 → 중립-음수.
- **bal_tak(기각, OFF)**: 선제 포머→QIC(qic<2시). forced 40판 VP −2.33, paired 75.0(ON)vs84.2(OFF, Δ−9.2). 선제변환이 오히려 손해 → 기각.
- **taklons(무레버)**: 시그니처(브레인스톤·번·PI토큰)가 수익 자동처리/이미 구현됨 — "안 쓰는" 게 아니라 자동이라 추가할 행동 없음. factionAbilityUsage의 1.7 vs 22는 로깅/패시브 차이.
- **메타 교훈(중요)**: 종족능력 개선은 "점수 보너스로 유도"·"선제 자원전환"은 self-play서 워시아웃(reach/MCTS벽 + vs사람 엣지 미보상). "결정부 직접교정"(advTileAlways)·"없던 행동 직접실행"(hadschHallasConvert)만 먹힘. 이게 통하는 fix와 안 통하는 fix의 분기선.

## 2026-06-30/07-01 다턴세팅·연방 분석(44+로그) + gaiaformerPlacedValue 기각(평균회귀)
- 로그 분석 인과사슬 확정: 확장연구 과소(4-7 vs 9-14)+가이아포머 미사용(0.03 vs 0.43) → 최종건물 7.19 vs 사람 11.11 → 연방 1.17 vs 다수(위성 5.79 vs 2.47 sprawl). **연방 = 건물수의 하류 = 새 타일 개방(다턴세팅)의 하류.**
- 진단: 평가기가 '놓은 가이아포머(pendingGaiaformerTiles, 다음라운드 성숙→가이아광산)'를 0으로 봄 → 포머 놓으면 gaiaformers-1(−5)만 남아 MCTS가 '순손해'로 회피(0.03). 배치는 MCTS결정이라 평가기 수정이 직접 먹히는 구조.
- **gaiaformerPlacedValue(기각, OFF)**: pending 가이아타일 +16 크레딧. 40판 VP +2.77·전건물↑·연방+0.11(유망) → **120판 확정 −3.05(p=0.12), 연방 −0.10·건물신호 붕괴.** 평균회귀. 40판서 채택 안 하고 확정런 돌린 게 정답.
- 결론: 다턴세팅(가이아포머 가치)조차 평가기 크레딧으론 self-play서 안 됨. 연방/건물수 벽은 여전 미해결 — 남은 정공법은 명시적 다턴 플래너(번들 계획·실행) 또는 vs사람 실전 검증뿐.

## 2026-07-01 초반 사람모방 분석 + shipOverPass 기각
- 사람 R1-2 액션 마이닝: 우주선 30.1% vs 봇 11.2%(최대격차), 패스 사람9.6% vs 봇16.9%(봇 초반 놀기), 가이아포머 5.1vs1.5, 연방 4.5vs0.6. 봇은 광산/교역소 편중.
- **shipOverPass(기각, OFF)**: 패스 직전 우주선 액션/입장 강제. 40판 VP −5.49(p=0.075), 우주선입장+0.27인데 우주선액션 −0.41 → '타고 안 쓰는' −5VP 낭비(기존 shipEntryGate 발견 재확인). 블런트 강제는 손해.
- 사용자 관찰(탑승자 있는 배 +2파워→파워액션 체인): 봇이 +20 후보가산으론 보나 후속 파워액션 가치는 미반영. 단 블런트 우주선 강제는 역효과 확인 → "입장"이 아니라 "체인(파워 얻어 굴리기)"만 가치.
- 메타: self-play 레버 거의 소진 — 우주선/연방/다턴 다 self-play가 vs사람 엣지를 못 보상. 채택된 행동fix(advTileAlways·HH·tinkeroids)의 진짜 판정은 1:3.

## 2026-07-01 다턴 플래너 twoTurnPlan v1 기각 — 평가기 천장 재확정
- twoTurnPlan: 탑6 메인후보를 [액션+다음턴 그리디후속]까지 정확시뮬(라운드내 수입없음)해 2턴 종료상태로 비교, 최고 커밋(MCTS 대체). 40판 VP −8.46(p=0.024 유의 악화). 교역소+0.89·총행동+2.45(더 바쁨)이나 VP 급락.
- 원인: 2-ply deliberate 그리디가 MCTS 400ms 깊은탐색보다 약한 탐색기 → MCTS 대체가 손해.
- **결정적 결론**: 이제 deep 탐색(deepRollout)·shallow deliberate(twoTurnPlan)·극단 eval부스트(advTileValueMax) 다 실패/무해. **봇 천장 = 리프 평가기(가치함수)지 탐색/플래닝이 아님.** 탐색 구조를 어떻게 바꿔도 나쁜 평가기를 더 열심히 최적화할 뿐. 유일하게 통한 건 특정 결정점 '직접 실행'(advTileAlways·HH)뿐.
- → 다턴 플래닝이 값하려면 **리프 평가기부터 개선**(사람게임 가치망/policy net = path2)해야. 그 다음에 깊은 탐색이 페이오프. 순서: 평가기 먼저.

## 2026-07-01 라벨링 루프 시작 + Q5(expandOverTS) 기각 + 우주선 파워 off-by-one 발견
- 전문가 문제집(5샘플 → 100문항) 라벨링 파이프 시작. 사용자 5답: Q1 우주선조건부·Q2 연방즉시·Q3 고급>L5·Q4 nav먼저·Q5 확장>초반TS. 4/5는 이미 봇에 있음(오늘 결정들과 일치).
- **expandOverTS(Q5 규칙, 기각 OFF)**: R≤3 미점유 확장타일 있으면 mine→TS 업글 후보 제거. 40판 +2.71(교역소 −0.21 의도대로) → **120판 −1.10 평균회귀.** 
- **핵심**: 전문가 룰이 옳아도(확장>TS는 타일경쟁 전제) self-play는 그 vs사람 가치를 못 보상 → 워시아웃. "라벨→규칙→self-play"는 대부분 무효. → 100답의 쓸모는 규칙테스트가 아니라 **30 휴리스틱 가중치를 pairwise 선호로 튜닝(평가기=천장 직접 사람화)**. 100개면 30파라미터 충분.
- **버그 발견(미확정)**: executeEnterSpaceship 입장파워 idx===2||3→+2/idx===4→+3 인데 주석은 "2·3번째 2PW,4번째 3PW" = off-by-one(첫 두 탑승자 파워0, 의도는 2번째부터). 사용자 룰 확인 후 수정 예정.

## 2026-07-01 로그 마이닝 라운드 종결 — r1TsFirst 기각, self-play 광맥 소진 결론
- 로그 재마이닝 발견: R1 첫수 사람 TS 59% vs 봇 광산 65%; 기술타일 봇 nav/sci 편식 vs 사람 균등(gaia/terra/AI); 파워액션 봇 테라폼67% vs 사람 자원61%.
- 파워액션 '테라폼 후 건설 안 함' 버그 의심 → **166/166(100%) 건설 이어짐, 버그 아님**(강제건설 가드 작동). 스타일 차이일 뿐.
- **r1TsFirst(기각 OFF)**: R1 mine→TS 강제. 40판 VP −3.26. 봇 광산-first R1이 나쁘지 않음.
- **종결 결론**: 로그 마이닝 직접-결정 후보(expandOverTS·r1TsFirst 등) 이번 라운드 전부 워시아웃/음수. self-play 최적화 광맥 소진 확정. 남은 진짜 레버 = ①게임 수 축적→가치망(평가기 천장 개선) ②vs사람 검증. 100문항 손라벨링은 로그 9817샘플과 중복이라 불필요(라벨수 아닌 게임수가 병목).

## 2026-07-01 TS룰 2종 기각 + 라운드분해 계측 도입 (★방법론 전환)
- 사용자 의심: "점수 +30 줘봤자 미미해서 똑같이 게임하고 '노이즈 범위'로 나오는 거 아니냐" → **정확히 맞음.**
- **tsScoreRework(기각 OFF)**: mine→TS 기본 저점 + good-case(광산3/랩발판/연방파워) 가점. 점수 ±140~160.
  - 라운드분해 계측 결과: TS(R1-2) 2.16→2.00(−0.16), TS평균R 3.11→3.09(−0.02). **초반 행동 거의 안 바뀜.** 전체 TS만 −0.28.
  - 원인: 후보 점수는 top-5 컷 통과용일 뿐, 초반 후보 ≤5라 TS 안 잘리고 MCTS 평가기가 결국 고름 → 점수=죽은 코드.
- **tsEarlyHardGate(기각 OFF)**: R≤2 & !isFirstTS & 광산≠3 이면 mine→TS 후보 '생성 안 함'(하드 컷).
  - 결과: TS(R1-2) 1.90→1.77(−0.13뿐!), 광산(R1-2) +0.20, TS평균R +0.09. VP −1.96(승률40%, p.567). 스자 +2.60(p.65 노이즈).
  - **핵심 발견**: 하드컷해도 초반 TS 거의 안 줄어듦 = 봇의 초반 TS는 이미 대부분 good-case(첫TS/광산3). **봇은 원래 초반 나쁜 TS를 거의 안 지었음** — 기존 패널티가 이미 막고 있었음. 24C 풍선은 초반 TS스팸이 원인 아님(그 1게임 특수상황 or 후반 미사용 hoarding). 한 게임 관찰→시스템 단정은 성급.
- **★도입(채택): 라운드분해 계측** — addGameLog에 round 필드, headToHead에 TS(R1-2)/광산(R1-2)/TS평균R 지표. 
  - 교훈: **VP만 보면 "무해"인지 "아무 일도 안 일어남"인지 구분 못 함.** 앞으로 모든 행동변경은 "행동카운트가 실제 바뀌었나(라운드별)"부터 확인 후 VP 판단. 죽은 코드를 VP 노이즈 뒤에 숨기지 않기.

## 2026-07-02 gaiaBoosterUsable 채택 (★행동 실측 성공 케이스)
- 사용자 관찰: 봇이 즉포(bon-2pw-gaiaproject, 2파워+가이아프로젝트 특수액션)를 집고 특수액션 안 쓰고 반납. "쓸 거 아니면 다른 부스터 집어야."
- **버그 실측(사람게임 45판, 봇만)**: 즉포 든 구간의 ~79-85%가 특수액션 미사용 = 2파워만 받고 슬롯 낭비. 진단: 낭비의 (b)"포밍가능한데 무료보너스 대신 파워로"=0건, (a)"그 라운드 포밍 자체 안 함"=100% → **선택-쪽 문제**(못 쓸 걸 집음), 사용-쪽 아님.
- **수정(gaiaBoosterUsable, 기본 ON)**: calculateBonusTileScore의 gaia_project 값을 `findGaiaformerActions>0 ? +4 : -3`으로. 포밍 가능할 때만 우대, 아니면 감점해 2파워 수입가치 상쇄→다른 부스터.
- **h2h 80판 결과**: 즉포선택 0.34→0.04(−0.31, 88%↓), 사용/선택 21%(79%낭비)→100%(낭비0). 가이아포밍 총량 0.54→0.47(거의 유지=포밍 덜 하는 게 아니라 낭비 슬롯만 제거). VP +0.13(p.963 중립). 
- **판정: 채택.** TS룰과 대조 — 여기선 행동카운트가 실제로 크게 이동(−0.31), 낭비 제거, VP 무해, 사용자 관찰. "행동 먼저 측정" 방법론의 성공 사례.
- 계측 추가: h2h 즉포선택/즉포사용 카운터(headToHead BEHAVIOR_KEYS gaiaPick/gaiaUse).

## 2026-07-02 lantidsEarlyPI 기각 (후보점수 함정 재현)
- 사용자 관찰: 란티다가 PI(의회)를 너무 안 지음. PI 지으면 기생광산마다 +2지식(gameState 5876)이라 PI 먼저 짓고 기생해야 이득.
- 실측: 코드상 란티다는 earlyPiAllowed 미포함 → R4 전 PI 후보 차단(continue). 로그 1판도 PI 0·기생 3 전부 PI前.
- 수정(lantidsEarlyPI): 기생 타겟 수 비례 PI 가점 + R2 조기 허용(R4·광산<5 게이트 우회). 
- **h2h 란티다강제 60판**: PI건설 0.37→0.36(거의 무변), PI평균R 4.57→4.23(약간 조기), 기생광산 0.77→0.59(감소), 란티다 VP 67.0→58.3 **Δ−8.79(p≈0.044) 유의 악화**.
- **판정: 기각.** (a) 후보점수(+60/+90)는 MCTS 최종선택을 못 바꿔 PI건설 거의 안 늘어남(TS와 동일한 candidate-score-vs-eval 함정). (b) 억지 조기 PI는 확장/기생 엔진 훼손해 VP 유의 악화. 평가기를 안 건드리는 점수푸시는 무효+유해. 계측(PI건설/PI평균R/기생광산) 채택.

## 2026-07-02 lantidsEarlyPI 채택 (직접강제 버전 — 점수푸시 기각 뒤집음)
- 사용자 재지적: "기각할 게 아니라 란티다 R3(~R1)에 의회 짓는 게 정석" → 봇이 실제로 하게 강제해야.
- 점수푸시(앞 기각 항목)는 후보점수만 올려 MCTS가 안 골랐음(PI건설 0.37→0.36, VP −8.79). 
- **직접강제 추가**: R3+ & PI미보유 & TS있음 & 감당되면 그 턴 메인액션으로 PI 업글 강제(r1TsFirst 방식, MCTS 우회). findUpgrade 게이트 우회는 유지.
- **h2h 란티다강제 60판**: PI건설 0.32→0.50(+0.18 실제 상승), 기생광산 0.67→0.82(+0.15 시너지), 란티다 VP 62.7→64.5(Δ+1.82 p0.65), 전체 +0.03(무해).
- **판정: 채택(기본 ON).** 직접강제=행동 실제 변화(PI건설↑·기생↑), VP 음수→양수, 사용자 정석. 교훈 재확인: 후보점수 X, 결정경로 직접강제 O. (PI평균R 4.3 — TS/자원 준비상 R3까진 못 당김, 후속 개선여지.)

## 2026-07-02 pendingStepsPreferFull 채택 + 종료효과음
- 사용자: "QIC 0(사거리 내)이면 투삽>원삽 우선. 지금 Nav2에 2거리 투삽 냅두고 1거리 원삽에 지음." 공짜빌드는 남은 삽이 버려져(clearFreeMineFlags reset) 원삽=낭비.
- 원인: findBuildActionsWithPendingSteps 후보점수(coveredByPending*40)는 투삽 우대하나 약함 + return builds→MCTS가 평가기로 가까운 원삽 고름(candidate-score≠eval 함정).
- 수정(pendingStepsPreferFull 기본 ON): 사거리내(nq=0) 후보 중 저커버(원삽) 제거 → MCTS가 투삽 강제 선택(후보필터=결정적, 점수 아님). 가이아/소행성/사거리밖(QIC) 제외.
- h2h 80판: 승률 B 58.2%(46:33 p0.144), VP −1.38(p0.558 노이즈), 광산 −0.32. do-no-harm + 사용자요청 + 구성상 결정적 → 채택. (직접 투삽선택 지표는 미추가 — 필터라 상황시 확정 작동.)
- 종료효과음(#11): client/src/lib/audio.ts playEndSound(isWinner) — 1등 상승팡파레/그외 하강2음 ~3초, 볼륨설정 존중. Game.tsx gameEnd 감지시 gameId당 1회 재생.

## 2026-07-02 itarsBurnFreely 기각(엉뚱한 레버) + 로그표시/효과음/투삽 채택
- 사용자: 아이타는 번한 토큰이 가이아복귀(소멸X)라 막라만 아니면 적극 번해도 됨. 근데 잘 안 태움.
- 확인: executeBurnPower 8485 — 아이타 번은 gaiaformerPower+1(복귀). 사용자 관찰 정확. 봇 파워번 실측 ~0/게임(A·B 모두).
- 수정시도(itarsBurnFreely): getPowerTokenReserve를 아이타 R<6에 0으로. h2h 아이타강제 60판: 파워번 0.00→0.01(무변!), itars VP 65.0→64.2(p0.855). **기각.**
- 원인: 예비 가드는 전략적 토큰소비(연방) 통제일 뿐 번 결정과 무관. 번은 광산콤보 preAction에서만 드물게 생성→~0. 진짜 수정=아이타 직접 번-후보 추가 필요(재설계).
- 계측 채택: h2h '파워번' 카운터.
- 같이 채택: 종료효과음(audio.playEndSound), 프리액션 로그 ×N 집계(gameState addGameLog — 동일변환 dedup→×N, 5번을 1번처럼 보이던 표시버그).

## 2026-07-02 midTerraformGuard 기각 (직관 vs 측정 상반)
- 사용자: 막라도 아닌데(특히 R4) 원삽 원시행성에 3O 테라포밍은 손해, 안 짓는 게 이득.
- 수정(midTerraformGuard): R3~4 costPerStep>=3 오레-테라포밍(1스텝)에 차단급 페널티(1000, earlyGuard R1-2와 동일).
- h2h 80판: 광산 8.61→8.54(−0.07 거의 무변), VP −2.38(승률 42.5% p0.32). 
- **기각.** R4 확장 차단은 광산 수 거의 안 줄이면서 VP는 악화 — 정체→파워액션/연구 대체가 확장보다 나쁨(2508 주석 '정체 회귀'가 실측됨). "안 짓는 게 이득"은 self-play에선 반대. 정교화(0스텝 대안 있을 때만 억제) 여지는 있으나 우선순위 낮음.

## 2026-07-02 itarsBurnCandidate 채택 ★세션 최고 (+6.85 VP, 사용자 아이디어 = MCTS 탐색)
- 사용자 관찰: 마지막 경기 아이타가 자원(특히 bowl2 9~10)을 쌓고 R4~R5 패스, R6에 폭발. 파워를 못 굴림.
- 사용자 아이디어: 휴리스틱으로 "언제 번할지" 박지 말고, MCTS가 번(2/8/0→2/0/4)을 돌려보고 최적을 찾게 하라.
- 수정 2종: (1) burn_power를 MCTS 후보로 추가(아이타·R6아님·bowl2≥2). (2) 평가기가 아이타 gaiaformerPower(번/가이아로 이동, 다음R 복귀+PI 기술타일 환전) 가치화(PI면 power3급, 아니면 power1급) — 안 그럼 MCTS가 번을 토큰손실로 오판.
- **h2h 아이타강제 60판→120판**: itars VP 67.7→74.5 Δ+6.85(p≈0.068, 60판 +8.42에서 유지=회귀X), 파워번 0.50→2.01(카운터 수정후, 실제 번↑), 우주선액션+0.27·기술타일+0.14.
- **판정: 채택(기본 ON).** 행동 확실히 변화(번 4×) + VP 대폭↑ 120판 유지 + 사용자 관찰/아이디어. 
- ★핵심 교훈: vs사람 엣지도 **평가기에 실제 가치(기제)를 넣으면 self-play가 보상**한다. 휴리스틱 하드코딩/점수넛지(무효)와 대조 — 후보를 열고 평가기를 옳게 = 탐색이 최적을 찾음. 번 카운터는 consolidation으로 action→'Free Actions'라 details도 검사하게 수정.

## 2026-07-02 ★채택 noFedTierUp(정제)+noBuildAdjFed — 연방 낭비 컷, VP +4.85 유의
- 사용자: "이미 연방에 속한 건물 티어업/딱 붙여 건설이 너무 아까움." 연방은 형성 1회 점수라 거기 보태면 낭비 + 새 연방 씨앗 소모.
- 원인: fedPenalty(−450)·noInflateFed(군집보너스 0)는 점수라 MCTS가 무시. 기존 티어업 필터는 비연방 대안 있을 때만 발동(전부 연방이면 통과).
- blunt noFedTierUp(연방건물 전부 제외): −3.56, 연구소 −0.24(TS→랩까지 막힘), 연방 −0.01. 기각.
- **정제 noFedTierUp**: 연방건물이라도 TS→랩·TS→PI(엔진)는 허용, mine→TS·아카데미(파워범프)만 후보 제외. **noBuildAdjFed**: 연방 인접(dist1) 신규 광산후보를 비인접 대안 있을 때 제거. 둘 다 점수 아닌 후보필터(결정적).
- **h2h 콤보 80판: VP +4.85(p=0.032 유의), 승률 61%(47:30). 교역소 −0.28(낭비컷), 연구소 +0.25·PI +0.14(엔진유지), 연방 +0.07.** → 둘 다 채택(기본 ON).
- 교훈: 사용자 "낭비" 직관 옳았고, blunt는 엔진까지 죽여 실패 → **낭비 서브셋만 정밀 컷**(엔진 살림)하니 유의 상승. 후보필터 레버 재확인.

## 2026-07-02 firaksLabLock 채택 (do-no-harm+사용자요청, +8.32는 winner's curse였음)
- 사용자: (1)파이락 PI 전 2번째 랩 금지(다운그레이드 엔진은 PI 필요), (2)PI 있으면 매 라운드 랩1개 확보해 능력 쓰게.
- 구현: (1)=하드 continue(firaks·PI없음·랩≥1 → 랩후보 제외), (2)=firaks·PI·랩0개면 랩업글 +250.
- firaks강제 60판 +8.32(p0.081) → **120판 −0.48(p0.875) 회귀 = 소표본 winner's curse.** 다운그레이드 0.20→0.33(+0.13, 엔진 발동↑=의도대로), 연구소 무변, 전체 +2.25(p0.26 노이즈).
- **판정: 채택(기본 ON).** VP 중립(−0.48, do-no-harm) + 사용자 요청 행동 + 다운그레이드 발동 실제 증가 + (1)락은 논리적으로 명백히 옳음. VP 대박 아님(아이타와 대조 — 그건 120판 유지된 진짜).

## 2026-07-02 noR1TokenGain 보류 (증상이지 병 아님)
- 사용자: R1에 인공물(획득=6토큰 소모) 갈 거 아니면 gain-2-tokens 하는 게 아까움.
- 구현: R1 gain-2-tokens score<0(후보제외). 예외: 트와일라잇 탑승+슬롯+토큰<6이면 허용(인공물 6토큰 모으는 중).
- h2h 80판: VP −1.04(p0.690 노이즈), 총행동 −1.50, 광산 −0.22, 파워액션 −0.21.
- **판정: 보류(기본 OFF).** R1 토큰액션 막으니 대체 건설을 하는 게 아니라 그냥 덜 함(더 수동적=자원쥐고패스 악화). 낭비 아니라 "R1 대안 부실"의 증상. 연방낭비(+4.85)와 대조 — 그건 대체(엔진업글)가 됐지만 이건 안 됨. 사용자가 실전서 계속 원하면 do-no-harm 경계로 채택 가능하나 음수+수동화라 비추.

## 2026-07-02 taklonsBrainBurn 보류 (옮기지만 안 씀 = hoarding)
- 사용자: 타클론 브레인 bowl2에 놀림. bowl3로 올려 파워액션/1O·3C 변환에 써야.
- 갭: burn_power 후보 아이타 전용, 타클론은 브레인 옮길 후보 없음(평가기는 bowl3 2.5>bowl2 1.2로 선호). 해법=브레인 in bowl2일 때 burn_power(moveBrainToBowl3) 후보 추가.
- taklons강제 60판: 파워번 0.75→1.28(+0.53, 브레인 실제 이동), 파워액션 −0.02(안 씀!), taklons VP 73.4→71.0(Δ−2.40 p0.611), 전체 +1.42.
- **판정: 보류(OFF).** 아이타(번→토큰이 파워액션으로 쓰여 +6.85)와 달리, 브레인을 bowl3로 옮기기만 하고 안 씀 → 평가기가 미사용 브레인 bowl3(2.5)를 과대평가해 hoarding, 옮기느라 토큰만 소모=살짝 손해. 진짜 수정=번+사용 커플링 or 평가기 브레인 가중치 하향(리스크).

## 2026-07-02 타클론 브레인 능동사용 2종 기각 (self-play: 브레인은 아껴라)
- 사용자: 타클론 브레인 bowl2 놀림/bowl3 안 씀/충전오버플로 시 안 비움 = "브레인 능동사용 안 함"(타클론 코어).
- taklonsBrainBurn(bowl2→3 번 후보): 단독 60판 파워번 0.75→1.28(+0.53, 이동함), 파워액션 −0.02(안 씀), taklons VP −2.40. 
- +taklonsChargeDrain(충전오버플로 시 브레인 3P→1O로 미리 씀; 기존 charge-drain은 타클론 제외였음): 콤보 60판 파워번 +0.53·파워액션 +0.33(이번엔 씀!), **taklons VP −2.67, 전체 −4.88(p0.085 유의근접).**
- **판정: 둘 다 기각.** 브레인을 더 쓰게 하니 오히려 약해짐 — 브레인 bowl3는 유연한 3파워 자산이라 1O/작은액션보다 들고 있는 게 나음(평가기 2.5=실가치, hoarding 아님). charge-drain은 브레인3파워를 2충전 아끼자고 써 손해트레이드. 두 시도 일관 음수 → self-play는 "브레인 아껴라". 사용자 직관(bowl2 오래 놀림)은 실제지만 blanket 사용은 −. 고가치 용처 한정은 이미 봇이 어느정도 함(파워액션 affordability가 브레인 셈).

## 2026-07-02 타클론 브레인 3종 전부 기각 (계측으로 원인 규명, 못 고침)
- 계측(브레인놀림=브레인 bowl3에 둔 채 턴종료 횟수): 현재봇 A도 1.6~1.9/게임 방치 = 사용자 관찰 데이터 확인.
- burn(bowl2→3): 타클론번 0.05→0.71(옮김) but 브레인놀림 1.63→2.09(+0.47, 옮기고 안 씀=헛일), VP −2.4~−5.4.
- chargeDrain(충전오버플로 시 브레인 3P→1O): 콤보 VP −4.88(브레인3파워를 2충전 아끼자고 써 손해트레이드).
- spendIdleBrain(패스 직전 브레인 3P→1O): 브레인놀림 −0.02(거의 무변! idle은 '턴종료'지 '패스'가 아니라 엉뚱한 훅), VP −4.31.
- **판정: 3종 다 기각(OFF).** 벽2개: (1)아키텍처 — 브레인 idle은 메인액션 후 턴종료 시점인데 거기 free-convert 훅 없음(pre-action/pre-pass만). (2)평가기 브레인 bowl3=2.5 hoarding — 어디서 강제하든 "2.5 버리고 1O" 손해로 봐 VP−. 사용자 관찰(방치)은 사실이나 self-play는 "유연한 3파워 자산이라 아끼는 게 최적"으로 일관. vs사람 엣지 가능성(1:3 전용). 계측(takBurn/takBrainIdle) 보존.

## 2026-07-02 타클론 브레인 4번째(평가기 가중치↓) 기각 — 이 영역 종결
- taklonsBrainSpend(brainStoneBowl3 2.5→≈0.5): 브레인놀림 1.79→2.23(+0.43 또 증가!), 파워액션 +0.48(일반파워로), taklons VP −1.49.
- 낮춤=지출유도 아니라 "브레인 무관심" → 브레인 방치 유지+증가. 
- **타클론 브레인 4시도 전부 실패 확정**(burn idle+0.47 / chargeDrain −4.88 / spend-at-pass idle−0.02 엉뚱훅 / eval↓ idle+0.43). 근본: (1)아키텍처 — idle은 메인액션후 턴종료 시점, 거기 spend-candidate 생성 경로 없음(pre-action/pre-pass만). (2)self-play는 저가치(1O/3C) 브레인지출을 손해로 봄(1O 0.8 < 보유). 
- **종결: 전부 OFF, 계측(takBurn/브레인놀림) 보존.** 제대로면 "메인액션 후 잔여파워 free-action 단계" 아키텍처 필요(대수술, 리스크). 사용자 관찰(브레인 ~1.9/게임 방치)은 데이터 확인됨 = vs사람 엣지, 1:3 관찰용.

## 2026-07-02 타클론 브레인 최종 완결 (올바른 훅으로 결정적 규명)
- 5번째 시도(taklonsSpendIdleBrain을 end_turn 직전=idle 실제 발생 지점 훅으로): 브레인놀림 1.80→**0.00**(훅 완벽 작동, 브레인 항상 씀), but 파워액션 −0.51, taklons VP −1.50.
- **결정적 규명:** 훅은 만들 수 있으나 매턴 1O 현금화 = 좋은 파워액션에 쓸 브레인을 고갈 → 손해. "놀던 브레인"은 낭비가 아니라 **좋은 파워액션 대기 중인 유연 자산**이었음. 평가기 brainStoneBowl3=2.5는 hoarding이 아니라 정확한 값. 
- **타클론 브레인 종결(5시도):** self-play는 "브레인 아껴두기가 최적"으로 완전 일관. 사용자 관찰(방치)은 사실이나 그건 의도된 대기. vs사람 엣지 가능성만 남김(1:3). 아키텍처 훅(end_turn 직전 free-action)은 존재 확인됨 — 향후 다른 용도로 재사용 가능.

## 2026-07-03 갭 재정량화(7/1-7/3 최신 9판) + 캘리브레이션 2종 채택
- 갭 유효 확인: techTiles 47.3vs3.9(-43.4) / research 38.4vs15.2 / roundMissions 35.6vs12.9(트리거 12.7vs4.9) / ships 20.4vs2.5. 봇 64.8.
- ★관통 진단: **QIC 기아(봇 종료 QIC 1.0)** — rebellion 3정큐(후보 380점)·twilight 재수령(350)·qic-action-tech 전부 QIC 없어 못 씀. 사람 연구분포 AI트랙 24% 1위(QIC엔진) vs 봇 가중치 꼴찌(×8 대충숫자). 기술타일 볼륨갭(9.2vs3.2장)의 상류도 QIC(rebellion/qic-action 펀딩 타일).
- **aiTrackQicEngine 채택(ON)**: AI트랙 ×8→×13 + R3+30. **roundMissionW15 채택(ON)**: 미션정렬 vp×5→×15(코드 VP환율 proto15×/shipfed26× 통일).
- 결합 40판: VP +0.38(완전중립 p=0.92), ON쪽 기술타일 +0.36·총행동 +1.06. do-no-harm 통과. 진짜판정 1:3.
- **twilightRecoupBeforePass 보류(OFF)**: 패스직전 3QIC 재수령 직접실행. 40판 −3.60(p=0.30 노이즈) + 발화 6회뿐(전제 3QIC+입장+연방 동시가 희박). 이득 입증불가 → OFF. aiTrackQicEngine으로 QIC 늘면 MCTS 기존 후보(350점)가 자연 발화 예상. 코드보존.

## 2026-07-04 ★ 50게임 게이트: 가치망 v2 probe 성공 → blend 통합 실패 (경로 판정 완료)
- **probe(scripts/valueProbe.mjs): 성공.** 사람게임only(39맵게임, 624 라운드말 스냅샷, fullGameLog재생)로 2덩어리 문제 해소 — R3 우승예측 62% vs 엔진크기베이스 49%, 쌍별 81.2% vs 71.8%(LOGO). 한계가치: tech+6.1/fed+13/ship입장+17/big+14/sector+4.6VP — 갭분석과 자기일관. **가치망 학습이 처음으로 신호.** humanValueNet.json 저장, 게임 쌓이면 재학습 파이프라인.
- **evaluator 가산블렌드(humanValueBlendK): 실패.** K=5 VP+0.15 중립 but 행동 의도방향 무변(기술타일 −0.31 노이즈). **K=12 VP −9.64 (p=0.002) 유의 악화.** 용량-반응 명확.
- ★판정: **선형 엔진카운트 블렌드 = 낮으면 중복, 높으면 유해.** 원인: 망은 자원/템포 몰라서 K 크면 전술 무시(placementPolicy ×60 −7.10과 동일 패턴). 낮은 K에선 휴리스틱이 이미 기술타일/연방/우주선을 점수화하고 있어 정보 중복.
- ★★교훈(placementPolicy와 공통 뿌리): **probe의 예측력 ≠ 통합 가치. 통합은 평가기가 *모르는 새 결정정보*를 더해야지, 아는 걸 재가중하면 중복~유해.** 남은 학습 경로: ①per-candidate 재랭킹(후보 중 사람 선택 학습 — humanCandidates 캡처 훅 가동중, *결정 그 자체*라 중복 불가) ②비선형 ③게임 축적. blend flag 기본0 유지, probe·json은 자산으로 보존.

## 2026-07-04 ★ per-candidate 착수: 후보생성 갭 37% 발견 + 확장 2종 채택(기본ON)
- **probe(scripts/candidateProbe.mjs, 18게임 3702결정)**: ①사람 수의 **37%(515건)가 봇 후보에 아예 없음** — 재랭커/가치망 이전의 병목. 원인=하드필터(변환부족 가설은 기각: 자원충분한데 없음이 연구81%/광산82%). 연구는 pickResearchTracks top-3 슬라이스, 광산은 소행성(Eclipse 폴백전용) 24%+normal 51%(테라폼가드 의심, 미규명). ②재랭커 top-1 31.4% vs 봇순서[0] 25.7% vs random 7.8%(보통 신호, 통합 보류).
- **allTracksResearch + asteroidMainCandidate 채택(기본ON)**: 자격 6트랙 전부 + Eclipse 6C 소행성 정규후보 승격. 랭킹은 MCTS가 — 후보 *추가*라 중복 불가(blend 실패와 대조).
- 검증: **40판 71.8% p=0.006 → 80판 확인 42.5% = winner's curse 또 적중.** 누적 120판 승률 52.1%, VP +0.29(중립), 행동은 일관 양(연구/기술타일/총행동↑). 비음수+방향일관+원리옳음 → 채택. 진짜판정 1:3.
- ★메타: 40판 유의(p=0.006)도 회귀함 — 확인 run 필수 재확인. 남은 갭: 광산 normal 51%(사거리내 정상행성인데 후보없음 — 테라폼 가드/필터 규명 필요).

## 2026-07-04 오후 세션 — 사용자 관찰 연쇄 교정 (전부 채택) + hang 근본수정
- **oreFedGuard 채택(ON)**: 무리한 연방(Ore 올인 위성) — R4-5 광석예비1+k≤4+위성≤6, R6만 관대. 40판 중립(-0.84)/연방수 보존.
- **tfCandidateOpen 채택(ON)**: normal 미싱 51% 규명 — 테라폼 페널티 1000-3000이 -50게이트에 걸려 R3+에도 후보 자체가 안 생김(사실상 하드필터). R3+ 페널티 ≤400 캡. 40판 +1.76/광산+0.64.
- **bowl3CashoutOre 채택(ON)**: 사용자 룰 "소모될 bowl3는 반드시 변환회수(캡30 예외)" — doomedCashout 헬퍼 ore우선(3P→1O)+캡가드로 업그레이드, 연방위성 deficit·인공물·포밍 3지점 적용. deficit이하 변환=수학적 순이득. 40판 +2.59/전지표 양성. (구 gaiaformPreSpend -5.24 원인=크레딧only·캡무시 규명)
- **upgradeConvertCombo+r6TsGuard 채택(ON)**: 사용자 R6 블런더(4O4C, 연구소미션4VP인데 무의미 TS) 2겹 — ①연구소(3O5C)/아카(6O6C) 크레딧갭 1O→1C 콤보 후보 ②R6 연방개선·미션 없는 TS 후보 제외(기존 무조건 +120이 부추김). 40판 +1.89/승률60.5%/TS-0.50 연구소+0.24 아카+0.36.
- **asteroidAnyFaction v2 채택(ON)**: 봇이 포머파괴 소행성 절대 못 짓던 필터버그(서버룰은 누구나, 사람 48건). v1(-140페널티) -6.40 p=0.012 기각 → v2(포머 idle시만: R5+ 또는 transdim 소진) +3.98 채택.
- **hang 근본수정 3중**: ①pending-build 실패→즉시 skip_ship_tech_mine/스텝포기(무한 재스케줄 기전 제거, 실패후보 JSON 로그) ②forceSkip이 pending 안 지우던 것 청소 ③월클록 워치독 20초. uk3aybql류 수 분 hang 봉쇄, 사용자 실게임 안정성. v2 run에서 HANG 0 확인.

## 2026-07-04 저녁 goal루프 — 경제궤적 비교(사용자 지시) + 실패채널 소멸 + 다턴뱅킹 1호
- **라운드별 경제 대조(PACE 12판 vs 사람 저널 24판)**: ①R1-3 VP 동률(10/8/7)! 갭=전부 R4+ 복리구간(사람 R4+19→R5+25→R6+42→정산+121) ②QIC: 봇 R2부터 0.3 고갈 영구(사람 1.1-1.5 유지) ③bowl3: 봇 1.1-1.6 vs 사람 3.3-4.3(파워액션 실탄) ④미션: 사람은 미션R에 같은액션 2-3회 burst(사전축적)=다턴, 봇 즉시소비. scripts/econTrajectory.mjs + PACE 게임파일화.
- **실패→강제pass 채널 소멸**: 920건/일 → 10건/40판. 번콤보 산수버그(burn1회=+1P인데 1개만 부착, 277건) 수정 + BONUSREJ/SHIPREJ 계측. 남은 건 enter_spaceship 7(사거리4/타클론brain-gaia3).
- **사고&회복**: 정규식 치환이 따옴표 깨서 TS에러 커밋(런타임 예외→HANG23) → 핫픽스. ★교훈: 파이프라인이 tc 에러시 커밋 중단하게 gate 필요(이후 ERRS 체크 도입). 부수확인: 깨진 코드에서도 워치독이 40판 전부 완주=hang방어 실전검증.
- **missionBankResearch 채택(ON)**: 다턴뱅킹 1호 — 다음R이 연구미션이면 이번R 연구보류→지식축적→미션R burst. 가드(L4보유/지식12+/R6). 40판 VP+2.30/연구+0.23/HANG0. 후속 후보: 뱅킹을 광산·TS·테라폼 미션으로 확장, QIC 재생산, bowl3 공급.
- **missionBankTS 채택(ON)**: 뱅킹 2호 — 다음R TS미션이면 mine→TS 1R 보류→미션R burst. 40판 +3.70/HANG0. 뱅킹 1·2호 합산 방향 일관 양(+2.30/+3.70). ★뱅킹 패밀리 = 다턴 중 유일하게 통하는 형태(플래너/search 전멸과 대조): 공개정보(미션 스케줄) + 자원 타이밍만이라 모델·평가기 오차 무관. 후속: 테라폼(갭 0.92)·가이아(1.0) 뱅킹.
- **missionBankGaia 보류(OFF)**: 뱅킹 3호. 40판 -1.69(노이즈) but 광산 -0.50 = 보류한 성숙포머 빌드 일부 증발(R6 경쟁에 밀림). ★뱅킹 경계 확정: **자원 뱅킹(연구지식·TS업글자원)은 증발 안함=양성 / 빌드 뱅킹은 증발 위험=음성.** 뱅킹은 자원형만.

## 2026-07-05 bonusTileHumanW 채택(ON) — 보너스타일 가중 데이터 교정
- 사람 vs 봇 선택분포: 사람 top=4pw충전 12%/2c-1q 13%, 봇은 1k-lab 12%(-8갭)/1o-1k 과애호. power ×1→2.5(bowl3 공급원)/know→2.5/qic→5.
- 1차 40판 승률33%(p=0.037)→확인 40판 57.5%(+0.26) = 첫 run 노이즈(양방향 curse 재확인). 누적 80판 VP -0.58 중립, 파워액션 +0.40/+0.51 두 run 일관(의도 행동). 채택. 진짜판정 1:3.
- **SHIPREJ 미러수정 2건 커밋(32f2dae)**: ①taklons 브레인-가이아 입장거부 미러 ②rangeBonusActive 포함 계산인데 useRangeBonus 미전송 mismatch. 검증 A/A 30판: SHIPREJ 0, 실패 5건(0.17/게임). ★실패→강제pass 채널 종결: 920건/일 → 0.17/게임.
- **QIC 재생산 측정(2026-07-05)**: rebellion 3정큐 47회/30판(1.6/게임, 이전 ~0)=소비체인 가동, 종료QIC 0.91. but AI트랙 L0.75(57% L0) — aiTrackQicEngine(후보점수×13) 무효 = "후보점수 nudge 무시" 함정 3번째 확인. 후보점수로 연구방향 유도 경로 종결(평가기/직접실행만 유효).
- **3일 누적검증(60판)**: 현재 vs 13-flag-OFF(3일전 전략) — 현재 +2.34VP/승률57.7%(노이즈). 행동 전부 의도방향(기술타일+0.48/파워액션+0.22/연방+0.07/총행동+1.05). 상호작용 악화 없음.

## 2026-07-05 사용자 관찰 4연타 — 연방·업글 순서/경제 교정 (전부 반영)
- **piBeforeFed 채택(ON)**: PI 미보유+건설가능이면 그 턴 연방 보류(의회 3-4파워 먼저→싼 연방). 실측근거: PI 연방前26/後41, xenos 前0/後6(문턱6 상실), 연방후 업글 51% 내부. 40판 VP -0.05 중립 + **연방 +0.09**(목표 방향) + HANG0.
- **fedWithK 부족분 변환 버그픽스**: k개 무조건 변환→연방이 spent개만 쓰면 잔여 증발(제노스 R6 8변환→4위성). 변환=max(0,spent-보유토큰)만. + oreFedGuard R6도 예비1/상한6(막라 광물=미션업글 가치, 사용자 지적으로 R6 관대 회수).
- **fed-12vp 비-초록 수정**: 푸시 2경로가 isGreen:true → 유일 예외 룰 위반(L5/고급 연료 오사용 가능). ship-fed-12vp는 초록 유지(사용자 확인).
- **bescods(매안) 업글트리 버그**: 봇에 매안 분기 전무 — TS→PI만 시도(서버 거부)→의회 영영 불가. 연구소→PI 소스 + TS→아카 직행 추가.
- **대시보드(수정묶음 반영 후 30판)**: VP 76.8(주초 66-70→75.3→76.8 상승세). 미션트리거 5.06(+0.6, 뱅킹효과), ★PI 前38/後28 역전(piBeforeFed 작동), 내부업글 51%→41%, 고급타일 유지(12vp green수정 부작용 없음). 연방 1.42(노이즈, 관찰유지).

## 2026-07-05 뱅킹 120판 격리(99판 유효) + 인프라 사고
- **뱅킹 패밀리 격리**: OFF 승률 54.5%/VP +1.41(노이즈) — 40판 +2.30/+3.70이 씻김(★winner's curse 4번째). '명확한 음수' 아님 + 대시보드 미션트리거 +0.6 실측 → ON 유지하되 효과 주장 철회. 뱅킹도 self-play론 중립이 진실, 1:3 판정.
- **★인프라 사고: 워커 오염 연쇄** — 워커2가 첫 hang 후 자기 게임 13연속 타임아웃→run wedge 6시간. 원인: h2h가 타임아웃 후 워커 서버를 재사용(오염 상태 지속). 수정 필요: 타임아웃 시 해당 워커 재시작(recycle).
- **누적 120판(14flag)**: 구전략 +3.10/56.3%(p=0.11) — 60판 +2.34(현재우세)와 부호 반대, 180판 합산 구+1.3(노이즈). 행동은 현재봇이 일관 사람방향(기술타일+0.61/파워액션+0.25), 구는 광산·연방 스팸형. ★self-play는 사람-정합 묶음을 중립~약음으로 봄(봇끼리는 광산스팸 유리) — 유지하되 self-play 이득 주장 안함, 판정=1:3. 인프라: 120판 타임아웃0 wedge없음.

## 2026-07-05 ★★ hang 근본원인 종결 (사용자 지시 "왜 자꾸 막히는지 찾아줘")
- **수법**: [HANG] 덤프 정밀화(필드명 오류로 turn=?(?)만 찍히던 것 교정 + pending 상세) → bal_tak 강제 재현(3건 공통인자) → 걸린 게임 로그 직독.
- **3층 구조 확정**: ①pendingEclipseResearch 봇 미처리(pending 19종 전수감사, 3종 미처리 발견) — 서버가 게이트하는데 봇이 영영 해소 못함 ②★봇 루프 예외 시 재스케줄 없이 침묵사(로그 뚝 끊김의 정체) + 예외 로그 콘솔-only(워커 stdio:ignore로 증발 — 원인이 안 보였던 이유) ③hang 워커 재사용→연쇄 타임아웃 확산.
- **수정**: ①eclipse_advance_track 함수 추출+봇 3경로 연결+forceSkip 청소 확장 ②예외 시 게임파일 스택 로깅+재스케줄(워치독 20s 회복 보장) ③워커 recycle+타임아웃 8분.
- **검증**: bal_tak 강제 30판 — 타임아웃 0, EXCEPTION 0 (이전 동일조건 1/30 재현되던 것). 향후 재발 시 예외 스택이 게임파일에 남아 즉시 원인 특정 가능.

## 2026-07-05 저녁 기존데이터 채굴 (사용자 지적 "있는 로그 분석해라")
- **★승자-패자 대조 5249판(신규 각도, within-game이라 A/B 한계 무관)**: 승자 판별력 = 연방 1.34× > 미션 1.26× > 깊은연구 1.21× > 기술타일 1.20× > 잔여자원 0.82×(소비가 이김). 사람갭 방향과 일치 — 개선 방향 대형 확인.
- **R6 해부**: 사람 R6 ~25액션(+42VP) vs 봇 ~8. 봇은 R6시작 광석 10.7(사람 8.2보다 많음!)을 쥐고 잔여 10+ 남기며 패스 = 소비 실패. 기전: 패스차단 게이트 R≤4 전용.
- **r6SpendDown 보류(OFF)**: R6 VP성 액션 있으면 패스 제외 → 40판 -1.19 노이즈 + 행동 무변(총행동 -0.79, 잔여 그대로) = 의도 발화 실패. ★해석: R6 burst는 패스금지가 아니라 '살 가치 있는 액션의 준비'(연방보상·고급연구·기술액션) — 엔진 질의 하류. 강제소비=잡동사니 구매.

## 2026-07-06 hhPiRush (HH 조기 PI) — ✅ 채택 ON (주의사항 포함)
- 동기: 실게임 부검(2026-07-03 fhhid49g, HH 33점): R2 TS 4연속(8O)→PI(4O6C) 광물 봉쇄 → PI 없는 HH는 신용 사용처 전무, 26~30C 만땅 사장(잔여자원VP 10). bot.ts의 R<4 hard continue로 PI 후보가 MCTS에 안 보였음(후보생성 갭 원리 재확인).
- 내용: ①HH 광산3+면 조기 PI 게이트(R<4·mine<5) 우회 ②PI 점수 +80(R≤2)/+60 ③PI 없는 TS3호기+ 감점 −140은 credits≥10(사재기 형성 중)일 때만.
- 측정(HH 강제좌석 paired): v1(무조건 감점) 24판 전체 VP +0.46, HH좌석 −6.9 → 감점을 credits≥10로 정밀화. v2 40판 전체 VP +0.60 승률 55%, HH좌석 −4.9. 행동: PI평균R 4.63→4.09(조기화), v1에선 HH변환 0.58→2.04.
- 판정 근거: 전체 VP 무해 2회 + 행동 의도대로 + 도메인 확실(HH PI=매라운드 4C→1Q/4C→1K/3C→1O 엔진, hadschHallasConvert 기본 ON이 순환 담당). ★주의: HH좌석 자체는 2회 연속 음수 방향(−6.9/−4.9, 합산 p≈0.26 노이즈)이나 전체마진(+0.5)과 모순 = 40판 해상도 밖. **진짜 검증 = 다음 1:3 HH 게임에서 PI 건설 여부·신용 사장 재발 확인.**

## 2026-07-06 새 1:3 게임 7판(7/02~03) 채굴 요약
- 봇 평균 ~64 vs 사람 182~248. 큰건물(PI+아카) 0개 봇 3명 평균 49 vs 1개 13명 66.
- taklons 37점 12위성 연방(파워토큰 전멸)·xenos 8광물 증발은 **7/04-05 채택분(oreFedGuard·부족분변환)보다 앞선 게임** = 기수정. 재발만 관찰.
- 잔여QIC 봇 0.85 vs 사람 0.83 동일(문제없음). 사람 QIC 46%는 우주선액션(Rebellion 135Q/Twilight 108Q/Eclipse 50Q) — 봇도 rebellion+twilight 커버됨.
- 연방수별 프로필(20,850시드): 연방 +1개당 건물파워 +2.5·VP +15.7, 2연방 봇 위성 11.2개 사용 — 위성 다리 통화, 기존 채택 방향과 일치(신규 레버 없음).
- 미해결 관찰: 우주선 입장 −5VP 대비 회수 부족(taklons 3입장 −15VP 회수 반토막) — 입장 결정에 잔여라운드×기대사용 평가 없음. 다음 사이클 후보.
- 실게임 에러 스윕 7판 0건(실패 채널 클린 유지).

## 2026-07-06 우주선 입장 ROI 스코핑 (다음 사이클 재료, 미구현)
- 2만 시드: 승자 입장 2.32회/우주선VP 7.9/순VP −3.7 vs 패자 2.05회/4.8/−5.5. VP0 공치기 시드 승34% vs 패48%.
- usedActionBy 기준 "죽은입장(액션 0회)"은 승62%/패64%로 **대칭 = 승패 무판별**. 단 usedActionBy가 Rebellion류 무제한 액션·shipTech·ship연방보상을 안 잡는 것으로 의심(계측 불완전) → 신뢰 불가.
- 결론: 입장 억제는 위험(승자가 더 많이 입장, 사람 QIC 46%가 우주선액션). 유효 레버가 있다면 "입장 후 수익화"인데 이는 R6과 같은 엔진질 하류 가능성. 진행하려면 먼저 usedActionBy+shipTechByShip+spaceshipFederationByShip+gameLog 통합 계측부터.

## 2026-07-06 ★버그수정+채택(ON): finalMissionFix — 최종미션 정렬이 죽은 필드였음
- **동기**: 새 1:3 로그 2판(fy42d29p·9a5dmht7) 분석 — 6봇 좌석 중 2곳이 **최종미션 0점**(xenos·bescods), 나머지도 15~21 vs 사람 36. 승자판별 미션 1.26×인데 봇이 통째로 버림.
- **근본원인(버그)**: `bot.ts calculateFinalMissionBonus`가 `game.finalScoringTiles`를 읽는데, 이 필드는 셋업 시 더미 `[{id:'fs1'},{id:'fs2'}]`로 초기화되고 **실제 미션으로 갱신 안 됨**(실제 미션은 `game.finalMissionIds`). → `fm_*` switch가 영영 매칭 안 됨 = **빌드후보 스코어링 13곳의 최종미션 정렬이 죽은 코드**. evaluator(evaluator.ts:838)는 별도로 올바른 `finalMissionIds`+`getFinalMissionVp`를 써서 부분 보완하고 있었음(그래서 완전 0은 아니었음).
- **수정**: `finalMissionIds`를 읽도록 교정 + 부활 시 드러난 누락 case 보강 — `fm_federation_buildings`(내 클러스터 인접 빌드 +12, 과군집 방지 modest)·`fm_outer_sectors`(새 외곽섹터 +25)·`fm_pi_academy_distance`(PI/아카 배치 시 반대짝과 거리×5)·`fm_gaia_planets`에 transdim 포함(서버 채점 일치). `fm_satellites`는 빌드가 직접 안 만들어 제외.
- **자매버그 확인**: `roundScoringTiles`는 서버가 제대로 채움(gameState.ts:5004) → 라운드미션 스코어링 정상. `finalScoringTiles`가 봇이 읽던 유일한 죽은 필드.
- **측정(40판, 가중치격리)**: VP +3.27(78.3→81.5) 승률 57.5%(p=0.307 유의성미달). 행동 의도방향: 연방 +0.14·총행동 +1.42·광산 +0.42·가이아광산 +0.36·우주선액션 +0.16·**PI평균R −0.57(조기화)**. 연구진행 −0.13(미미).
- **판정: ✅ 채택(기본ON)** — 실제 버그 수정 + 음수 아님 + 행동 일관 양성(더 큰/더 연방적인 엔진). ★주의: +3.27은 첫 40판이라 winner's curse 가능(4~5회 실증)→순효과 do-no-harm~약양성으로 볼 것. 진짜 판정 = 다음 1:3에서 봇 최종미션 0점 재발 여부. 확인 120판은 여력될 때.

## 2026-07-06 채택(ON): fedEngineUpgOutside — 연방 내부 엔진업글을 연방 밖으로 유도
- **동기**: 사용자 관찰("여전히 연방 내부 건물 티어업이 많다"). 재점검 결과: `noFedTierUp`(ON)이 연방건물 티어업을 막되 **엔진업글(TS→연구소/PI)은 '항상' 통과**시킴 → -450 fedPenalty를 MCTS가 무시(후보 컷 아님)하고 연방 안 TS를 연구소/PI로 올림. 전면차단은 −3.56 기각 전례(TS→랩 엔진까지 막음).
- **교정**: 엔진업글 자체는 유지하되 **같은 타깃의 비연방 대안이 있으면** 연방 안 업글만 후보에서 제외(점수 아닌 필터=결정적). 비연방 대안 없으면 기존대로 허용(−3.56 실패 회피). 엔진 파워증가가 다음 연방 씨앗이 되게(fedMinTrim 정합).
- **측정(40판, 가중치격리)**: VP +4.17(76.5→80.6) 승률 61.5%(p=0.225 유의성미달). 행동 의도방향: 연방 +0.15·총행동 +2.08·기술타일 +0.35·우주선액션 +0.35·가이아광산 +0.24·**PI평균R +0.32·TS평균R +0.26(업글 절제=늦춤)**.
- **판정: ✅ 채택(ON)** — 음수 아님 + 행동 일관(연방↑·업글 절제=정확히 의도) + 도메인 옳음. ★+4.17은 첫40판 winner's curse 가능→순효과 do-no-harm~약양성. 진짜판정 1:3.

## 2026-07-06 채택(ON): fedPreferUpgradeSelfClose — VP+5.40, 행동 양성(단 연방 개수는 flat)
- **동기**: 사용자 관찰(4+1+2 예시) — 봇이 위성으로 뻗어 자립가능 별도 클러스터까지 삼켜 거대연방 1개 → 미래 연방 소진(봇 1~2 vs 사람 5).
- **구현**: 위성 브리지 연방이 '자립 씨앗 클러스터(단독 ≥요구−2 파워)'를 병합할 때만 페널티(seeds×140). cannibalizedSeedCount 헬퍼.
- **측정(40판, 가중치격리, 챔피언=finalMissionFix+fedEngineUpgOutside ON)**: VP **+5.40**(75.6→81.0) 승률60% p=0.083. 행동 크게 변화: 총행동 +2.36·우주선액션 +0.60·연구진행 +0.42·기술타일 +0.30·파워액션 +0.24. **단 연방 Δ+0.01(개수 flat).**
- **판정: ✅ 채택(ON)** — 처음 "연방 개수 flat=노이즈"로 보류했다가 정정(사용자 지적): 기준은 'VP 음수 아님 + 행동 일관 + 도메인 옳음'이고 셋 다 충족. 행동이 크게 바뀜(compact 연방→건물 풀림→템포↑) = "행동 무변=노이즈" 케이스 아님. ★단 '연방 개수↑' 효과는 미발현이라 주장 안 함 — do-no-harm+엔진/템포 양성으로 채택. 진짜판정 1:3.
- **★3런 교차관찰(중요)**: 세 채택(finalMissionFix/fedEngineUpgOutside/이것) 챌린저=81.5/80.6/81.0 **안정**, 챔피언만 78.3/76.5/75.6 흔들림 → 마진(+3.27/+4.17/+5.40)이 커진 건 챌린저 개선이 아니라 **챔피언 표본 변동**(절대VP 표본노이즈 실증). 셋 동급, 개별 마진 과대. **per-flag 실효과는 작음** → 누적강도는 '전부 ON vs 과거봇' 별도 런으로만 확인.

## 2026-07-06 기각+되돌림: artifactVpTiming — ★잘못된 레버(구현 결함, 사용자 지적)
- **동기**: 사용자 관찰 "6파워(bowl3 실탄)로 3VP 스케일인공물 조기 취득=손해". 사람 실측: 스케일-VP 인공물 R1-3 취득 0건, R4-5 중앙값 9-12VP.
- **구현(결함)**: art-vp-* R1-3 금지 + R4-5 rawVp<9 금지 (게이트 미달 score=10).
- **측정(40판)**: VP **−3.31**(79.3→76.0) 승률 42.5%.
- **★사용자 지적(옳음)**: 의도는 '타이밍 이동(나중에 더 크게 먹기)'인데 구현은 '조기엔 안 먹기'=**총 취득 수 감소**. 인공물은 경쟁 슬롯이라 안 먹으면 상대가 가져가 영영 못 먹음 → count 감소 = 버그. 게다가 **자연 스코어링이 이미 나중=고가치를 담음**(rawVp가 진행하며↑ + 배수 R5에 12→24) → 하드 라운드차단은 불필요+유효 조기취득까지 죽임. 원래 '6파워 기회비용'은 net-value로 다뤄야 했지 라운드차단이 아니었음.
- **판정: ❌ 기각 + 코드 되돌림**(잘못된 레버라 보존 가치 없음). 인공물은 자연 스코어링에 맡김. 재시도 시 '취득 수 유지 + net-value(6파워 기회비용) 반영' + 취득 count 계측 필수.

## 2026-07-06 기각(OFF): qicEngineValue — QIC 기아는 평가기 route로도 안 풀림 (★2경로 확정)
- **동기**: shipActionDiag 확정 — 우주선 최강 #1액션이 QIC부족 74~94% 탈락. AI트랙(QIC 즉시부여 L4까지 6QIC)을 평가기에서 상향하면 QIC 확보→우주선 열림 가설. aiTrackQicEngine(후보점수)과 달리 평가기 term=유효 route.
- **사람 타이밍 반영(사용자 교정)**: 55게임 177전진 — AI투자 91%가 R4+, L1첫진입 중앙 R5·기술6·연방3. flat R1부스트(확장 뺏음) 대신 R5-6=22·R4(연방有)=18·R≤3=기본10.
- **측정(40판, 가중치격리, +SHIPDIAG)**: VP −2.00(77.0→75.0). 행동은 의도방향(우주선액션 +0.58·기술타일 +0.47·연구진행 +0.39). **BUT SHIPDIAG 병목 안 풀림**: twilight#1 6%→5%(R4+ 8%), rebellion#1 11%→7%, tf_mars#1 26%→17% — QIC eligibility 그대로/하락.
- **판정: ❌ 기각(OFF)** — ★핵심: QIC 기아는 **후보점수(aiTrackQicEngine 중립)도 평가기(qicEngineValue −2.00)도 못 고침**. QIC를 버는 즉시 소비(거리QIC·확장)해 eligibility가 안 오름 = "AI트랙 더 평가"가 레버 아님. **진짜 병목은 QIC throughput(구조적)**. AI투자 지식이 확장 뺏어 net음수. 자원엔진류 후속은 'QIC 소비처 억제/보존'이나 'QIC 대량공급 소스(종족PI·특정 고급타일)' 쪽이어야. 코드 OFF 보존.

## 2026-07-06 기각+되돌림: bescodsLowestValue — 매안 science-우선은 버그 아니라 최선 (★반직관)
- **사용자 관찰**: "매안이 자꾸 첫 트랙으로 science 올린다". 매안 무료 특수(매 라운드 최하위 트랙 +1, executeBotBescodsAdvanceLowestTrack)의 동점 타이브레이크가 임의로 science 1순위·확장 꼴찌였음.
- **가설**: science(초반 최저가치)에 공짜전진 낭비, 확장(#1 갭) 방치 → 가치순(terra/nav/gaia 우선)으로 재정렬.
- **측정(bescods 강제 40판 paired)**: ★[bescods] 신순서 83.5 vs 구순서 93.6 → **Δ −10.10**(p≈0.13). 매안 좌석 −10 = 큰 손해(fedCompactBuild −4.22보다 큼) = 노이즈 아님.
- **판정: ❌ 기각+되돌림** — ★반직관 확정: 매 라운드 **공짜** 전진을 방치된 science에 넣어 **트랙 폭/균형**(L3 파워·breadth 보너스·최종미션 다양성) 확보 + 유료연구는 확장 집중 = 매안 최선. 확장을 공짜전진에 쓰면 유료연구와 겹쳐 과집중+science 사장. 사용자 관찰은 사실이나 버그 아님 → 구 science-우선 복원, 코드에 "건드리지 말 것" 주석. ★교훈: 무료 자원의 최적배분 ≠ 유료 자원(공짜는 방치트랙 메우기가 이득).

## 2026-07-06 채택(ON): ivitsStationFreeFirst — 하이브 우주정거장 무료자리 우선
- **사용자 관찰**: 하이브가 모행성 옆 무료(QIC0) 자리 두고 연방하려 이상한(QIC 드는·먼) 위치에 정거장 배치.
- **원인**: ivitsFedAwareStation 정렬 1순위가 adjOwn(인접 내건물 수) 내림차순 → QIC 내고라도 더 연결된 자리 우선.
- **교정**: 정렬 1순위를 neededQic 오름차순(무료 우선)으로. 공짜 연결자리 있으면 QIC 안 쓰고 거기(그중 인접건물 많은 순), 무료 없을 때만 QIC 지불.
- **측정(forced-Ivits 40판 paired)**: ★[ivits] 무료우선 70.0 vs 현재 67.3 → **Δ+2.80**(p≈0.62). 전체 −3.88은 non-Ivits 표본노이즈. Ivits 좌석 양수 + 낭비제거(명백히 옳음) → 채택. (bescods −10 기각과 대조 — 이건 양수.)

## 2026-07-06 라운드 내 긴급도 순서 (사용자 모델: 뺏길 수 없는 액션은 뒤로) — 3플래그
- **bescodsLateSpecial ✅ 채택 ON (매안강제 40판)**: 매안 트랙업이 botHandler에서 라운드 첫턴 무조건 자동사용(메인액션 소모)이던 것을 MCTS 후보(bescods_advance_lowest)로 전환 — 인에이블러면 일찍, 아니면 자연히 늦게(사용자 교정: 무조건 늦게도 콤보를 깨서 안 됨). 패스 인터셉트 안전망(유실 0, 연구진행 5.17=5.19 확인). L5경쟁(최하위 L4)은 즉시사용 유지. 전체 VP +0.89, ★매안좌석 82.0 vs 77.4 (+4.6).
- **deferSafeBuild ⏸ 120판 재확인 대기**: 무경쟁(상대 3헥스 밖) 가이아 1QIC 건설을 파워액션 후보 있는 턴에 하드필터로 미룸(자원 2회분+비패스턴 가드). 40판: 승률 67.6% p=0.033, VP +1.49 — 단 ★가이아광산 2.24→1.68(−0.57) = '순수 재배열' 의도 위반(일부 영영 안 지음). candidate expansion(71.8%→42.5%) 동형 winner's curse 위험 → 120판 + 행동 재검 후 결정.
- **balTakShipQic 🧪 테스트 예정**: 발타크 포머→QIC 변환이 광산 경로에만 연결, 우주선 액션(리벨리온 3정큐 등 4종)·입장 거리 QIC는 지갑만 체크 → 유효QIC(지갑+미사용 포머)로 게이트 확장 + 부족분 변환 preActions(사용자 지적).

## 2026-07-06 balTakShipQic ✅ 채택 ON (발타크강제 40판)
- 포머→QIC 변환을 우주선 액션 4종(리벨리온 3정큐 등)·입장 거리 QIC까지 확장(기존 광산 경로만). 유효QIC 게이트 + 부족분 변환 preActions.
- 전체 VP +0.71, ★발타크좌석 77.2 vs 73.0 (+4.2). 우주선입장 +0.08/액션 +0.20/기술타일 +0.09 모두 양성. 후보생성 갭 교정 + 도메인 확실(사용자 지적).

## 2026-07-06 deferSafeBuild ✅ 채택 ON (120판 재확인)
- 40판 승률 67.6% p=0.033 → 120판 48.7%로 회귀 = ★winner's curse 5번째 실측. 단 VP는 +1.49→+1.29 두 번 다 양수(일관).
- 40판의 가이아광산 −0.57은 노이즈였음 — 120판 −0.08(불변) + 총행동 +0.61 = '무경쟁 가이아 건설을 미뤄도 결국 지음(순수 재배열)' 의도 확인.
- 판정: 비음수 일관 + 행동 의도대로 + 도메인 옳음(사용자 순서 모델: 뺏길 수 있는 파워액션 먼저) → 채택.

## 2026-07-06 geodensPiFirst ❌ 기각 OFF (기오덴강제 40판)
- 진단은 유효: 첫랩 360 vs 기오덴PI 140 + R≤2 광산<5 게이트로 PI 후보 차단 → 순서 역전(사용자 관찰).
- 시도: 게이트 우회 + PI 지불가능 턴 랩 defer(순서 재배열). 결과: 전체 −1.23, 기오덴좌석 68.2 vs 70.9(−2.7), ★PI건설 0.65→0.51 하락(의도 반대) — 랩을 치워도 MCTS가 PI(점수140) 대신 광산/파워액션을 골라 자원 소모 → PI도 랩도 못 짓는 케이스 증가. PI평균R 3.90 조기화는 됐으나 건설률 하락이 상쇄.
- 교훈: 순서 재배열이 안전하려면 '미룬 것이 반드시 돌아와야'(deferSafeBuild) 하는데, 여기선 미룬 랩 대신 PI가 선택된다는 보장이 없었음. 근본 원인 = 평가기가 기오덴 PI의 미래가치(새 유형×3K 스트림)를 모름 → 후보 조작으로 불가, 평가기(computeScore)에 종족 PI 가치 모델링이 필요(미착수).

## 2026-07-07 geodensPiValue ❌ 기각 (winner's curse 6번째 — 40판 유망→120판 유의음수)
- 평가기 항: 기오덴 PI 보유 시 '도달권 새 유형 수 × 25' 가산. 40판: 좌석 +8.4, VP +5.47, 승률 65% p=0.058, PI 조기화+건설률 유지 — 全지표 유망.
- ★120판 재확인: 승률 39.5% p=0.022 **유의하게 약함** — 채택 금지. 좌석 VP는 +1.5 중립인데 승률이 유의 음수 = PI 조기화가 게임 승부처에서 손해(추정: 4O6C 조기 소모가 확장/기술 타이밍을 밀어냄).
- 교훈: ①40판 '전 지표 양성'도 믿지 말 것(경계 p는 반드시 120판) ②기오덴 PI 조기화 자체가 셀프플레이 메타에서 이득이 아닐 수 있음. 코드는 플래그 0=OFF로 보존.

## 2026-07-07 bescodsPiBeforeFed+bescodsPiValue(100) ❌ 기각 (winner's curse 7번째)
- 사용자 모델: 매안 PI(파워4) 먼저 → 싼 연방. 구현: 조기 PI 후보(랩1+광산3+) + 평가기 PI보유 +100 + piBeforeFed 시퀀싱.
- 40판: 좌석 +6.7, 연방 +0.21, PI건설 +0.09, VP +4.03 全양성 → 120판: 승률 42.5%, VP −1.97, 좌석 +1.8, ★PI건설 0.48(효과소멸). 시점 조기화(−0.50R)·연방(+0.09)만 잔존.
- 판정: 전체 음수 방향 + 목표행동(PI율↑) 미달성 → 기각. geodens와 함께 "조기 PI 강제는 셀프플레이 메타에서 손해" 2연속 — 4O6C 조기 소모가 확장/기술 타이밍을 밀어내는 비용이 PI 이득을 상회하는 것으로 추정.
- ★유보: 사용자 1:3 관찰("사람은 대부분 PI 먼저")과 셀프플레이 판정이 충돌하는 케이스 — 사람 상대 실효는 미검증. 필요시 플래그 켜서 1:3 실측 가능(코드 보존, OFF).

## 2026-07-07 기각(OFF): techTileRankFix — 기술타일 랭킹, 역효과
- 사용자 랭킹(4C>4PW>1o1P>1K1C, 1K1C=필러) 반영 + 광석기아 시 1o1p 우대. income 타일 획일 120 차등화.
- 측정(40판, +ratioDiag): VP **−2.75**, 광석기아 16.9%→**19.7%(악화)**, 연구진행 −0.60·경제상승 −0.60.
- ★원인: ①1K1C 페널티가 track-advance(1K1C를 트랙 올리려 먹는 용도)를 깎아 연구 전진↓. ②17% 광석기아는 tile-pick이 아니라 **구조적 광석수입 부족**이라 tech-tile로 안 고쳐짐(오히려 악화). 잘못된 레버.
- 판정: 기각(OFF). 기술타일 선택은 직접-픽이라 행동은 바뀌었으나(연구·우주선액션 변동) 방향이 손해. 광석기아는 income 구조(광산 유지·경제 과투자)로 접근해야.
- ★계측 확정(baseline): **광석≤1 & 크레딧≥4인데 빌드 안 하는 결정이 16.9%**(7117 결정 중 1204). 봇 액션갭의 핵심 = 구조적 광석수입 부족. mine→TS 340건은 다 광석 있을 때(same-turn 기아전환 0), 즉 다음R 광석수입↓로 작용 추정.

## 2026-07-07 기각(OFF): mineKeepGate — income-side로 액션갭 못 고침 확정
- 가설(사용자): 광산을 TS로 바꾸면 광석수입 무의미화 → 다음R 광석기아. good-case(광산3/첫TS/연방) 아니면 mine→TS 하드게이트로 광산 유지.
- 측정(40판, +ratioDiag): VP −1.27(노이즈). 교역소 −0.51(게이트 작동), **but 광산 −0.44(안 늚)·총행동 −2.95(액션 감소)·광석기아 16.9%→15.4%(거의 그대로)**.
- ★결정적: TS 막아도 봇이 광산 더 안 짓고(못 짓고) 보존 광석을 빌드로 안 바꿈 → 그냥 액션 덜 함. mineFirstExpansion −2.92 재확인.
- ★★세션 확정: **17% 광석기아=액션갭은 income-side(tech-tile·mine-keeping) 손규칙으로 안 고쳐짐**(2각도 다 실패/역효과). 진짜 벽=확장(광산/행성 못 늘림, 사거리·다턴 협응). 단발 룰 5연속 실패(artifact/qic/bescods/techTile/mineKeep). → 다음은 좌표된 오프닝 스크립트 / path2 / 정책망(큰 프로젝트).

## 2026-07-07 채택(ON): expansionEngineOpen — 확장엔진 오프닝(빌드오더 갭 첫 타격)
- **빌드오더 데이터(사람게임 R1-3, 봇48/사람32 좌석)**: 봇 테라 0.19/가이아 0.15/가이아포머 0.17 = 사람(0.94/1.03/0.94)의 15~20%. 광석엔진(광산+테라+포머) 사람 8.63 vs 봇 5.35. 대신 내비(1.65vs1.16)·경제·TS 과투자. **= 광석기아·96점갭·17% 액션갭의 단일 뿌리.**
- **구현**: R1-3·지식≥4·연방아님·할인업글없음·bal_tak제외면 방치된 가이아(L1=가이아포머)→테라(광석·buildability) 연구를 **직접-리턴 강제**(점수 nudge는 MCTS가 덮으므로. researchLikeHuman 점수판=행동무변이었음).
- **측정(40판, +ratioDiag)**: VP **+0.88**(음수아님). ★가이아포밍 0.56→**1.23(+0.66, 2.2배)**=행동 실제 이동! 연방 +0.10·TS −0.26. 광석기아 16.9%→16.0%(약간), 광산/총행동 약간↓.
- **판정: ✅ 채택(ON)** — 음수아님 + 행동 정확히 의도(확장엔진 가동) + 도메인옳음. ★손규칙 5연속 기각(income/tech/mine류) 뒤 첫 non-negative+의도발현 = 빌드오더-타깃 + 직접리턴 접근이 옳음을 증명(점수 vs 메커니즘 교훈 실증).
- **한계/다음**: 첫 발. 가이아포밍은 2.2배지만 광석기아 거의 그대로·광산 약간↓ = "가이아 행성 생성"은 시작했으나 "그 위에 광산 건설(광석 전환)" 다운스트림 미완. 다음: 가이아포밍→가이아광산 건설까지 이어지게 + 내비/경제 초반 억제.

## 2026-07-07 채택(ON): humanRule7C + mineCreditCombo — gain-7-credits 미사용/크레딧기아 교정
- **사용자 관찰(반복)**: 봇이 4파워 있으면서 gain-7-credits(4P→7C) 대신 연방 前 `fedSpendBowl3`가 idle bowl3를 1P→1C ×4(=4C)로만 현금화하고 연방함. "7c먹고 연방하면 크레딧기아 더 벗어남". → 1P→1C=1C/파워 vs 7C=1.75C/파워, 같은 파워로 3C 손해(하위호환 상시 사용). gain-7-credits는 main action이라 연방과 동턴 불가 → 진짜 선택="연방now+4c" vs "7c now+연방 다음턴".
- **사람 데이터 검증(actionJournal 사람좌석 395결정)**: 사람이 7C 누르는 상태 = C4 O2 P3=5, C4 O7 P3=5, **C6 O11 P3=4**(광석 많아도 크레딧 병목이면 누름), C7 O4 P3=5 → **C≤7 & P3≥4**. gain-2-ore는 C24/C19(크레딧부자)에서 = humanRule2O 검증.
- **humanRule7C**(bot.ts~655, 직접-return): v1 무조건 C≤4 → 40판 크레딧기아 25→17%(성공)이나 **VP −2.38·광석기아 16→20%·광산 −0.34**(크레딧 풀리니 병목이 광석으로 이동=확장벽). → **v2 프로브**: (a)지금 못 짓고 (b)+7C면 build_mine/업글이 열리며 (c)광석≥2일 때만 발동 = "크레딧이 유일 병목"일 때만. 병목이동 방지.
- **측정(v2+mineCreditCombo 결합, weightsDiffer=false)**: 40판 VP −0.53·크레딧기아 19.1%·광석기아 15.5%. **120판 확정: VP −0.79(SE1.84, p0.67=평탄)·총행동 +1.15·광산 +0.22·가이아포머 +0.15·powerAct +0.23·연방 +0.02. 크레딧기아 25→18.7%·광석기아 16→18.3%.** 상호배타 기아합 41%→37% = 정체결정 ~4%p를 행동으로 전환.
- **판정: ✅ 채택(ON)** — VP 노이즈범위 평탄(음수아님=disaster필터 통과) + 행동 전부 의도방향(7C 실제사용·크레딧기아↓·행동↑) + 도메인옳음(7c>4c 산술) + **사용자 실전관찰 정확교정**. mineCreditCombo 단독은 40판 +3.74(참고). 자가대국 VP는 이 실전문제를 과소평가, 1:3이 판관(스킬 원칙).
- **한계/다음**: 행동교정이지 120벽 강화 아님. 광석기아 소폭↑(mineCreditCombo 광석소모)은 **구조적 레버=조기 TS 크레딧수입**으로 이어감(사람: C낮음/O높음일 때 교역소 19% 건설로 크레딧 *수입* 확보 — 봇은 스팟만 만져 매R 재기아). 다음 실험: credit-poor+ore-rich 지속 시 mine→TS(크레딧income) 우선.

## 2026-07-07 기각(OFF): creditIncomeTs — 크레딧 income용 강제 mine→TS, TS/변환-side 6번째 실패
- 가설(사람데이터+사용자 휴리스틱): 크레딧빈곤+광석부자(C≤4/O≥5)+광산≥3(수입정체)면 사람처럼 mine→TS로 **크레딧 수입** 확보(스팟 아닌 income)로 매R 재기아를 끊는다. 3번째+ 광산은 수입정체라 TS 전환=손실없는 순이득 논리.
- 측정(40판, 채택베이스[7C+mineCreditCombo]ON 위, weightsDiffer=false): VP −0.46(평탄) **but 의도행동 미발현** — TS **−0.23(오히려↓)**·tsEarly ±0.00(거의 발동 안 함)·총행동 −0.52·연구 −0.25. **광석기아 16→22.4%(악화)**(TS업글이 광석2 소모).
- 판정: 기각(OFF). "행동 안 바뀌면 전략 무의미" 원칙 — 목표행동(TS/크레딧income↑) 미달성 + 광석기아 악화. 조건이 좁아 발동희소 + 발동시 광석소모 역효과.
- ★★확정: **TS/변환-side 레버 6연속 실패**(mineKeepGate·mineFirstExpansion·techTileRankFix·creditIncomeTs 등). 크레딧기아를 income 구조로 고치려 해도 TS강제는 광석을 깎아 병목만 옮김. 스팟교정(humanRule7C/mineCreditCombo)은 do-no-harm 한계. **진짜 벽=확장(expansionEngineOpen 노선: 가이아포밍→가이아광산 건설 다운스트림 + 내비/경제 초반억제)**. 손규칙 클래스 종결, 확장/정책망 큰프로젝트로.

## 2026-07-07 채택(ON): shipActionBalance — 자원밸런싱 정책의 직접-return 부활 (세션 최강 결과)
- **근본원인 발견**: 사용자 자원밸런싱 정책(돈많으면 소비-확장/돈없으면 income 우주선액션)은 **이미 2026-06-15 `shipResourceBalance`(기본ON)로 코딩돼 있었으나 점수 nudge(+130/+90)**라 MCTS 평가기가 덮어(후보점수 무시 교훈) 행동에 안 나타남 = "제안 반영해도 봇이 안 따름"의 구조적 정체.
- **수정**: humanRule2O/7C처럼 **직접-return** `shipActionBalance`. 돈≥9 → TF마스#3(3C→테라+광산)·이클립스#3(6C→소행성광산)=크레딧풍선→확장. 돈≤5 → 리벨#2(광산→TS,1O3P)·트왈#2(TS→연구소,2O3P)=무비용 엔진충전. findSpaceshipActions가 canUseShipAction+타깃 검증한 액션만. 연방/할인업글 우선.
- **측정(weightsDiffer=false)**: 40판 VP +1.07(발동69) → **120판 VP +1.92(SE1.89, bWin54.6%, 발동211: TF마스#3=80·리벨#2=65·이클립스#3=53·트왈#2=13)**. shipAct +0.34·총행동 +0.47·광산 +0.05·가이아포머 +0.04(전부 중립~양성). 크레딧기아 19.1%·광석기아 18.0%.
- **판정: ✅ 채택(ON)** — ★VP가 확정판에서 오히려 강해짐(+1.07→+1.92, mineCreditCombo 회귀와 반대) + 정책 실제발현(211회) + 사용자 정책 + 도메인옳음. **이 세션 최강 결과.** nudge→직접-return 교훈 재실증.
- **함의**: 후보점수 nudge로 코딩된 다른 사용자 정책들도 같은 이유로 무력화됐을 가능성 — 행동 안 바뀌면 직접-return 전환 검토. shipResourceBalance nudge는 in-between 폴백으로 ON 유지(직접-return이 극단 케이스 선점).

## 2026-07-07 채택(ON): gaiaMineFollow — 가이아포머→광산 회수(확장 다운스트림 완성)
- **사람 로그(56게임/99좌석) 분석**: 가이아포머 배치→가이아 광산 건설 = **100%(62% 같은 라운드)**. 사람은 포머를 투기적으로 안 놓고 반드시 광산 회수. 사람 광산 건설률 R1-3 각 ~2채(총 15.2/게임) vs 봇 ~8.4(절반). 사람은 버퍼(8C/4O)에서 건설·기아<9%(봇 크레딧풍선과 반대). R1 첫수 TS업글 63%, R2=광산스팸, 내비 우선.
- **가설**: expansionEngineOpen로 가이아포밍은 2.2배 늘었으나 그 위 광산 건설 다운스트림 약함(광산 −0.34) = sink 누락. 내 완성 가이아포머(pendingGaiaformerTiles) 행성에 광산 건설을 직접-return 강제(점수 nudge는 MCTS가 덮음). 자금은 mineCreditCombo/humanRule7C가 pre-fund.
- **측정(weightsDiffer=false)**: 40판 VP +2.06·총행동 +2.59·광산 +0.75(유망) → **120판 확정: VP +0.37(SE2.02, p0.85=평탄)·가이아광산 +0.41·가이아포밍 +0.17·광산 +0.26·총행동 −0.42·powerAct −0.33. 발동 251회(~2/게임).** 40판 큰값은 winner's curse.
- **판정: ✅ 채택(ON)** — VP do-no-harm + ★의도행동 확고(가이아광산 +0.41·포밍 +0.17, 251회 = 사람 100% 패턴 재현) + expansionEngineOpen 체인 완성 + 사용자 목표(확장). VP 강화 아닌 낭비교정 성격(가이아포머 방치 방지). 자가대국은 이 실전가치 과소평가 가능.
- **다음**: powerBeforeFedBurn(연방이 bowl3 태우기 전 파워/우주선액션으로 먼저 쓰기=재활용+고가치) 준비(OFF). 사람 플레이북 잔여: r1TsFirst(R1 TS 63%)·내비우선.

## 2026-07-07 버그수정: ore→token 연방이 소모될 bowl3 미회수 (커밋 bfc35ec)
- 사용자: "제노스류 연방 시 토큰 바꾸고(ore→token) 3그릇 안 쓰고 연방 여전". fedWithK 경로(shortfall>0=토큰부족→bowl3 포함 전부 위성 소모)에 doomedBowl3Cashout 미부착 → 소모될 bowl3 가치 0회수. 수정: ore→token 뒤 doomedBowl3Cashout(spent, bowl1Extra=shortfall) 추가. 무변환 경로와 통일. 낭비회수라 측정불요.

## 2026-07-07 기각(OFF): powerBeforeFedBurn — 실전 발동 희소(죽은 룰)
- 사용자 전략: 연방이 bowl3를 위성으로 태우기 전 gain-7-credits/2-ore로 먼저 씀(bowl3 재활용+고가치 1.75c/파워 vs cashout 1c/파워).
- 측정(40판): 발동 **3회뿐**(gain-2-ore). VP −0.31(노이즈), 행동델타 전부 노이즈(3회론 설명 불가). 조건(fed satellite가 bowl3 ≥4 소모 + p3≥4 + 파워액션 가능)이 실전에 거의 없음 — 대부분 연방은 bowl3를 그만큼 안 태움.
- 판정: 기각(OFF). 이상은 옳으나 실용 빈도 미미 + fedWithK bowl3 cashout(bfc35ec)이 이미 소량 낭비 회수. 임계값 낮추면 연방 지연 리스크만 커짐. 코드 OFF 보존.

## 2026-07-07 기각(OFF): expansionMineDrive — 광산 강제 7번째 실패 (확장 벽 = 평가기 문제 확정)
- 사람 로그 전수: 광산 사람 ~14 vs 봇 8.3(57%)=진짜 벽. R2-4 페이스 미달(광산<2×R)이면 봇 자체 최고점 광산 직접-return(mineFirstExpansion −2.92와 차별: R1제외·페이스게이트·사거리반영 최고점·연방/연구 양보).
- 측정(40판): **VP −4.08**(bWin35.9%, p0.21)·**광산 −0.90(강제했는데 감소!)**·총행동 −0.96·powerAct −0.33. 발동 106회.
- ★결정적: 강제가 빌드오더 교란(이른 시점 차선 광산 → 이후 더 좋은 확장 차단) → 광산 경제 악화. 챔피언 9.19 vs 챌린저 8.29.
- **판정: 기각(OFF). 광산/확장 강제 룰 7연속 실패**(mineFirstExpansion·mineKeepGate·creditIncomeTs·expansionMineDrive). **확장 벽은 직접-return 강제로 불가** — 봇 문제는 "광산 선택 안 함"이 아니라 게임 전체(위치·사거리·시퀀싱)가 광산을 뒷받침 못 함 = **평가기(가치함수) 문제.** gaiaMineFollow(투자 회수형)만 do-no-harm으로 예외 생존.
- **다음: 모델 경로**(사용자 승인 "둘 다 순차" 2단계). 단 policyNet(정책 prior)는 MCTS가 덮으므로 실효 의문 — 진짜 레버는 **가치망(evaluator)**이 확장 포지션을 옳게 평가하게 하는 것(더 어려움). 정책망 59게임 재학습은 완료(선형 25.6%, 약함).

## 2026-07-07 무효/롤백: humanValueBlendK 재측정 (내 파이프라인 실수)
- humanValueNet을 59게임 재학습하려 reconstructHumanFeatures→trainHumanValueNet 사용했으나, 그 파이프라인은 **매 결정 12902샘플·다른 피처셋**이라 evaluator.computeHumanValueVP의 **하드코딩 22피처(624 라운드말 스냅샷)와 정렬 불일치**. → predVP 쓰레기값 → K=1·K=0.1 **양쪽 동일 붕괴**(봇 즉시 패스, VP −61). 스케일 아닌 구조적 오류.
- 조치: 원본 39게임 humanValueNet.json 복원(.39bak). humanValueBlendK 측정은 **무효**(잘못된 모델). 선형 value 블렌드 질문은 기존 "중립"(value-net-blend-neutral) 유지, 재논의 안 함.
- 교훈: evaluator의 computeHumanValueVP는 자체 22피처 스펙 — 재학습하려면 **그 스펙과 동일한 피처**로 학습해야 함. Phase 1b MLP 가치망은 이 정렬을 반드시 맞춰 구현.

## 2026-07-07 기각(OFF): policyPrior(MLP 정책망) — winner's curse + prior 무력 확정
- Phase 1a: MLP 정책망(비선형, 검증 top-1 30.9%/top-3 59.9%, 선형 25.6% 대비 +5pt) 학습·통합(policyNetMLP.json, bot.ts policyProbs MLP forward).
- 측정: **40판 VP +6.63(p=0.025 유의!)·확장 전방위↑** → **120판 VP −2.13(SE1.92, p0.27, bWin47%)·행동 무변(총 −0.03).** 40판 유의는 착시(winner's curse 재현 — 스킬 경고대로 소판 p≈0.05는 뒤집힘).
- **판정: 기각(OFF).** ★확정: **정책 prior는 선형이든 비선형(MLP)이든 중립** — MCTS 가치함수가 최종 선택을 지배해 prior(탐색 방향)를 덮음. policyNetMLP.json 코드/모델은 보존(OFF), Phase2 자가대국 정책헤드로 재활용 가능.
- **함의**: 사람 모방 *정책*은 한계. 진짜 레버 = **가치함수(리프 평가)**. + 정책망 데이터 천장(59게임서 top-1 31% 정체) = 모방 자체가 데이터 부족. → Phase 1b(가치망 정렬 재구현) → 안 되면 Phase 2(자가대국으로 가치함수 재학습, 데이터 자가생성).

## 2026-07-07 세션 종합 결론 (밤샘 자율진행 정리) + Phase 2 플랜
**오늘 채택(ON)**: humanRule7C+mineCreditCombo(크레딧기아 25→19%), shipActionBalance(**+1.92, 유일한 뚜렷 VP이득**), gaiaMineFollow(가이아포머 회수, do-no-harm), expansionEngineOpen. **버그수정**: fedWithK bowl3 회수, 어드민 턴롤백 prev스냅샷 유실.
**오늘 기각**: creditIncomeTs, powerBeforeFedBurn(발동 3회), expansionMineDrive(−4.08), humanValueBlendK(무효-피처불일치), policyPrior 선형·MLP(40판+6.63 착시→120판−2.13).

**확정된 벽 구조**:
1. **강제 룰 = 전멸(7+)**. 확장은 강제로 못 늘림(빌드오더 교란). gaiaMineFollow(투자회수형)만 예외.
2. **정책 prior = 중립(선형·비선형 모두)**. MCTS 가치함수가 최종선택 지배 → prior 묻힘.
3. **선형 가치망 = 중립/약함**. 다중공선성.
4. **모방 데이터 천장**: 59게임(4032결정)서 정책 top-1 31% 정체. 데이터 부족이 병목.
→ **결론: quick 모방 win 소진. 봇 평균 ~75 유지(행동은 사람화). 진짜 레버 = 가치함수를 자가생성 데이터로 재학습.**

**Phase 2 플랜(사람과 함께, 신중히)**:
- 가치망 파이프라인이 2개(computeHumanValueVP 인라인22피처 / features.ts ValueNet)라 혼선 — 먼저 **배선 정리·문서화**(어느 net이 어느 consumer인지 1:1 확정) 후 손대기. 무인 금지(오늘 humanValueNet 무효 교훈).
- 자가대국 루프: MCTS로 (state, MCTS방문분포, 최종결과) 생성 → 가치망(+정책헤드) 재학습 → 반복. valuenet-data.jsonl 인프라 활용. 매 iteration 챔피언 대비 head2head 게이팅.
- policyNetMLP.json(top-3 60%)은 Phase2 정책헤드 초기값으로 재활용.

## 2026-07-08 Phase 2 iteration 1: 가치망 재학습 파이프라인 작동 확인 (중립, 이론상 예상)
- 배선정리 후: ①humanValueNet.json(22피처,valueProbe) ②valueNet.json(33피처MLP,trainValueNet/trainHumanValueNet→OUT수정). Phase2=②.
- 기존 valueNet.json(6/8 구 self-play) valueNetBlend w=0.15: **VP −9.44(p0.001) 해로움** → 재학습 필수 확인.
- 구 코퍼스 4.6M 백업 → 현 챔피언 자가대국 120판 신규수집(39,225샘플, VALUE_NET_COLLECT). trainValueNet 재학습: **val MAE 9.83 vs baseline 16.21 = 일반화 성공**(밤샘 human-only 실패와 대조 — 데이터 질/양이 관건이었음).
- 재학습 망 valueNetBlend w=0.15: **VP −2.12(SE4.31, bWin54%, p0.62) = 중립**(−9.44에서 대폭 개선). 행동 total −1.59 약간↓.
- **판정: 파이프라인 작동·안전 확인. iteration 1 중립은 AlphaZero 정상**(현정책 반영≠초월). 개선은 다-iteration 점진. valueNet.json은 재학습본으로 갱신(inert, blend OFF). 구 net은 valueNet.OLD.json 백업.
- **다음 결정**: 다-iteration 그라인드(self-play→재학습→반복, 각 ~2-3h, 점진·불확실)를 며칠 투자 vs 여기서 뱅킹. 배선 안전해져 무인 자동 iteration 가능.

## TODO (보류, 사용자 요청 2026-07-08): 타클론 브레인→3크레딧 후보 추가
- 갭: 봇은 브레인을 3P→1O(오레)·useBrain으로만 씀. `1brain-to-3credit`(브레인→3C) 프리액션은 서버엔 있으나 봇 후보에 없음 → "크레딧 기아 시 브레인 놀리지 말고 3C로" 선택을 안 함.
- 할 일: 크레딧기아(C낮음)+브레인 bowl3 idle일 때 1brain-to-3credit 후보/직접-return 추가(humanRule7C/mineCreditCombo 계열). 플래그 게이트→head2head do-no-harm 측정. Phase2 루프 종료 후 착수.

## 2026-07-08 Phase 2 자동 3-iteration 추세: 중립 수렴 확정 (climb 아님)
- 루프(각 iter: 최신net blend로 self-play 100판 수집→누적코퍼스 재학습→valueNetBlend w0.15 40판 측정):
  - iter1 VP −2.12 / iter2 **+5.44(p0.098)** / iter3 **−0.80(p0.799)**. 코퍼스 39k→144k.
- **판정: 상승추세 아님, ~0 노이즈 진동**(SE~3.2/iter). iter2 +5.44는 winner's curse(policyPrior +6.63→−2.13와 동일 패턴), iter3가 확인. 3점 평균 +0.8=노이즈.
- ★확정: **자가대국 가치망 루프는 do-no-harm/중립 — ≈휴리스틱 수렴, 반복해도 안 오름.** 원인(추정): (a)가치망만 작게 blend(정책+가치 MCTS 통합 아님) (b)~75봇 self-play는 그 수준을 반영할뿐 초월 못함 (c)휴리스틱이 이미 강함 (d)40판 SE~3.2라 ±1-2 실효는 판별불가(340판/군 필요).
- **성과(뱅킹)**: Phase2 파이프라인 검증·안전(배선정리·net일반화 val MAE9.8·해로움→중립). 라이브 봇은 순수 휴리스틱(모든 net 플래그 OFF) 유지=리스크0.
- **진짜 120벽 돌파에 필요한 것(별도 대형 프로젝트)**: 정책+가치망을 MCTS PUCT+리프에 통합(작은 blend 아님) + 피처 확장(점수타일 컨텍스트) + 대규모 다-iteration 컴퓨트. 불확실. 지금은 뱅킹 권장.

## 2026-07-08 AlphaZero POC (kill-gate): 가치망-in-MCTS = RED 확정, 정통 재작성 기각
- 배경: "큰 결심"으로 정통 AZ 검토. 조사결과 현 MCTS는 단일플레이어·non-negamax·비정규화 휴리스틱리프라 정통 AZ=코어 재작성(수주·고위험). 그래서 저위험 POC(기존 MCTS 유지, azValueLeaf/azVisitSelect/azAllNodePrior 플래그, 전부 기본 OFF)로 go/no-go.
- 측정(weightsDiffer=false):
  - 전체조합 8판 **VP −19.38(bWin0%)**.
  - 분해(8판): useValueNet −14.44 / +azValueLeaf −20.31 / **azVisitSelect +1.71(bWin71%)** / azAllNodePrior −1.19.
  - azVisitSelect 40판 확정: **VP +0.35(p0.92)=중립**(8판 고점은 노이즈).
- **판정: RED.** ★가치망이 킬러(−14~−20) — 휴리스틱 리프가 가치망보다 압도적. 정통 재작성해도 동일 가치망 수준이라 같은 벽 → **정통 AZ 재작성 기각(수주·고위험·불확실 정당화 불가).** azVisitSelect(방문선택, 정석)·azAllNodePrior 둘 다 중립 → OFF 파킹(코드 보존).
- **누적 확정**: net-in-MCTS 전 접근(가치 blend/replace·정책 prior 선형/MLP·PUCT·자가대국 iteration·POC)이 **≈휴리스틱 천장**. 이 엔진(강한 휴리스틱+짧은 MCTS+단일플레이어 트리)에서 net은 휴리스틱을 못 넘음.
- **다음 큰 지렛대 = Path B: 휴리스틱 가중치 진화**(aiWeights.json 40+가중치를 자가대국+사람벤치마크로 CMA-ES/진화 튜닝). net 대신 *강한 몸체(휴리스틱)를 직접* 강화. MCTS·net 안 건드림=저위험. 인프라 일부(aiWeights.candidate.json A/B) 존재.

## 2026-07-09 Path B 가중치 진화: 중립 (defaults 국소최적) — 강화 3방향 전부 천장 확정
- 발견: aiWeights.json = defaults와 100% 동일(미튜닝). tuneAi fitness=자가대국avgVP(약함) → 대신 head2head candidate-vs-champion(gold) fitness로 진화.
- 루프: 챔피언(defaults) 고정 + σ0.18 승수변이 후보 6개 각 40판 A/B. 결과: CAND1..5 = −6.75/−1.14/−2.40/−2.24/−3.14, **CAND6 +4.97**(최고).
- CAND6 120판 확정: **VP −0.22(SE1.75, p0.90)=중립**. 40판 +4.97은 winner's curse + 6표본 최댓값 selection-bias였음(회귀 확정).
- **판정: 중립. defaults가 σ변이 국소최적** — 단순 랜덤 진화론 hand-tuned defaults 못 넘음. 챔피언 미변경(채택 없음).
- ★★**강화 3방향(AZ POC RED·Phase2 자가대국 중립·Path B 가중치진화 중립)이 독립적으로 봇 ~75 = 현 아키텍처+측정예산의 실용 천장을 확정.** net·self-play·weight-tuning 다 소진. 추가 강화엔 근본적 대형투자(교대트리 재작성+큰망+피처, 불확실) 필요. 현실적 가치=버그/UX/사람관찰 do-no-harm 교정.

## 2026-07-09 채택(ON): noR1Gaiaformer — R1 가이아포머 조기배치 억제 (사용자 관찰, do-no-harm)
- 사용자: 봇이 R1에 가이아포머 조기 배치. 사람 R1=인프라(TS·내비), 포밍 R2+.
- 측정(40판, weightsDiffer=false): VP +0.97(SE3.13, bWin49%, p0.76)=do-no-harm. 가이아포밍 −0.56(R1 억제 작동)·TS +0.49(사람 R1 패턴 근접)·총행동 +0.40.
- 판정: ✅ 채택(ON) — 음수아님 + 도메인옳음(R1 조기포밍 억제) + 행동 사람화(R1 TS우선) + 사용자관찰. free project(즉시포밍) 예외 유지.

## 2026-07-09 조사종결: 고급기술 "force-take"(분석#1)는 이미 최대(advTileAlways) — 미소비 갭=엔진 벽
- 분석 에이전트 #1: 봇이 초록연방 벌지만 고급타일 전환 ~15%(사람 ~80%), 0.72 green/seat 미소비. "force-take" 권고.
- **재확인 결과(코드+DECISIONS): 이미 구현·채택됨.** ①`select_advanced_tech_tile`은 `pendingTechTileSelection` 있을 때만 유효(자유 메인액션 아님, gameState 8504 검증) → 별도 강제 메인액션 불가. ②실제 선택부 `findTechTileAction`(bot.ts 4180-4204)에 `advTileAlways`(기본 ON, 2026-06-30 채택, 0.03→0.51)가 이미 "청구가능(초록+L4+미보유)이면 무조건 고급 우선". **선택 시점 전환율은 이미 최대.**
- 남은 0.72 미소비 = "선택 문제"가 아니라 **트리거 생성 문제**(L4+ 상태에서 기술타일 획득 이벤트를 안 만듦) = greenForAdvTile 직접계측 기각(2026-06-29 "아카데미 트리거 강제 필요=실패전력")과 동일한 **엔진 벽**. [[action-gap-is-expansion-wall]]과 일관. 재litigate 안 함.

## 2026-07-09 기각(OFF): taklonsBrainCredit — 크레딧기아 시 브레인 1B→3C 직접-return pre-fund
- 사용자 요청("나중에 추가해줘"): 타클론이 브레인을 프리액션으로 3C 변환할지 봇이 판단 못함.
- 구현: 브레인 bowl3 idle + 크레딧<4 + **+3C가 지금은 못하는 광산을 언락**(after&&!before 프로브, humanRule7C/mineCreditCombo 동형)이면 `1brain-to-3credit` 직접-return(프리액션, 메인 유지). tsc통과, 행동검증=실발동 확인(logs: "1B→3C 크레딧기아→광산 언락" 다수).
- 측정(40판, **taklons 강제**paired, weightsDiffer=false): **VP −2.03(SE4.01, bWin43.2% 16:21, p=0.41).** 방향 음수·상방 없음.
- 판정: ❌ 기각(기본 OFF 유지, 기능은 플래그 뒤 dormant=라이브 무영향). **원인**: 브레인은 bowl3에서 다음R 파워 3P로 유지 가치(>3C)라 MCTS의 "브레인 보존"이 대체로 옳음 — 내 mid-turn 강제가 그걸 덮어 손해. "강제 룰이 평가기를 덮으면 진다"(11+ 실패)와 일관. hang 3건은 taklons TURN-REVERT(다른셋 패스→마지막 무한리버트, 룰 발동 0회)·bal_tak pendingShipTechMine=기존, 룰 무관.

## 2026-07-09 기각(revert): nevlasKnowledgeCycle — 네뷸라 지식-사이클(1P→가이아공간+1K) itars레시피 이식 실패
- 분석에이전트 #1: 네뷸라=봇 최약체(56.1), 시그니처 액션 `1power-to-1k-gaiaformer`(PI 프리액션, bowl3 1개→+1K, 토큰은 다음R bowl1 복귀)를 봇이 전 게임 **0회** 사용(사람 5-14/게임). itarsBurnCandidate(+6.85)와 동일 레시피 권고: 재활용 토큰 액션을 MCTS 후보로 + 평가기가 gaiaformerPower 가치화.
- 구현: bot.ts 후보 추가(PI+power3 게이트) + evaluator.ts nevlas gaiaformerPower=power1Value(플래그 격리). 행동검증=MCTS 실제 선택 확인(지식↑, 토큰 재활용).
- 측정(forced nevlas paired, weightsDiffer=false):
  - 1차(power3≥1): 전체 −6.65, nevlas자리 −2.6 → 마지막 토큰까지 강등=과용.
  - 2차(보수 power3≥3+R≤4) 40판: 전체 −2.35, nevlas자리 **+2.4**(유망해 보임).
  - **2차 120판 확정: 전체 −1.91(SE1.95,p=0.054), nevlas자리 A(OFF)68.3 vs B(ON)62.5 = −5.8.** 40판 +2.4는 winner's curse 노이즈.
- 판정: ❌ 기각 + **코드 revert**(bot.ts 후보·evaluator.ts 분기 제거). **원인**: itars는 가이아토큰이 PI로 기술타일교환(큰 VP)이라 이득이었지만, nevlas 토큰은 bowl1 복귀뿐 → 순비용(bowl3 강등+재충전 2회)에 이득은 +1K뿐. 평가기에 억지 가치부여→MCTS 과용→손해. **교훈: "재활용 토큰" 레시피는 그 토큰의 *하방 페이오프*(itars=기술타일)가 있을 때만 이식됨. 페이오프 없는 종족엔 안 통함.** [[action-gap-is-expansion-wall]] 강제=평가기덮음=진다와 일관.

## 2026-07-09 기각(flag OFF, dormant): tokenGainNeedGate — gain-2-tokens 낭비 차단이 오히려 액션갭 악화
- 사용자관찰: 봇이 usable bowl3 파워를 소모(cost 3)해 bowl1 토큰 2개로 바꾸는 gain-2-tokens를 용처 없이 남발("파워 income 미인지→미리 태움"). 원인규명: gain-2-tokens 기본점수 160→findPowerActions[0]→powerActionOverPass가 패스 대신 강제(로그 확인).
- 구현: totalTokens≥7(연방보너스 밖) & 인공물추적 아님이면 gain-2-tokens score=-1 → findPowerActions(score≥0만)·powerActionOverPass 강제서 제외. noR1TokenGain의 전 라운드 일반판.
- 측정(40판, 전종족, weightsDiffer=false): **vpMargin −2.29(SE3.12, 승률53.8%, verdict=노이즈). 행동델타: powerAct 3.04→2.69(gain-2-tokens 감소 확인), 총액션 46.76→44.74 = −2.0/게임.**
- 판정: ❌ 기각(flag OFF, 코드 dormant 유지). **핵심발견**: gain-2-tokens는 파워 비효율이 맞지만, 끊으면 봇이 "패스 대신 머무는" 액션을 잃어 **총 액션 −2/게임**(봇 #1 약점=액션갭 [[action-gap-is-expansion-wall]]). 파워 아끼기 < 액션 유지. 사용자 직관(국소 옳음)을 head2head가 교정한 사례. **재방문 여지**: 더 나은 대체 파워액션이 affordable할 때만 gain-2-tokens 배제(액션 유지+낭비 감소 양립)면 다를 수 있음.

## 2026-07-09 기각(inert, flag OFF dormant): ambasSwap — MCTS가 시그니처 스왑을 0회 선택
- 갭: Ambas 시그니처 `ambas_swap_pi_mine`(PI↔광산 위치교체, 메인·1회용)을 봇이 전 게임 0회 사용(사람 2.12/게임). 강제/점수nudge(실패패턴) 대신 각 광산과의 swap을 **MCTS 후보**로 열어 평가기가 개선 위치일 때만 고르게 함(itarsBurn/advTile 패턴). 사람 소켓 핸들러(ambas_swap_pi_mine)와 동일 로직 확인.
- 측정(forced ambas 40판 paired, weightsDiffer=false): **★[ambas] ON 84.5 vs OFF 84.5 → Δ+0.00 (p=1.000). 스왑 실행 0회.** 게임이 ON/OFF 동일하게 진행 = 후보는 열렸으나 MCTS가 한 번도 선택 안 함.
- 판정: ❌ 기각(inert). **원인**: 스왑은 즉시 페이오프(자원·VP·건물수 불변)가 없고 위치/연방 가치는 평가기가 모델링 못 해 MCTS가 무가치로 판단→미선택. [[action-gap-is-expansion-wall]] "후보로 열어도 평가기가 가치를 못 보면 안 골라진다"(taklonsBrainCredit·nevlasKnowledgeCycle과 동일 벽). Ambas는 이미 84.5(3위권 강종족)라 손해 0.
- 처리: 코드는 **플래그 뒤 dormant**(기본 OFF, 라이브 무영향) 보존 — 사람이 봇 상대로 둘 때/향후 평가기가 위치가치 모델링 시 재활용 가능. challenger.flags {} 복귀.
- ★교훈 재확인: 종족 시그니처 갭은 **즉시·확정 보상**(geodensNewType +3K)일 때만 MCTS가 집음. 위치/셋업형 시그니처는 평가기 벽에 막힘 — 후보 개방만으론 부족.

## 2026-07-09 종족 랭킹 갱신 (챔피언 자가대국 120판, n=480)
- 최약: **nevlas 62.1 · taklons 62.9** · darkanians 68.3 · ivits 69.4 · lantids 69.7 · geodens 70.8 · moweyip 71.2 · bal_tak 74.2 · hadsch 75.6 · itars 77.0 · terran 77.1 · firaks 77.4 · xenos 78.6 · space_giants 79.1 · ambas 79.1 · tinkeroids 82.9 · bescods 83.0 · **gleens 92.9(최강)**.
- vs 옛 랭킹(2026-06-18): lantids 62→69.7·geodens 61→70.8 개선(확장 채택 효과), ambas 81.8→79.1. 최약 여전히 nevlas·taklons(파워엔진=확장/파워벽 종속, 시그니처픽 다 기각).
- 사람 profile 대조(human-faction-profile.mjs, 사람 50게임): 사람 최강 lantids 205.8(순위1.4)·gleens 205.5, 최약 itars 95.8. 봇↔사람 최대 역전 = lantids(사람#1 vs 봇중위)이나 봇 개선돼 완화. nevlas 사람 170(순위1.9)·taklons 153(순위2.3)=사람도 최상은 아님.

## 2026-07-09 기각(OFF): fedPacePush — winner's curse 6번째 (40판 +6.56 → 120판 −0.33)
- 동기: #1 측정갭=연방 페이스(봇 1.29연방/5.48위성 vs 사람 4.17/1.55). 개수직접보상(−3.55 실패)·위성캡(−2.99 실패) 대신 승인 메커니즘(clusterFedBonus=가까운 클러스터 티어업으로 7파워 마감)을 인간페이스(R3:1/R4:2/R5+:3)보다 뒤질 때만 ×1.9 증폭. evaluator term, 미측정이었음.
- 측정(전종족, weightsDiffer=false): **40판 VP +6.56±3.15(p=0.037, 승률62.5%)·총행동 +2.48·전방위 활동↑** → **120판 VP −0.33±1.88(p=0.862, 52.1%)·총행동 +0.05·연방 +0.08(소멸).**
- 판정: 기각(OFF). 40판 큰 양수+광범위 행동변화는 winner's curse 6번째(policyPrior +6.63→−2.13, geodensPiValue +5.47→−1.5, Path B +4.97→−0.22 등과 동일 패턴). 120판에선 연방 델타 +0.08로 근접-inert = ×1.9 증폭이 fed 페이스를 실질 못 바꿈.
- ★근본: 연방 sprawl은 **흩어진 빌드**(확장 패턴)가 뿌리라 클러스터가 애초에 마감권(gap≤3)에 없음 → 평가기로 그 보상을 증폭해도 발동할 클러스터가 없어 무효. [[action-gap-is-expansion-wall]] 재확인. 연방 페이스는 확장(빌드 밀집도) 문제의 하류.

## 2026-07-09 기각(revert): qicToOre — 사람 마이닝 최상위 후보였으나 QIC 옵션가치 과소평가로 손해
- 마이닝(서브에이전트, 사람 actionJournal 64게임): 1위 후보. 광석 고갈+QIC 잉여 시 무료변환 1Q→1O로 임박한 광석-업글 감당. 사람 52/64게임(81%)·325회, 게이팅 qic≥2 90%/ore≤1 79%, 직후 광석빌드 72%. 봇은 1qic-to-1ore 전무.
- 구현: upgradeConvertCombo(광석→크레딧) 미러링, mine→TS·TS→Lab·Lab→Aca 3경로에 순수 광석-병목(credits충분)만 프리액션 1Q→1O. tsc통과.
- 측정(전종족, weightsDiffer=false): **40판 VP +0.59(교역소 +0.52·총행동 +1.48=의도발현)** → **120판 VP −1.78±1.92·승률 40.7%(p=0.043 유의 약화)·교역소 +0.03·총행동 −0.43(행동붕괴).**
- 판정: 기각 + **코드 revert**. **★교훈: "프리액션=공짜"가 아님.** QIC는 사거리점프·연방·기술·QIC액션의 높은 옵션가치를 갖는데 evaluator가 QIC 예비 미래가치를 저평가 → 광석 소량 위해 QIC 과다 변환 → 옵션 상실로 순손해. MCTS가 결과상태 평가해도 evaluator의 QIC-blindness 때문에 과변환을 못 막음. [[value-net-blend-neutral]]·evaluator-wall과 일관.
- ★세션 함의: 사람 81%-빈도 free-conversion do-no-harm 후보조차 evaluator 옵션가치 저평가로 실패 → free-conversion 마이닝 벤(#2 powerToQic도 동일 옵션가치 리스크). 남은 갭은 evaluator-bound 확정.

## 2026-07-10 기각(inert, revert): boosterPassVp — 확정 VP지만 평가기 결정 안 바뀜(중복)
- 평가기 분석(서브에이전트): 평가기가 player.bonusTile 0참조 → 부스터 패스보너스(매R 확정 VP, 예 랩부스터+랩3=9VP) 무시. 동일 메커니즘 고급기술 pass타일(evaluator 965)은 크레딧함=직접 불일치. geodensNewType형 확정보상 저평가로 판단, #1 후보.
- 구현: computeScore에 부스터 passBonus.type별 카운트 × vp × vpWeightLate × 0.5(adv-pass와 동일 스케일, 현재 R 1패스만 확정이라 ×1). tsc통과.
- 측정(전종족 40판, weightsDiffer=false): **VP +0.00±3.04(p=1.000), 승률 47.5%. 행동 미미(연구소 +0.15·가이아광산 +0.20뿐, 총행동 −0.09).**
- 판정: 기각(inert)+revert. 원인: 부스터 VP는 leaf 상태 오프셋이라 형제 수 간 차이가 marginal(빌드 1개=+1카운트)뿐 + 봇 빌드가 이미 부스터와 대충 정렬 → 명시적 크레딧이 결정 불변=중복. 정가로 Δ0이라 인플레는 왜곡위험(안 함). ambasSwap류 inert.
- ★교훈: "확정 VP 저평가"라도 (a)marginal 신호가 작고 (b)기존 휴리스틱이 이미 그 방향이면 평가기 term은 inert. geodensNewType이 통한 건 봇이 *안 하던* 새유형 빌드를 크게 유도(+100 후보점수)했기 때문 — 이미 하는 걸 크레딧하는 건 무효.

## 2026-07-10 기각(revert): endgameResourceVp — R6 자원 3:1 크레딧이 과다 뱅킹 유발(소비 인센티브 훼손)
- 평가기 분석 #2: R6 종료 시 남은 자원(O+C+Q+K) 3당 1 VP 확정(gameState 7040)인데 평가기가 자원을 0.1× hoarding억제로만 봐 ~180~590× 저평가. R6-only로 실제 3:1 크레딧. #1(inert)보다 큰 저평가라 기대.
- 측정(전종족 40판, weightsDiffer=false): **VP −4.58±2.81(p=0.104), 승률 45.0%. 빌드 다운(광산 −0.19·의회/아카 −0.15·기술타일 −0.17·총행동 −0.73).**
- 판정: 기각+revert(−4.58+이해된 메커니즘=120불필요). **원인**: R6 자원을 3:1로 크레딧하니 봇이 자원을 쟁이고 가치있는 R6 빌드를 포기 → 손해. 빌드는 거의 항상 자원을 0.33VP보다 많이 환산(구조물VP+스코어링) → **평가기의 낮은 자원가중치가 사실 옳았음**(소비 유도가 정답). 분석가 경고("소비 인센티브 상충") 적중.
- ★★평가기 실험 방향 결론: 분석가 #1(boosterPassVp=inert, 중복)·#2(endgameResourceVp=−4.58, 소비인센티브 훼손) 둘 다 실패. **손수 확정가치 term은 (a)기존 휴리스틱과 중복(inert)이거나 (b)기존의 옳은 인센티브와 충돌(음수).** = 평가기의 *확정가치* 가격은 이미 좋음. 진짜 결핍은 *투기적 가치*(확장 포지션·옵션가치)인데 그건 학습함수(net) 영역이고 이미 ~75 천장서 실패. [[value-net-blend-neutral]] 재확인.

## 2026-07-10 아키텍처 프로젝트 Phase 0+1: net-leaf는 스케일버그 아닌 "근본적으로 거침" 확정 (NO-GO 싼수정)
- **Phase 0 진단(서브에이전트)**: net-leaf −14~−20 主원인 = net이 형제 수(1-5 VP 차이)를 랭킹하기엔 너무 거침(MAE 9.83). 부차: 스케일 불일치(net~77 vs 휴리스틱~4956, C=√2). 근본: 33피처가 전부 스칼라 카운트 → **스코어링타일 컨텍스트·위치/reach·연방잠재력·타일정체성·템포 전부 누락** → 다른 상황이 동일 벡터로 뭉개짐.
- **Phase 1 프로브(siblingProbeRun.mjs, 오프라인 형제-랭킹, 171결정)**: **Top-1 일치율 30.4%**(GO기준 85%), Top-3 57.3%, **Spearman ρ=0.12·Kendall τ=0.13(≈무상관)**. → **NO-GO 싼수정**: net 랭킹이 휴리스틱과 거의 무상관이라 정규화+C재튜닝으론 못 살림. 스케일버그(a) 기각, 근본거침(c) 확정.
- **결론**: 값망을 MCTS 리프로 쓰는 유일 경로 = **Phase 2 피처 대개편**(스코어링타일·위치/reach·타일정체성·템포·연방잠재력 추가) + 재학습 → 재프로브 게이트(top-1 상승?). ★남은 리스크: 데이터 천장(자가대국 ~75 라벨은 net을 그 수준에 고정; 사람데이터 64게임=0.2%). 피처개편으로 net이 휴리스틱을 *매칭*할 순 있어도 *초월*하려면 AZ 부트스트랩이 실제로 climb해야(과거 self-play iteration 중립). = 대형·불확실.
- 도구: siblingProbeRun.mjs(수집)+siblingProbeReport.mjs(집계) 스킬에 보존 — 피처개편 전후 재프로브로 진전 측정.

## 2026-07-10 아키텍처 Phase 2a: 증류 게이트 = NO-GO (스칼라피처 net-leaf 천장 ~65%, 공간정보 손실)
- Phase 1 정정: 표본확대(171→2594결정)로 raw valueNet top-1 **36.1%**·Spearman 0.12 (NO-GO 재확정).
- Phase 2a: 서브에이전트가 rich 피처(33→**99차원**: 스코어링타일 one-hot·위치/reach·타일정체성·템포·연방잠재력) 추가 + 9357결정 수집(결정별 heur[]·baseF[]·richF[]). JS-MLP 학습은 크래시 → 내가 **오프라인 리프로브**(수집데이터로 학습+형제랭킹 측정, self-play 재실행 불필요) 직접 작성: distillReprobe.mjs(릿지)·mlpReprobe.mjs(MLP).
- **증류 결과(휴리스틱 출력을 타겟 = 가장 쉬운 타겟)**:
  - 릿지 base(33): top-1 45.5%·ρ0.48 / 릿지 rich(99): 50.3%·ρ0.55.
  - MLP rich H64/40ep: **65.5%·ρ0.665** / MLP rich H128/80ep: **65.3%·ρ0.689** (train MSE 0.0095→0.0037 떨어져도 val top-1 정체).
- **판정: NO-GO.** ①**타겟 결정적**: 휴리스틱타겟(ρ0.69) ≫ 최종VP타겟(raw net ρ0.12) — 최종VP 노이즈가 raw net 실패의 큰 원인. ②**피처 절반의 문제**: rich+비선형이 36%→65% 개선하나 ~65% 천장(용량↑ 무효) = 스칼라 99피처 한계. 나머지 35%는 위치/인접/클러스터/맵그래프 정보(휴리스틱이 전체보드서 계산, 스칼라로 표현불가). ③증류 자체는 이득無(휴리스틱 손실복사본).
- **★아키텍처 프로젝트 결론**: 스칼라-피처 net-leaf는 휴리스틱을 매칭도 못함(65% 재현=35% 오랭킹 → drop-in 시 −점, POC −14와 일관). net-leaf가 휴리스틱을 넘으려면 (a)공간/그래프 피처(hex맵 인접·reach·클러스터 위상 = GNN/CNN급, 스칼라 추가로 불가) + (b)~75 초월 학습데이터(자가대국 천장·사람64게임). 남은 경로 = 스칼라피처 확장이 아니라 **공간ML 대개편 + 데이터천장 해결** = 수주~수개월·고불확실. Phase별 kill-gate로 싼/중간 경로 전멸 확정.
- 도구 보존: distillReprobe.mjs·mlpReprobe.mjs·siblingProbe (오프라인 리프로브 = 향후 피처실험 저비용 측정).

## 2026-07-10 아키텍처 Phase 2b: 공간 GNN 게이트 = NO-GO (스칼라보다 못함 + full 학습 CPU 불가)
- 목표: hex맵 공간 인코딩(타일별 유형·구조물·소유·인접그래프) + GNN 가치망이 휴리스틱 증류에서 스칼라 65% 천장을 깨는가.
- 데이터: 6616결정/49,572후보 수집(spatial-probe.jsonl, 타일피처+adjacency+heur라벨). 학습=Python/torch(CPU빌드, GPU GTX1650 있으나 torch가 cuda=False).
- **결과(오프라인 증류 재프로브, train_spatial.py GNN)**:
  - 800결정/8ep: val top-1 **44%**(best@ep1, 과적합), ρ0.44.
  - 3000결정/12ep: val top-1 **46.8%**(best@ep10), ρ0.516, top3 83%. train_mse 0.81→0.055(train은 잘 맞음).
  - 6616 full: **CPU에서 hang**(1시간 무진행, epoch 0) — 비선형 스케일링(대형 그래프 배치 scatter). 학습 불가.
- **판정: NO-GO.** ①공간 GNN이 **스칼라 MLP(65%)보다 오히려 낮음**(47%). 원인: 스칼라 rich피처는 휴리스틱이 쓰는 것(스코어링타일·reach)을 손수 담아 직접적인 반면, GNN은 원시 타일에서 공간패턴을 *학습*해야 해 더 어렵고 CPU·데이터기아로 미달. ②full 데이터 학습이 **CPU torch로 불가**(GPU 필요, 미가용). ③데이터 3배(1000→3000)로 44→47% 미미한 상승 = 데이터 늘려도 65% 도달 요원.
- **★아키텍처 프로젝트 4단계 최종결론**: Phase0(진단)→1(raw net 36%)→2a(스칼라 65% 천장)→2b(공간 GNN 47%, 스칼라보다 못함+CPU불가). **net-leaf가 휴리스틱을 매칭하는 경로를 이 환경에서 못 찾음.** 남은 길 = (a)GPU 인프라(현 torch CPU빌드→CUDA 필요) + (b)대량 전문가 데이터(자가대국 ~75천장·사람 64게임=0.2%) + (c)공간망 대규모 학습. = 별도 대형 인프라+데이터 확보 프로젝트. 현 환경(CPU·64게임)에선 VP 천장돌파 불가 확정.
- 보존: train_spatial.py(GNN+재프로브), distillReprobe.mjs·mlpReprobe.mjs·siblingProbe (향후 GPU/데이터 확보 시 재사용). spatial-probe 데이터는 워크트리(정리 대상).

## 2026-07-10 기각(revert): burnCapSteps — 과다번 차단이 광산/액션 줄여 손해 (액션갭 증상이었음)
- 발견: 실게임 봇 파워번 3.2% vs 사람 0.2%(16배). 확인: 일반종족 봇(tinkeroids 5·space_giants 3회/게임)이 광산 1개 위해 step 파워액션(gain-1-step 3P/gain-2-steps 5P) 감당하려 토큰 2~5개 영구소각(bot.ts 3082/3125 콤보). 고수는 거의 안 함.
- 수정: 번 ≤1로 충분할 때만 콤보 후보 생성(power3<2/<4면 제외), itars/taklons 예외. 플래그 burnCapSteps.
- 측정(전종족 40판, weightsDiffer=false): **VP −2.10±2.86(p0.46), 파워번 −0.21(감소O)·광산 −0.50·광산R1-2 −0.50·총행동 −1.35.**
- 판정: 기각(revert). **원인**: 과다번은 낭비지만 봇이 광산 짓는 *수단*이라, 막으니 광산·액션이 줄어 순손해. [[action-gap-is-expansion-wall]] + tokenGainNeedGate("파워아끼기<액션유지")와 동일. ★통찰: 봇 과다번=액션갭/확장벽의 *증상*(더 나은 확장수단 부재→토큰 태워 광산). 증상 차단은 활동만 줄임. 성공한 do-no-harm(소행성)은 활동을 *redirect*했지 *제거*안 함 — 이게 판별 기준.

## 2026-07-10 기각(revert): shipUseBroad — 우주선 활용 강제가 더 나은 빌드 밀어내 손해 (실전갭≠자가대국갭)
- 발견(실게임): 봇 우주선 사용/입장 1.0 vs 사람 5.6~6.7 (AI Bot2는 5VP 내고 0회). shipActionBalance가 C>=9/C<=5만 커버, 중간대 C6-8 미발동 → 입장한 배 방치.
- 수정: 중간대에서도 income 우주선액션(리벨#2·트왈#2) 직접-return 활용(ADD형). 플래그 shipUseBroad.
- 측정(전종족, weightsDiffer=false): 40판 VP +0.33 [승률60%, 우주선액션 -0.11=미발동] → 120판 VP -3.03 +-1.69 [승률44.4%, p0.073], 우주선액션 +0.37[발동]·광산 -0.15·연구소 -0.12·총행동 -0.22.
- 판정: 기각+revert. 40판 +0.33은 winner's curse[우주선액션 미발동 노이즈]. 120판서 실제 발동하니 우주선 income액션이 더 나은 빌드/업글을 대체 → -3.03. burnCap과 동일 = 강제 활동이 기존 좋은 수 밀어냄.
- ★핵심 통찰: 봇 우주선 저활용은 vs-사람 문제지 자가대국 문제가 아님. 자가대국선 전원 봇이 저활용이라 "안 씀"이 비용이 아니고, 오히려 억지 사용이 자원 쓰며 손해. → 이 부류[vs-사람 실전 낭비]는 self-play head2head로 측정 불가[bot-vs-bot ~75 등가]. do-no-harm 채택[소행성 등]은 도메인 판단이 판관이지 self-play VP가 아님.

## 2026-07-10 기각(OFF): shipEntryGate — 낭비-차단조차 self-play 음수 (vs-사람 갭 재확정)
- 발견: 우주선 입장의 66%가 액션 없이 -5VP 순손실(shipEntryGate 주석). 이미 구현된 gate[R6 입장금지 + 미사용 우주선 있으면 추가입장 금지] 측정.
- 측정(전종족 40판, weightsDiffer=false): VP -3.00 +-2.83[승률35.1%, p0.29], 총행동 -1.36[활동 감소].
- 판정: 기각. force-use[shipUseBroad -3.03]와 방향 반대인 cut-waste인데도 음수. 자가대국선 그 "낭비" 입장/액션도 봇 활동의 일부라 자르면 액션갭 악화[burnCap과 동형].
- ★★세션 결정적 결론: self-play perturbation 全종류 소진 확정 = force-use(-3.03)·cut-waste(-3.00)·cut-burn(-2.10)·fed증폭(-0.33)·변환(-1.78)·inert(boosterPassVp/ambasSwap/fedPacePush 40판허상). 봇=hand-tuned 국소최적[~75]. 실전 비효율(우주선저활용·과다번=66% 낭비)은 vs-사람 문제라 self-play head2head가 원천적으로 못 측정/개선. 자율 self-play 최적화 공간 완전 소진. 남은 진짜 레버 = 데이터확보(SUPABASE 키)→가치망 / vs-사람 평가 / 도메인-판단 실수교정[소행성류, self-play 무관].

## 2026-07-10 실행+정량기각: humanValueNet 재학습 — 64게임은 가치망 학습에 근본 부족(데이터 천장 정량증명)
- goal 문자그대로 "사람 로그로 학습" 실제 실행: human-features.jsonl 10,266샘플(58게임, bot제외, 게임단위 split 누출없음)로 33피처 MLP(valueNet.json, 시스템②) 학습.
- **결과: valMAE epoch0 26.7 → epoch39 33.3(과적합 증가), 상수예측 기준 24.9. 망이 상수보다 나쁨=일반화 실패.** 대조 Phase2 self-play망 valMAE 9.83(기준16.2, 39k~144k샘플). 
- 판정: 블렌드 무의미(상수만도 못함→손해 확실), 측정 생략, valueNet.json 복원. **★데이터 천장 정량증명: 사람 64게임(1만샘플)은 가치망 일반화에 근본 부족(needs ~40k+ = 수백 게임).** 이전 "데이터가 진짜 레버" 결론을 실측 MAE로 확정. self-play망이 통한 건 순전히 데이터量.
- 함의: 자율로 가치망을 개선하려면 **더 많은 사람게임(SUPABASE 키) 필수** — 이건 이제 정량근거 있는 요구지 추정 아님.

## 2026-07-07 qicShipBudget ✅ 채택 ON (40판 무해 확인, 실효판정=1:3)
- 사용자 관찰: 봇이 시작 QIC를 가이아 건설(1Q)·점프에 즉시 소진 → 우주선 입장 QIC가 없어 R1 우주선 혜택(입장·리벨리온 3정큐)을 사람처럼 못 받음.
- 구현: R1-2 빌드 지불가능 QIC = 보유 − 예약(①미입장 우주선 최소 입장거리 QIC(≤2) ②리벨리온 탑승+3정큐 미사용이면 3Q 적립 보호). maxPayQicForMine 중앙게이트라 가이아/점프 전부 적용, 입장·우주선액션은 예약 무관. 자원예약형(미션뱅킹 성공 계열).
- 40판: VP +1.67 승률 51.4%(무해), 가이아광산 2.37→2.33(유지 — 건설이 사라지지 않고 밀리기만), 입장 +0.04(셀프플레이에선 발동조건 드묾). 셀프플레이 계측한계 독트린에 따라 무해+도메인으로 채택, 실효는 사용자 1:3에서 관찰(R1 입장·3정큐 사용 여부).

## 2026-07-07 연방 병목 해부 → 레버 2종 판정
- **해부(2.2만 시드)**: 연방 결정실패 0%(만들 수 있으면 만듦). 병목: 전체 71% LOW_POWER(엔진 크기), fed1 정체군의 45% SCATTERED(흩어짐). LOW_POWER의 83%는 잔여자원도 없음(수입 부족). 배치 실측: 새 광산 직접인접 사람 31% vs 봇 21%, 군집거리 2.12 vs 2.52.
- **clusterFedGradient ❌ 기각 (winner's curse 8번째)**: 평가기 미연방 클러스터 그라디언트. v1 40판: 연방 −0.06(선언 회피 부작용 — 선언하면 클러스터 보상 소멸). v2(연방화파워+클러스터 통합지표) 40판: 연방 +0.10 VP +2.01 승률 63%로 유망 → ★120판: 39.8% p=0.030 유의 음수, 연방 효과도 소멸. **평가기 지오메트리/PI 항 3연속 기각(geodensPiValue·bescodsPiValue·이것) — 평가기 전역 항 추가 축은 셀프플레이 메타에서 채택 불가로 닫음.** 기존 clusterFedBonus(gap≤3 좁은 창)가 이 계열의 최적점.
- **earlyDigResearch ✅ 채택 ON**: 실측 삽 L1 사람 R2.3(25명) vs 봇 R3.8(5명), 가이아 L1 R2.1 vs R3.5(사용자 진단 ②). R2-3·지식4+·실제 대상 있을 때(1-2삽 행성 2+ 또는 트랜스딤 2+)만 해당 연구 직접-return. 40판: VP +1.52, 연구 +0.41, 광산 +0.22(R1-2 +0.11), 가이아광산 −0.23(삽 행성으로 확장 이동, 총광산 +) — 全지표 의도 방향.

## 2026-07-07 R1 비효율 분석 + shipActionBurn ❌ 기각
- **R1 실측(사람 196석 vs 봇 72석)**: 액션수 동일(7.82 vs 8.24)·입장 동수(0.86 vs 0.75)인데 우주선액션 1.30 vs 0.25. 사람은 광산 절반(1.16 vs 2.24)+번으로 할인 우주선액션(TS→랩 2O3P). 코드 원인: 우주선액션 p3>=3 게이트가 현재값만 체크(파워액션 번콤보의 미수정 사본).
- **shipActionBurn 기각**: v1(전면) VP −4.14(행동은 +0.96 대폭). v2(R1-2·엔진업글형·bowl2예비2) −3.27에 행동효과 소멸(−0.10). 셀프플레이에선 번(2토큰→1P3) 비용이 할인이득을 항상 상회. 코드 보존 OFF — 사람 상대 전용 가설로 1:3 실측 대상.
- 신규 레버 기록: ACT 기술타일 픽 선호(보유 사람 0.60 vs 봇 0.15/석, 사용률은 동일 ~3.7회/타일) — 픽 스코어링 수정 대상.

## 2026-07-07 shipEntryOption ✅ 채택 ON (40판)
- 사용자 모델: 우주선은 "지금 못 써도" 들어간다 — ①다음 라운드 수입으로 열리는 액션의 선점 옵션가치 ②입장 순번 충전(2·3번째 +2pw, 4번째 +3pw). 기존 입장가치는 현재 자원 즉시가치만.
- 구현: 입장가치 = max(즉시, 수입반영 다음라운드 가치×0.75) + 충전량×12(기존 고정+20 대체). R6 입장금지 유지.
- 40판: VP +3.33, 입장 +0.10, 우주선액션 +0.23, 총행동 +0.45 — 의도 방향. (승률 42%는 VP와 불일치 = 노이즈)

## 2026-07-11 earlyBurnGuard ❌ 기각 — 번 정책은 현 수준이 셀프플레이 로컬 최적
- 사용자 관찰: R1 번3(토큰6)→3P 1삽은 비효율. 가드(R1-2 번≥2 콤보 차단) 40판: VP −4.92, 파워번 −0.46과 함께 ★R1-2 광산 −0.33 소멸 — 콤보 광산의 컴파운드 가치 > 토큰 6개.
- 번 3연속 판정: 늘리기(shipActionBurn −4.14/−3.27)도 줄이기(earlyBurnGuard −4.92)도 손해 = 현재 번 빈도가 로컬 최적. 코드 보존 OFF.

## 2026-07-11 rebellionBurnQic ✅ 채택 ON / r1PiOpen 🧪
- rebellionBurnQic(사용자 룰: QIC2+번≤2→4P→1Q→3정큐는 번 값을 하는 예외 보상): 40판 VP −0.55(중립·무해), 발동조건 좁아 셀프플레이 신호 없음 — qicShipBudget 패턴(무해+도메인) 채택, 실효=1:3.
- R1 빌드오더 분석(사용자 주도): "연구소 자금 부족" 가설 기각(봇 랩도달 76%>사람 67%, 잔여자원도 적음). 진짜 갭 = ①연구소 도달 속도(사람 3번째 vs 봇 4~6번째) ②R1 의회 오프닝(사람 32건: 스자7·란티다7·네뷸라6·기오덴4·피락스4·할라3 vs 봇 0건 — round===1 절대차단이 earlyPiAllowed 점수를 죽은 코드로).
- r1PiOpen: PI-파워 종족만 광산2+ 조건 R1-2 게이트 개방(순수 후보추가, 점수조작 없음). 40판 검증 대기.

## 2026-07-11 r1PiOpen ✅ 채택 ON (40판 +7.55 p=0.019 → 120판 +2.95 부호 유지)
- 사용자 주도 R1 빌드오더 분석의 결실: 사람 R1 의회 오프닝 32건(스자·란티다·네뷸라·기오덴·피락스·할라) vs 봇 0건 — round===1 절대차단 + R≤2 광산<5 게이트가 earlyPiAllowed 우대(+80)를 죽은 코드로 만들던 것을 PI-파워 종족·광산2+ 한정 개방.
- ★순수 후보 개방(점수조작 없음)이라 세 번 연속 뒤집힌 프로필(40판 유의→120판 음수)에서 유일하게 생존: 120판 VP +2.95, 기생광산 +0.12·가이아광산 +0.17 유지. 조기 PI 실험 3전 4기 — 실패했던 밀어넣기(점수/평가기)와 달리 게이트만 열면 MCTS가 옳은 경우를 골라냄(후보생성 갭 원리 재확인).

## 2026-07-11 acadGateOpen ✅ 채택 ON / 파워배분 진단
- acadGateOpen(아카 갭 172건: R≤3&광산<5 게이트를 광산3+로 개방): 40판 VP +1.88 승률 57.5% 무해 — r1PiOpen 동형 게이트 개방 계열 채택.
- 파워 이코노미 진단(사용자 "파워 관리" 지적): ★총량은 동일(리치VP 8.7=8.6, 파워액션 11.6P vs 13.2P, 프리액션 9.4P vs 8.3P) — 문제는 배분(봇 삽 2.17 vs 사람 0.83 몰빵, 7C 1/4·2K 1/10)과 소싱(4P타일 42% vs 10%). 상류(늦은 삽연구)는 earlyDigResearch로 교정 작동 확인(7-11 실전 3판: R1 삽/가이아 연구 4건 발생).
- 7-11 실전 3판: 봇 평균 77.8(64.4→73.2→77.8 상승 지속), 우주선 입장→동라운드 사용 등장(아이타 R2 Twilight/Rebellion 즉시 사용), bescods 100.

## 2026-07-11 판정 3건 + 디스크 정리
- **asteroidEarly ✅ 채택 ON**: 소행성 R5+ 조건 제거(갭 93건). 40판 +4.21 → 120판 +1.48 부호 생존(게이트 개방 계열 r1PiOpen·acadGateOpen에 이어 3연속 생존 — 이 계열만 120판을 통과함).
- **act4pPick ❌ 기각**: 4P타일 픽 +50 → 40판 −6.17. 보유 42% vs 10% 갭에도 셀프플레이에선 income 타일 대체 손해. (사용자도 "꼭 먹으라는 건 아님" — 파워관리 총량은 사람과 동일함이 실측된 상태.)
- **humanRule2K ❌ 기각**: −2.42 + 연구진행 +0.04(목표행동 미달).
- 디스크: logs/ final_state 7일+ 삭제 7.6→3.9GB (사용자 승인), run-h2h.sh에 자동 정리 상시화.

## 2026-07-11 gaiaResearchUseGate ✅ 채택 / filler1k1cGuard ❌ 기각
- gaiaResearchUseGate(사용자 관찰: R1 가이아 L1 올리고 포머 방치 — 배치파워6·트랜스딤 체크 없이 연구 4K 증발): L0→L1 후보를 '트랜스딤 사거리+2 내 존재 + 총파워 5+'일 때만 생성. 40판 +3.96 비음수 → 채택.
- filler1k1cGuard(1K1C를 더 나은 풀 대안 있을 때만 −60): 40판 −5.76 승률 36.8% → 기각. techTileRankFix(-2.75)에 이어 1K1C 감점 2연속 손해 — 1K1C 픽은 보기보다 봇 운영에 기여(지식+크레딧 동시수입). 이 축 종결.
- 세션 재시작으로 배치 알림 유실됐으나 테스트는 완주 — 고아 워커 12개 정리.

## 2026-07-11 파워토큰 심층 비교(사용자 지속 요청) + humanPowerRace 채택(사람게임 한정)
- 심층 실측(사람 196석 vs 봇 72석, 동일 게임): ①번 봇 1.07 vs 사람 0.51/석(R1-2는 3.5배) ②프리미엄 파워액션 선점 — 사람 7C 0.73/2O 0.80/2K 0.33 vs 봇 0.19/0.58/0.02, 봇은 삽(2.08)·토큰(0.75)만 ③토큰재고 봇이 R6에 4.2 방치(사람 2.8) ④리치·총소비량은 동일(기존 확인).
- ★구조적 발견: 파워액션 = 라운드당 선착순 공유자원인데 셀프플레이에선 봇 전원이 프리미엄을 기권해 선점가치가 0으로 측정됨(humanRule2K -2.42 기각의 진짜 원인). **선점 경쟁은 셀프플레이로 검증 불가능한 축.**
- humanPowerRace(기본 ON): '사람이 있는 게임에서만' 7C(c≤6)/2O(o≤3)/2K(k<4) 미사용+지불가능이면 직접-return 선점. 셀프플레이 = 정적으로 no-op(측정 무오염·h2h 불필요). 검증 = 사용자 1:3에서 프리미엄 점유율 변화.

## 2026-07-11 humanPowerRace ✅ 채택 ON (하한검증 통과 — 사용자 절차 교정 반영)
- 절차: 사용자 지적("검증 먼저")으로 기본 OFF 회귀 → humanPowerRaceForce로 셀프플레이 하한 측정(선점이득 0인 최악세계에서 비용만 측정) 40판: VP +1.08 무해, 총행동 +1.24.
- 논리: 하한 무해 + 실전은 선점이득 순증 → 채택. 실게임 발동은 hasHumanOpp(사람 있는 게임)에서만, Force는 측정 전용.
- 1:3 관찰 지표: 봇 프리미엄 점유율(7C 0.19 → ?, 2K 0.02 → ?).

## 2026-07-11 리벨리온 3정큐 빌드 — rebellionQicPlan ✅ 채택 / r1RebelCommit ❌ 기각
- 사용자: "아무도 3정큐 빌드를 계획 안 해 그것만으로 이김. 사람은 R1부터 계획적으로 만듦. 봇 1개는 시도했으면."
- 룰 확정: 우주선 액션 매 라운드 리셋(7215) = 3정큐는 라운드당 1회(점유자 공유·선착순) 반복 기술타일 엔진.
- **rebellionQicPlan 채택**: 탑승 중 + Q<3 + AI연구(+1~2Q)로 3Q 완성될 때 AI 트랙 직접-return(AI L1~5=+1/+1/+2/+2/+4Q가 연료트랙). 40판 +3.26, 기술타일 +0.24·연구 +0.21 — 엔진 작동.
- **r1RebelCommit 기각**: 러너 1봇 지정+입장 직행. v1(무조건) 콤보 −5.56, v2(무료입장 한정) −1.00 — 강제 입장이 단독 이득(+3.26)을 2회 다 깎음. 탑승은 유기 경로(r1ShipPriority·shipEntryOption)에 맡기고, 탑승하면 rebellionQicPlan이 빌드를 돌리는 구조로 확정. 코드 보존 OFF.

## 2026-07-11 burnComboLate ❌ 기각 — ★번(burn) 축 5연속 음수로 완전 종결
- 지연 설계(R1-2 번2+ 콤보를 내 메인액션 2회 전엔 미생성 → 리치 후 더 싼 번으로 동일 광산): 40판 −3.77인데 행동 완벽(번 −0.49, 광산 +0.27) → 120판 −2.26, ★행동효과 소멸(번 +0.16, 광산 −0.14) = 40판 프로필은 노이즈.
- **번 축 최종 결론(5연속: shipActionBurn v1 −4.14/v2 −3.27, earlyBurnGuard −4.92, burnComboLate −3.77/−2.26)**: 봇의 현재 번 행동(R1 3번 포함)은 셀프플레이 메타에서 낭비가 아니라 정당한 트레이드 — 늘려도·줄여도·미뤄도 손해. 이 축의 손규칙 재시도 금지.
- 사용자 관찰(R1 3번 = 비효율)은 vs사람 환경 가설로 남음 — 제한형이라 humanPowerRace식 하한검증 불가. 유일한 검증로는 사용자 1:3 실측 A/B(원하면 burnComboLate/earlyBurnGuard를 사람게임에서 켜고 관찰). 기본 OFF 보존.

## 2026-07-11 rebelAdjacentQicHold ✅ 채택 ON — 3정큐 라인의 '탑승 전' 구멍 봉인
- 사용자 구체 사례: 발타크 리벨 인접 시작 + Q4 라인(연구소 1O1Q타일+가이아트랙)인데 R1에 가이아 건설로 Q 소진 → 라인 사망. 원인: qicShipBudget 3Q 보호가 탑승 후에만, 탑승 전엔 입장비용(인접=0)만 예약.
- 수정: 미탑승이라도 리벨리온이 사거리 내면 R1-2 지갑 QIC 3까지 보호 → 가이아 1Q 건설 컷 → 연구소/입장 라인 부상 → 탑승 시 rebellionQicPlan이 인계.
- 40판: VP +0.69 무해, 우주선액션 +0.17·입장 +0.06·가이아광산 +0.08(유지). 발동조건 좁음 — 실효는 1:3(리벨 인접 봇의 R1-2 3정큐 발생).

## 2026-07-11 rebelPrepPlus ✅ 채택 ON — 3정큐 빌드 마감 디테일 2종(사용자 라인)
- ①2K→1Q 브리지: 탑승 중 q==2·k≥2·리벨3번 미사용이면 AI연구(4K)보다 리벨3번(2K→1Q+2C)을 먼저 — 같은 라운드에 3번→1번(3정큐) 연결 가능.
- ②엠바스식 Nav 선행 입장: 3Q 보유 + 리벨 사거리 밖 + Nav 1레벨로 닿게 되면 QIC 점프 대신 Nav 연구(무료 입장 + 3Q 보존). navBeforeJump의 입장판.
- 40판: VP +0.25 무해, 우주선액션 +0.24(브리지 작동). 발동조건 좁음 — 실효 1:3.

## 2026-07-11 twilightQicPlan ❌ 기각 (유의 음수 -9.58 p=0.021)
- 가설: 트와일라잇 #1(3Q→연방보상 8~12VP, 라운드 반복)은 리벨 3정큐와 동형 엔진 → 리벨 스택(3Q예약·AI연구 브리지·인접보호) 확장.
- 결과: 기술타일 −0.58·연구 −0.27·우주선액션 −0.26 전부 역행. 추정 원인: 트와일라잇 탑승은 흔해서 '#1 미사용이면 3Q 예약'이 게임 내내 Q를 동결하는데, MCTS가 정작 #1을 안 눌러(가치평가/confirm 체인 미비) 예약 비용만 내고 엔진 수익이 없음 — 리벨(380점, 타일 즉시가치)과 달리 사용률이 뒷받침 안 됨.
- 교훈: 동형 구조라도 '사용률'이 먼저 — 다음에 손대려면 ①3Q 보유 시 트와 #1 실사용률 계측 ②confirm_twilight_federation 보상 선택 품질 점검 후 사용측부터. 코드 보존 OFF.

## 2026-07-12 twilightQicPlan v2 ✅ 채택 ON — 사용자 타이밍 룰이 -9.58을 +0.64로 반전
- v1 기각(-9.58): 조기 3Q 예약이 Q동결. 사용자 룰: "트와 재수령은 R4+ 또는 기술연방 후" → v2: 예약·브리지·후보 전부 타이밍 게이트 + R1-2 인접보호에서 트와 제외 + 조기 후보 자체 차단(3Q 절약 신규 효과).
- 40판: VP +0.64 무해, ★우주선액션 +1.15·기술타일 +0.40·연구 +0.34 — 후반 재수령 엔진 실가동.
- 역할 분담 확립: 초반 3Q = 리벨 타일 엔진, R4+ 3Q = 트와 재수령 VP 엔진 (사용자 플레이 모델 그대로).

## 2026-07-12 twilightQicSave ✅ 채택 ON (사용자 룰: R4-5 트와 대기 중 저가치 2Q 액션 금지)
- R4-5 + 트와 탑승 + 재수령 미사용 + 지출 시 3Q 붕괴면 QIC 파워액션 후보 제외(qic-action-tech는 고가치라 예외, R6 자유). 40판 +1.64, 우주선액션 +0.56·파워액션 −0.15 — QIC가 재수령으로 흐름.

## 2026-07-12 fedTokenRelax ❌ 기각 (연방 +0.04 무효) — 연방 셀프플레이 직접레버 소진 재확정
- 가설(토큰예비 R3-4:3이 SCATTERED_TOK 연방 차단) → 완화(-2)해도 연방 1.44→1.48 무효, 파워액션 -0.69 부작용, VP -1.68. 코드 폐기.
- 병렬세션 기록 합치: fedPacePush도 기각(+6.56→-0.33 wc6), "self-play 섭동 全소진, 봇=국소최적 ~75" — 연방 수 직접 증폭은 두 세션에 걸쳐 소진.
- 남은 경로: ①라운드별 실시간 병목 프로브(종료시점 분류는 과대추정) ②채택된 엔진 스택(삽연구·아카·소행성·우주선)의 하류 연방 효과를 실게임(1:3)에서 추적.

## 2026-07-12 fedSatCapRelax ❌ 기각(연방 무효 1.38→1.36) — ★연방 직접레버 5계열 완전 소진 확정
- 위성상한 6→9·변환 4→6 완화해도 연방 불변 = 상한도 병목 아님. 코드 폐기.
- **최종 결론**: 봇 연방(1.4)이 안 느는 건 규칙 차단이 아니라 '중반에 연결 가능한 비연방 파워 클러스터(재료)가 물리적으로 없어서' — 재료 생성 = 배치·업글의 다턴 좌표조율 = greedy 1-ply 천장(377행 결론과 합치). 소진 계열: 강제형성(-4.14·재료null)/지오메트리보상(wc)/페이스증폭(wc)/토큰예비(무효)/위성상한(무효).
- 목표(최소2/평균3) 스코어보드: scripts/fedTracker.mjs — 실게임 채택스택 하류효과 추적. 7-11 이후 3판 기준선 기록.

## 2026-07-12 fedValueMult ❌ 기각 — 연방 8각도 완결 + winner's curse 신기루 4호
- 연방 코어가치 ×1.5(120→180): 연방 1.38→1.39 무효(40판·120판 동일). VP 40판 +6.26 p=0.064 → 120판 −1.46 (동일 프로필 4연속 신기루: deferSafeBuild형·geodensPiValue·clusterGradient·이것).
- ★연방 직접레버 최종 완결(8각도): 강제형성/브리징/다턴커밋/지오메트리보상/페이스증폭/토큰예비/위성상한/코어가치 — 全 무효·역효과. 연방 수는 셀프플레이 손잡이로 안 움직임이 8중 확인. 유일 경로 = 엔진스택 실게임 하류(1.19→1.56 진행 중, fedTracker.mjs) + 학습 데이터 임계.

## 2026-07-12 fedZoneUpgrade ❌ 기각 (연방 −0.17 역효과) — 연방 직접레버 9각도 최종 완결
- 기록상 "분리 재측정 필요"였던 순수 '업글로 연방 닫기'(+480/+280, 위성예산 제외판): 연방 1.44→1.28, PI건설 −0.14, VP −2.14 — 역효과로 종결. (!simulation 게이팅의 시뮬/실결정 불일치 혹은 업글 위치 왜곡 추정.)
- ★★연방 목표 최종 기록: 9각도(강제형성·브리징·다턴커밋·지오메트리보상·페이스증폭·토큰예비·위성상한·코어가치·업글닫기) 전수 무효/역효과. 연방 수를 움직인 유일한 것 = 엔진 스택의 실게임 하류(1.19→1.56). 이후 이 목표의 작업 단위는 ①fedTracker 실게임 추적 ②사용자 관찰 장면의 국소 수정 ③데이터 임계 후 학습. 셀프플레이 연방 실험 신규 제안 금지(9중 소진).

## 2026-07-12 fedSatCapHuman ✅ 채택 ON (사람게임 한정) — 연방 목표의 실게임 직접 레버
- 위성/변환 상한 완화(변환+2, 위성 6→9)를 사람 있는 게임에서만 발동. 근거: 셀프플레이 무효(-2.59·연방불변)의 원인 = 봇끼리는 리치 빈약으로 토큰이 안 쌓여 상한이 안 물림; 사람 게임은 리치 8.7VP지불 ≈ 토큰 17+ 순환이라 상한이 실제 물림. humanPowerRace와 동일한 측정-무오염 구조(셀프플레이 정적 no-op).
- 판정 = fedTracker.mjs 실게임 연방 추이(기준선 1.56). 후보 확장일 뿐 선택은 MCTS라 하방 위험 제한적.

## 2026-07-12 humanFedCommit ✅ 채택 ON (사람게임 한정) — 사용자 목표가 셀프플레이 VP보다 우선하는 명시적 트레이드
- 사용자 목표 "못해도 2개, 평균 3개"에 따라: 사람 있는 게임에서 R4+에 연방<2, R5+에 연방<3이면 플래너가 찾는 연방을 MCTS보다 우선 형성. 플래너 품질게이트 유지, 셀프플레이 무영향(측정 무오염).
- 근거: 셀프플레이 강제형성 기각(−2~4VP)은 '재료 null' 케이스 + 셀프플레이 메타 기준 — 사용자 명시 우선순위(연방 수)와 리치 풍부한 실게임 환경에선 트레이드 수용. 판정 = fedTracker(기준선 1.56, fed2미만 44%).

## 2026-07-12 humanFedCommit OFF 회귀 (사용자: "강제 형성은 별로")
- 강제 형성은 목표 수치는 채울 수 있어도 연방의 질(재료·타이밍 트레이드)이 나쁨 — 사용자 판단으로 OFF. 유기 레버(fedSatCapHuman·엔진스택)만 유지. 연방 목표는 유기 경로+데이터 임계로.

## 2026-07-12 r1ExpandValve ✅ 채택 ON — 조기가드 교집합 기아 밸브 (사용자 실관찰 HH 케이스)
- 실관찰: HH R1 광산 0채, 랩+2/6TS만 하고 5O20C 쥔 채 패스. 원인 = earlyTerraformGuard(3O삽 차단) × qicShipBudget(가이아 1Q 차단) × 0스텝 확장지 사거리 밖 — 개별 검증된 가드들의 교집합이 광산 후보 전멸.
- 밸브: '0스텝 확장지 없음' 판정 시에만 ①우주선 QIC 예약 해제 ②삽 차단 1000→400 캡. 기아 아니면 가드 전부 유지.
- 40판: VP +3.71, ★R1-2 광산 +0.50(기아 해소 직접 확인), 총행동 +1.26. ★교훈: 하드 게이트를 쌓을 때마다 교집합 기아 케이스 점검 + 밸브를 짝으로.

## 2026-07-12 midTerraformGuard ❌ 기각 (유의 손해 33.3% p=0.037) + 기보복기 1호
- 기보복기(사용자 제안 포맷, 996pef1d xenos 51점): 패인 = ①R1 1수 가이아에 2QIC 전소(→전게임 QIC 기아, 채택스택이 커버) ②빈손 입장 3회 −15VP ③R4 6O 테라포밍+12K nav몰빵+더블입장 ④고급타일 0(초록연방 2개 방치). R3 연방·R5 Proto 시퀀스는 수준급.
- ③의 6O 삽을 midTerraformGuard(R3-4 원삽 3O+ 차단)로 막으려 했으나 33.3% p=0.037 유의 손해 — 번 축과 동일 교훈: '흉한 확장'도 비확장보다 나음. 무딘 확장 차단 클래스 종결(early/mid 모두).
- 잔여 복기 항목: 트랙 밸런스(nav 몰빵), 고급타일 정렬 실패 — 관찰 누적 후 정밀 타깃.
- 2026-07-12 **채택(ON)**: `hhConvertAfterMain`+`qicReserveAllRounds`+`hhConvertMinimal` (HH 변환 정책 v2, 실게임 ofhfvztt 복기 3갭) — 2종 40판 VP+0.55 우주선+0.59▲ / 3종 랜덤 40판 승률55.3% VP−0.50(노이즈) / 3종 HH강제 40판 승률59.0% **VP+5.08**(p=0.151) 우주선+0.97▲, 챌린저HH 79.9 vs 챔피언HH 79.2(+0.7 무해). 행동확인: 변환 묶음 ×6~8→×2~3 축소. 셀프플레이=구조적 최악(갭이 사람게임에서 발생)이라 무해면 채택. HH변환 지표 0.00은 로그 ×N중첩(538b8dc)으로 액션명이 'Free Actions'로 바뀐 탓 — 대칭이라 비교 유효, 지표는 추후 detail 기반으로 수선 필요.
- 2026-07-12 **채택(ON)**: `rebelReachQicHold` — 사용자 관찰(스페이스자이언트): 리벨 3거리+'+3사거리' 부스터 보유인데 2Q를 가이아 광산에 유출해 3정큐 라인 사망. ③rebelAdjacentQicHold의 도달 판정에 보너스타일 range_3 미사용분(+3) 포함. 40판 승률44.7% VP+2.51(p=0.366) 우주선액션+0.79▲ 가이아광산+0.36▲(기아 없음). 무해+정방향+도메인 정합.
- 2026-07-12 **채택(ON)**: `tfBonusCombo` — 사용자 룰: 1TF 보너스타일 놀리면서 1QIC(사거리)-모행성 짓지 말 것, QIC 낼 거면 1스텝 행성에 1TF 쓰고 지어라(모행성은 0스텝이라 보존 이득). 부스터+건설 콤보 후보 신설(3P→1삽 콤보와 동일 패턴, 베이스 340: QIC 지불 시 TF콤보>모행성, 0QIC 사거리 내 모행성 근소 우위). 40판 VP−0.01±3.56(완전 중립·무해) — 사용자 명시 룰이라 채택.
- 2026-07-12 **채택(ON)**: `gaiaResearchPlaceSync` — 사용자 관찰: R1 가이아 연구 후 포머 방치 패스(실측 60판: R1 연구 41건 중 37% 미활용). 3중 수정: ①연구를 배치 가능 라운드(R2+)로 동기화 ②미배치 포머 시 배치예산(토큰 6/4/3) 밑으로 파먹는 정리변환 금지 ③주 공급원이던 expansionEngineOpen 직접-return이 gaiaResearchUseGate 우회하던 것 봉합(못 쓰면 테라 폴백). 40판 승률55% VP+0.81, 행동: R1 가이아 연구 45→11(챌린저≈0), 연구진행 +0.19▲.
- 2026-07-12 **채택(ON)**: `hhJitConvert` — 사용자 룰: HH는 미리 변환하지 말고 쓰기 직전에(크레딧 유동성 보존, 그 사이 상황 변화 대응). 발타크 GF→QIC 패턴 미러링: 가상 지갑(지갑+크레딧 변환가능분)으로 후보 생성 + 부족분만 4C→1QIC preActions(광산/가이아/우주선 3Q), 사전 변환 루프(pre-main/post-main) 비활성. HH강제 40판 승률60% VP+0.34, HH석 82.5 vs 82.7 동률(무해). hhConvertAfterMain은 JIT ON 시 자동 무력화(상위 대체).
- 2026-07-12 **채택(ON)**: `balTakNavTileGate` — 사용자 룰: 발타크는 PI 없으면 Nav 전진 불가(서버가 전진만 버리고 타일은 줌) → Nav 트랙 밑 타일 = 전진 증발 = 순손해. 예외: 7VP/유형당1K를 R5+에만 허용. 서버의 '첫 매칭 트랙 슬롯 소모' 규칙 미러링. 발타크강제 40판 VP+4.40(전체), 발타크석 77.7 vs 71.3(+6.4) — 종족 직격 개선.
- 2026-07-12 **채택(ON)**: `lantidsParasiticPI` — 사용자 관찰: 의회 후 기생 0회. 갭 ①의회 +2K/기생(서버 실지급)이 점수 미반영(고정 260 → PI 시 +100) ②거리 앵커에 기생광산 제외(서버는 포함 — 룰 미러링). 란티다강제 40판: 란티다석 64.3 vs 58.6(+5.7), 기생광산 +0.20▲. 전체 VP −3.01은 비-란티다 좌석 노이즈(코드 동일). 기각된 lantidsParasiticPush(밀집 강제 −1.83)와 별개 각도.
- 2026-07-12 **채택(ON)**: `advTileValueFloor` — 사용자 관찰: 외각 0개인데 '외각×2VP' 고급타일 획득. advTileAlways가 점수 무관 강제라 조건부 가치 0짜리도 선택됨 → 한계가치(베이스45 초과분) <12면 안 집음(초록토큰+표준타일 커버 비용 반영). 40판 VP+0.23, 고급타일 획득 0.35→0.15(가치낮은 획득 필터, VP 무손실 = 걸러진 건 기여 없었음). 주의: 추후 "고급 안 먹네" 재관찰 시 임계 12→8 완화 여지.
- 2026-07-12 **기각(OFF 유지)**: `ts26BanEarly` — 사용자 실험 질문: R1-2 비할인(2O/6C) TS 전면금지(첫 TS 예외)하면 점수가 어떻게 되나. 40판 승률51.3% VP−1.27±2.95(방향 음수·노이즈). 행동은 작동(TS R1-2 2.13→1.91, 교역소 −0.53)했으나 아낀 자원이 광산(±0)·연구소(+0.09)로 전환 안 됨 = 6C TS가 차선 중 최선이던 판 존재. TS 억제 계열 7번째 무효/음수 — 이 계열은 하드필터 정밀판도 무효 확인, 재시도 비권장. 플래그는 보존(실게임 검증 원하면 켤 수 있음).
- 2026-07-12 **채택(ON)**: `earlyWasteBan` — 사용자 가설 적중: R1-2 낭비 3종(비할인 6C TS[첫TS 예외]·번3+[리벨 번2 체인 예외, 아이타 제외]·+2토큰 액션[인공물 추적 예외])을 '묶음'으로 금지해야 자원이 가치로 전환. 단독 6C TS 금지는 VP−1.27(누수: 다른 낭비로 샘)이었으나 번들 40판 VP+0.61, 광산(R1-2)+0.14▲ 연구소+0.13▲ 파워번−0.19▼ TS(R1-2)−0.17▼ — 막힌 자원이 광산·연구소로 흐름 확인. TS억제 단독실패 7건의 원인 규명(개별 금지=누수 이동)이기도 함.
- 2026-07-12 **채택(ON, 사람게임 한정)**: `creditCapGuard` — 사용자 관찰: 연구소1+TS3 크레딧 포화인데 TS 증설+7C 액션 → 상한(30) 오버플로우로 수입 증발. 임계는 사람 15,250결정 캘리브레이션(평균 8.1C, p95=19, ≥20C=4% 비정상 꼬리): ①현금+다음R수입 ≥21이면 비-첫 TS −260 ②7C 결과 20C+면 −200, 상한30 초과 예약이면 후보 제외. 셀프플레이: v1(임계26) 무발동 중립(−0.65) / v2(21) 40판 +6.06 p=0.027 → **120판 −1.67 완전회귀(winner's curse 9호 — p<0.03도 40판이면 회귀)**. 실게임 문제는 실재(사용자 관측) → hasHumanOpp 게이트(fedSatCapHuman 패턴), 120판이 셀프플레이 하한 검증.
- 2026-07-12 **기각(OFF 보존)**: `advPassGrowth` — 실게임 4판 분해: 사람 기술타일 VP 몸통 = 고급 패스타일 성장분(2vp-asteroid 52·3vp-fed 42·3vp-lab 33, 전부 취득 후 성장). 봇 평가는 현재 개수만 계산 → 성장 투영 추가. 40판 승률65% VP+6.47(p=0.086) → **120판 45.8% VP−1.47 완전회귀(curse 10호), 고급타일 획득 효과도 소멸(0.13 vs 0.14)**. 원인: 셀프플레이 봇은 자격(초록연방+L4) 자체가 드물어 평가 개선이 무의미 — 진짜 상류 = 연방(초록토큰)·L4 도달·타일 획득량(사람 9장 vs 봇 4장). 평가는 도메인 옳음(사람 검증)이라 상류가 풀리면 재검 가치. 연방 5VP 버그수정(1eb014a)이 이 세션의 진짜 점수 수확.
- 2026-07-12 **채택(ON)**: `rebelEntryFareGuard` — 사용자 관찰: 3Q(3정큐 스택)를 원거리 리벨리온 입장비로 전소(거리 계산은 서버와 일치 — 문제는 QIC예약이 건설만 막고 입장비는 안 막던 구멍). 입장비 2Q+ & 입장 후 잔여 <3Q면 3Q엔진 배(#1 미사용 리벨/타이밍 트와) 입장 후보 제외(1Q 입장은 2K→1Q 브리지 복구 가능해 허용). 40판 승률55% VP+2.67, 입장 총량 동일(2.34)에 Nav+1 +0.19▲ = 낭비 입장만 유효 입장으로 전환.
- 2026-07-12 **채택(ON)**: `artifactVpFloor` — 사용자 룰: 순수 VP형 인공물(가이아×3·과학×3·유형 등)은 9VP+ 노리거나 토큰이 정말 남아돌 때(구매 후 10+)만 — 3VP 타이밍에 6토큰(위성 재료) 전소하는 집착 차단. 기존엔 3VP도 점수 86>임계 80으로 통과 + 광석→토큰 변환까지 하며 구매. 자원/수익형 인공물은 엔진 가치라 제외. 40판 승률55% VP+2.40.
- 2026-07-12 **채택(ON)**: `cleanupIncomeAware` — 사용자 관찰: 4/0/1 + 충전수입 2인데 1P→1C 후 패스(3/2/0 시작 — 2/2/1이 우위). 원인 = 패스 전 '최소 운영자금' 변환(크레딧<2 → 1P→1C, 광석<1 → 3P→1O)이 다음 라운드 수입을 무시. 수입 합산 후에도 바닥일 때만 변환하게 수정. 40판 VP+0.59(무해, 케이스 희소), 조건 엄밀화라 도메인 정합.
- 2026-07-12 **기각(OFF 보존, 2판 연속 유의손해)**: `lateResearchMerit` — 사용자 관찰(R5-6 경계 미도달 경제/지식 상승 = 낭비, 실측 R6 저메리트 64건/80판)로 후반 저메리트 연구 차단 시도. v1(R6 일괄차단) 승률32.5% **VP−4.11(p=0.027 유의손해)** / v2(사다리 완주자금 판정 정밀화) **VP−5.99(p=0.077)**. 원인: '저메리트'로 보인 후반 상승의 상당수가 **연구 라운드미션(rs6 +2VP/상승)·adv-vp-research(+2VP/상승) 수입이 걸린 정당한 플레이** — 잔여 4K(1.33VP)보다 나음. MCTS가 이미 이 가치를 옳게 계산 중이었고 일괄 룰이 그걸 파괴. ★교훈: "낭비로 보이는 행동"을 차단하기 전에 그 행동이 실제로 낭비인 판의 비율부터 측정할 것(미션/타일 부수입 확인) — 사용자 관찰 케이스는 실재하나 드문 예외였음.
- 2026-07-13 **채택(즉시커밋, 룰버그)**: 유령라운드 수정(0df46f3) — PASS-TRACE 계측으로 확정: 유령 라운드(봇이 결정 없이 라운드 통째 패스, 24판 중 12건) = missionBankResearch가 유일 후보군(연구)을 숨겨 후보 0 → no-valid-action 강제 패스(로그가 game.id 없이 증발해 은폐). 밸브(후보 0이면 뱅킹 해제) + 로그 가시화. **검증 24판: 유령 12→1, 연방 평균 1.44→1.65**. fed0 저점게임 주범 제거.
- 2026-07-13 **기각(OFF 보존)**: `rebelEntryBridge` — 사용자 관찰(리벨 옆 2K+2Q 미탑승)로 입장 가치에 온보드 브리지 포함(2Q+2K→유효3Q=380). 40판 승률56.4% VP−1.01, 행동 역방향(입장 −0.19, Nav+1 −0.26▼) — 다수 봇이 공유 슬롯(리벨#1 라운드 1회)에 동시 몰려 경합 낭비 추정(contention blindness의 역방향 사례: 셀프플레이는 경합을 재현해서 오히려 과잉 경쟁). 실게임 재관찰 시 사람게임 한정(hasHumanOpp) 재검 여지.
- 2026-07-13 **채택(ON)**: `fedFreshTsFirst` — 사용자 관찰: 새 할인(2/3) TS 자원이 있는데 연방 내 TS를 4/6(의회)로 먼저 올림(미래 연방 파워 0 기여). 연방 밖 할인 TS 가능 시 연방 내 업글 −200 추가 지연. 40판 VP+2.55, 교역소 +0.39▲/의회 −0.06(의도 그대로: 새 재료 먼저, 안쪽 업글 다음 턴).
- 2026-07-13 **채택(ON)**: `powerAcceptBudget` — 저점 실측(피락스 31점 파워차감 −13 vs 사람 215점 −8): 수락 모델의 파워당 0.8VP 가정이 전환 못 하는 봇에겐 과대 — 누적 지불 ≥8VP(사람 상한)면 추가 수락 가치 40% 할인. 120판 승률54.3% VP−0.27(중립·무해), 도메인 정합(사람 캘리브레이션).
- 2026-07-13 **[야간 루프 종합]**: 유령라운드 4단 수정(뱅킹밸브→재시도→진단→**MCTS null반환 폴백 우회 봉합**이 근본) — 강제패스 18→3/40판, <40점 4→1~2/160, 평균 75.4→78.6. 부수 채택: fedFreshTsFirst(+2.55). 기각: rebelEntryBridge(경합 역방향), lateResearchMerit(미션 부수입 무시). 남은 유령(~5/40판)은 'MCTS가 후보 있는데 패스 선택' 가치판단 케이스 — 버그 아님, 별도 트랙.
- 2026-07-13 **채택(즉시커밋)**: 유령라운드 v5 최종 그물(2550a6e) — 결정 null + 후보 존재 시 비-패스 후보[0] 그리디 실행(패스=라운드 증발보다 항상 우위). 검증 60판: **폴백 19회 발동**(증발 라운드 19개 구조), 유령 17→8, <40점 2/240(0.8%), 평균 78.6. 잔존 유령 8 = MCTS의 정당한 패스 선택(가치판단) — lateResearchMerit 교훈상 강제 비패스는 역효과라 여기서 종결. getNextMove 체인 내 null 원인은 [PASS-TRACE]/진단로그 유지로 실게임서 계속 관측.
- 2026-07-13 **기각(둘 다, but 핵심 학습신호)**: `azValueLeaf`(−5.90 p=0.056)·`azValueLeaf+humanValueBlendK5`(−2.09) — 다턴 언락 시도(미시도 셀: 롤아웃 제거 × 사람넷 v2 리프). ①롤아웃(6스텝 그리디)엔 정적평가가 못 담는 템포/자원 신호 실재(제거 시 −5.9) ②사람 가치망은 리프에서 **+3.8 회복** = 넷이 리프에서 실제 작동하는 첫 정량 증거(이전 가산블렌드 −9.64와 대조 — 통합 지점이 맞았음). 결론: 다턴 언락 = 넷이 롤아웃 신호(템포/자원 피처)까지 커버해야 → 사람 게임 71→100판 + 자원/템포 피처 확장 후 재시도. 트리거 도달 시 이 셀부터.
- 2026-07-13 **채택(ON)**: `lastRoundFedFree` — 사용자 관찰: R6 종료 시 2O3C+3O5C 보유(업글 체인→7VP 타일 = 잔여 대비 순+3)인데 패스. 원인 = 연방 보존 스택 3종(fedPenalty −450·noFedTierUp 필터·fedDeferPenalty −200)이 라운드 인지 없이 R6에도 작동 — '미래 연방 재료 보존' 근거가 소멸한 라운드에 페널티만 남아 업글 후보를 죽임(특히 연방 안 mine→TS는 후보 완전 제거 = 체인 첫 계단 붕괴). R6엔 전부 해제. 40판 VP+2.73.
- 2026-07-13 **버그수정(즉시커밋, 주말 코드리뷰 발견)**: 종료 파워 자동환산(1607ea9)이 롤아웃(`scoreTerminalStateForRollout`)·강제종료에만 적용되고 **실게임 종료 경로(전원 패스)만 구식 합산** — bot.ts R6 정리변환 스킵이 자동환산을 전제하므로 봇이 실게임에서만 판당 1~3VP 손실(h2h는 대칭이라 미검출) + 롤아웃/실정산 불일치. 실경로도 `endgameLeftoverUnits`로 통일. 스모크 4판 검증: itars 잔여 5유닛+bowl2 8토큰 → 정산 3VP(구식 1VP) 실확인. 동반수정: `cancelBotExecution`(어드민 롤백)이 botLoopGen 세대를 안 올려 좀비 finally가 새 루프 락을 지울 수 있던 구멍 + since/selfCheck 타이머 정리.
- 2026-07-13 **채택(ON, v2)**: `finalMissionRankAware` — 사용자 관찰: 외곽미션 0개·상대 4/5/6으로 4위 확정인데 R6에 1개 만들러 감. 원인 = 정산은 순위제(18/12/6·0개 제외)인데 후보 고정가점(+25류)과 평가기 ownVal 그라디언트(×2.5)가 추월 가능성을 안 봄. 수정 = "낙관 지평(남은 라운드×2 진행)으로도 내 미션 VP 불변이면" 그 미션의 빌드 보너스·그라디언트 0 (projVp>0 방어가치는 유지, 공유 getFinalMissionVpProjected로 동률 분배까지 정확 판정, 후보측은 gameLog 길이 키 메모). v1(R4+) 40판 −2.16(빌드 위축, 미전환) → **v2(R5+ 한정) 40판 VP+2.54 승률 55%, 광산+0.63·TS+0.31·연구소+0.29(추격 자원이 실빌드로 전환)** — twilightQicPlan식 타이밍 조이기 반전 2호. 가중치 격리 확인(동일).
- 2026-07-13 **채택(ON, 룰 미러링)**: `ivitsFedCumulative` — 사용자 관찰(e28xbh42): 이비츠 건물 21파워+정거장 6개인데 연방 1개로 종료. 원인 = 서버 정본(computeFederationPreview)은 누적 요구파워(7×n)를 '기존 연방 헥스 시드 포함 전체'로 판정하는데, 플래너는 연방 밖 건물만으로 전액(14/21)을 요구(이중 청구) + BFS가 기존 연방 칸을 통로로 못 씀 → 2번째 토큰 후보 자체가 미생성. 수정 = 요구치를 차액(요구−기존연방파워)으로 보정 + 연방 칸을 무비용 커넥터로. 이비츠강제 40판: ★연방 OFF 1.15 → ON 2.50(전판 2+, 최대 4) — 목표 행동 직격. VP 이비츠석 Δ−1.30(p 0.83 노이즈·명확한 손해 아님), 룰 정합 + 연방 목표(평균 3) + 초록토큰 상류라 채택 (lantidsParasiticPI 계열).
- 2026-07-13 **룰버그 수정(사용자 확정, 즉시커밋)**: 아이타 의회 4토큰 교환에서 우주선 기술타일 선택 불가 — 연구소/아카데미/리벨리온 획득은 전부 `availableShipTechTileIds`를 채우는데 itars_pi_exchange만 누락(UI 미표시 + 검증 거부). 수정 3곳: ①소켓 경로 ②봇 경로(executeBotItarsGaiaformerExchange)에 필드 세팅 ③executeSelectTechTile 우주선 분기가 조기 반환하며 아이타 잔여토큰 체인을 건너뛰던 동결 위험 봉합(체인 복제 + helperProceed). 아이타강제 16판 스모크: hang 0, 교환 9회 중 우주선 타일 획득 2회(ship-tech-2tf-mine 포함) 정상 진행 — [ITARS-CHAIN ship-tech] 로그로 체인 완주 확인.
- 2026-07-13 **채택(ON)**: `geodensDigCycle` — 사용자 모델: 기오덴 = "삽/거리 연구→빠른 확장→광석→K(+3K 새유형)→다시 삽/거리" 순환인데 봇은 K 확보가 늦고 얻어도 경제로 샘. 실측(기오덴강제 20판): 상위7 삽+거리 7.4 vs 하위7 4.9, 경제/과학 무차이 — 가설 적중. earlyDigResearch의 기오덴 연장: PI 보유·R2-4·새유형 1-2삽 대상 2+ 남아있으면 삽 L3까지, 거리는 새유형이 실제 새로 열릴 때 L4까지 직접-return(+3K 리베이트 = 연구 4K의 75% 회수 = 즉시·확정 보상 클래스). 기오덴강제 40판: 기오덴석 Δ+1.95, ★광산 5.55→6.85(+1.3)·유형 4.90→5.35·삽+거리 7.70→7.90 — 全지표 의도 방향, 15게임 발동. (참고: 강제 20판 재측정으로 기오덴 실갭은 −6.4, 앞선 63.5는 n=6 노이즈 과장.)
- 2026-07-13 **기각(OFF 보존)**: `lantidsParasiticAdj` — 사용자 관찰(기생 너무 안 함, PI후 +2K 1.78회/판): 기생 후보만 인접 보너스(내건물+50/상대+20) 미적용이던 것을 패리티 적용. 란티다강제 40판: 란티다석 Δ−4.10, ★행동 무변화(기생 2.85 vs 2.90, +2K 1.05 vs 1.20) — 점수 마진을 올려도 기생 횟수 불변 = 병목이 후보 점수가 아님(MCTS 평가기 결정 or 기회/타이밍 구조). 어제 lantidsParasiticPI(+100·앵커)가 이 축의 한계였던 것으로 판단. 다음 각도는 점수가 아니라 ①기생 가능 시점에 실제 뭘 골랐는지 결정 계측 ②평가기의 기생광산 가치(수입 미러링) 점검.
- 2026-07-13 **채택(ON)**: `lantidsParasiticEval` — 사용자 가설 적중("기생 능력이 좋은데 전혀 계산 못하는 것 아니냐"): 평가기에 parasitic 등장 0회 — 서버는 기생광산을 광산 수입(getEffectiveMineCount)·연방 파워·미션에 정식 산입하는데 평가기 광산 항은 ownerId 필터라 기생 자산가치 0 → 시뮬에서 '기생 지은 미래'가 무가치로 평가돼 후보점수를 올려도(어제 +100, 오늘 패리티 기각 — 행동 무변화 2.85/2.90) 안 골랐음. mineCount에 기생 산입(수입 스트림 미러링). 란티다강제 40판: ★기생 3.65→5.00(+37%, 점수축 2연속 무변화와 대조) 란티다석 Δ+4.30. ★교훈: '능력을 안 쓴다' 계열은 후보→점수→**평가기 자산 인식** 순으로 파야 — 후보/점수가 무변화면 평가기가 그 자산을 세는지 먼저 확인.
- 2026-07-13 **도구: 리프로브(scripts/reprobeCandidates.ts) + 캘리브레이션** — 사용자 방향("사람 로그 76판 체계 분석"): 저널 결정시점 상태를 재구성해 오늘 코드 getCandidateMoves를 재실행, 라이브 캡처(당시 코드)와 교차 검증. 판정: ①advance_research 위양성 2% = 신뢰 — **연구 갭은 사실상 닫힘(5.8%)**, 과거 채택이 갭 58건 닫은 것도 실측 ②upgrade_ts 23%·parasitic 25% = 중신뢰 ③build_mine 58%·academy 56%·PI 78%·asteroid 83% = 타일급 재구성 부족(라운드미션·부스터·포머·위성 미복원 → top-N 왜곡), 이 클래스는 리프로브 단독 판정 금지 — '라이브 갭 ∧ 리프로브 갭'(neither) 교집합만 사용: build_mine 308·asteroid 118·PI 38·academy 38건. ★최대 확정 갭 = 소행성 광산(라이브 단독으로도 96% 후보 미생성, 7/11 이후 게임서도 42건) — 다음 타깃. 슬림 저장(게임설정 필드 부재) 대응: 파워액션 표준셋 합성, gameState 훅 var화(TDZ), 출력은 전용 파일(data/reprobe-report.txt).
- 2026-07-13 **채택(ON, ★120판 유의 +4.28 p=0.026)**: `asteroidCandOpen` — 리프로브 1호 수확. 사람 소행성 갭 118건의 근본 원인 3중: ①소행성 점수 분기가 홈 분기(tile.type===homeType) 안에만 있어 비-홈 종족은 steps<=0 continue로 전멸(asteroidAnyFaction 7/11 개방이 필터만 열고 점수 분기 부재 — 3480행 비홈 페널티는 도달불가 죽은 코드) ②자원기아(1O2C 미달) 시 조기 반환이 소행성까지 죽임 — 서버 룰은 소행성 = 포머+거리QIC만, 1O2C 무청구(룰 불일치) ③탑4 컷. 수정: 비-홈 소행성 점수 분기(포머 기회비용 반영) + 기아 시 소행성 전용 패스 + 예약 슬롯. 리프로브 회귀: 커버 4→72(갭 96.8%→41.9%). 120판: VP +4.28±1.92 p=0.026 승률 58.5%(p=0.066), 광산 +0.94·R1-2 +0.38·가이아광산 −0.53(포머 전환 의도대로). ★리프로브 파이프(사람 갭 랭킹→원인 분류→수정→회귀→h2h)의 첫 완주 사례.
- 2026-07-13 **채택(ON)**: `upgradeOreConvert` — 리프로브 2호 수확. 아카/PI '자원 부족' 갭의 실체 규명: 서버 버그도 스냅샷 오류도 아니고, ★사람은 아카의 19%(43/225, 저널 델타 실측)를 3P→1O·1Q→1O 변환으로 광석을 채워 완납 — 저널 playerBefore가 턴시작 스냅샷이라 '저지불'로 보였던 것. 봇은 지갑 광석만 봐서 후보 자체가 없었음. 수정: PI(4O6C)/아카(6O6C) 광석 갭 ≤2를 파워(3P→1O, 타클론 제외)·여유 QIC(1Q→1O, 예비1 보존) preActions로 충당. 40판: VP +2.94±2.78, PI건설 +0.10▲ 의회/아카 +0.04▲ — 무해+도메인+행동 정방향. ★교훈: 저널 playerBefore는 '결정 직전'이 아니라 '턴 시작' — 자원 기반 갭 분류 시 프리액션 변환 여력을 합산해야 함(리프로브 문서화).
- 2026-07-13 **채택(ON)**: `piGateOpen` — 리프로브 3호. 라이브 PI 갭 40건 재검증: R1 11건은 r1PiOpen이 이미 닫음(리프로브 재구성 노이즈 = 시작광산 리플레이 누락 — playerBefore 턴시작 이슈와 함께 문서화), 진짜 잔여 = R2-3 비허용 종족(bescods3·xenos3·itars2·ambas·terran·darkanians)이 광산 4-10개 기반 PI — round<4 종족 게이트 차단. 광산4+&R2+ 개방(r1PiOpen·acadGateOpen 동형). 40판: VP +0.89 무해, PI평균R 4.31→3.84(조기화 ✓), PI총량 −0.13(노이즈 혼조 — 유의 시그널 아님). 무해+도메인+게이트개방 계열로 채택, 실효는 1:3 관찰(비허용 종족 R2-3 PI 발생 여부).
- 2026-07-13 **조기 재시도(사용자 요청, 미달 기록)**: 가치망 v3 — features.ts 33→40피처(다음R 수입 5종 getNextRoundIncomePreview + 전환가능파워 + 총토큰 = 롤아웃 신호 흡수 목적), 77판 16,388샘플 재구성·재학습. 결과: ①기본 valMAE 33.9 > 상수 24.3 ②MINVP=120 필터도 28~30 > 25.8, 프로브 3/7 유지(가이아행성 −171 등 소데이터 과적합 징후). ★viability 기준(엔진축 4/7+) 미달로 h2h 미실행 — 100판 트리거가 옳았음을 재확인, 병목은 피처가 아니라 판수. valueNet.json은 40피처판으로 교체(소비자 전부 기본 OFF라 무영향, 33피처 백업 valueNet.33f.bak.json). 100판 도달 시 이 파이프(재구성→학습→프로브 4/7 게이트→azValueLeaf+useValueNet h2h) 그대로 재실행.
- 2026-07-13 **릿지 가치망(사용자 통찰 적중 + 리프대체 참패)**: 사용자 "5판이면 배운다/좌석 세면 209석" → 실측 209석·16,388샘플. 릿지(λ=50, trainRidgeValueNet.ts, MLP JSON에 선형 정확 임베딩): ★valMAE 23.0 < 상수 24.0 — 가치망 최초로 기준선 통과, 엔진축 4/7+ 양수(기술타일+18.7·광산+7.0·가이아연구+6.6, 병리 없음). MLP 실패(33.9)의 원인 = 데이터가 아니라 모델 클래스(과적합) 확정. 그러나 azValueLeaf+useValueNet(리프 대체) 40판 ★−33.61 p=0.000 참패 — 회귀 MAE ≠ 리프 순서 해상도(휴리스틱 리프 −5.9 << 릿지 리프 −33.6 = 대체 불가). ★교훈: viability 프로브(그라디언트 부호)는 필요조건일 뿐, 리프 투입은 '형제 후보 순서' 충실도가 관건 — 다음 셀 = 블렌드(휴리스틱 순서 유지 + 망 가산).
- 2026-07-13 **기각(가치망 블렌드 셀) + 근본 원인 특정**: `valueNetBlend`(릿지, 0.5 혼합) 40판 −33.74 p=0.000 — 리프 대체(−33.61)와 동형 참패. 행동 시그니처(광산 −2.2, 자원 미지출)가 원인 특정: 릿지 가중치의 자원 항(ore/credits/score) 양수 → "지출하면 예측VP 하락" = ★자원 보유 편향. '부자 중간상태 → 높은 최종VP'는 교란(강한 엔진→부자)인데 인과로 학습 — 상태가치 회귀의 고전 함정(MLP 트레이너는 score만 마스킹, 자원 미마스킹 동일 문제). 다음 재시도 요건: ①자원·score 피처 마스킹(엔진 구성만으로 예측) 또는 ②이득(advantage)/행동조건부 라벨 ③블렌드는 sibling-차별 신호가 검증된 뒤에만. 릿지 회귀 게이트 통과(23.0<24.0)는 유효한 진전 — 도구·파이프 보존.
- 2026-07-13 **기각(마스킹 셀) + ★리프 대체 축 종결**: 자원·점수 마스킹 릿지(엔진 전용, 지출편향 제거 확인 — 자원 가중치 정확히 0) 40판 −34.29 승률 0%. 비마스킹(−33.6)과 동급 = 편향이 주범이 아니었음. 행동(기술타일 −2.86, 광산 −2.2)이 최종 원인 확정: ★features.ts 요약 피처는 위치맹 — 'A타일 광산 vs B타일 광산'이 동일 벡터라 형제 후보 구분 정보가 원리적으로 0, MCTS 리프에 필요한 건 위치 해상도(인접·연방 잠재력·미션 정렬 = 휴리스틱이 하던 것). ★결론: 전역 요약 피처 기반 리프 '대체'는 모델/마스킹/판수 무관 불가 — 이 축 종결. 생존 경로: ①휴리스틱 위에 소가산 넛지(+3.8 전례) ②정책망(후보 순서 — 리프로브 데이터, top-1 29.2%>24.2% 이미 신호) ③보드 인코딩(육각 그래프) 가치망 = 별도 프로젝트. 오늘 밤 시리즈 순수확: 회귀 기준선 통과 도구(릿지) + 교란/위치맹 2대 함정의 실측 지도.
- 2026-07-13 **기각(행동 무변화)**: `gaiaBaseQicCost` — 가이아 기본 QIC가 점수에 0원인 회계 구멍은 실재하나, 28→45로 올려도 2Q+가이아 지표 불변(+0.04/+0.05, 2연속) + 가이아광산 오히려 증가. 결론: 잔존 2QIC 가이아는 미션/교두보가 정당화하는 케이스로 MCTS가 점수컷을 시뮬 가치로 무시 — 진짜 낭비는 기존 QIC 예약/maxPay 게이트가 이미 차단 중. 후보점수 레버로는 못 줄임.
- 2026-07-13 **보류(OFF, 사용자 판단 위임)**: `balTakAsteroidR6Only` — 사용자 룰 "발타크는 R6 전 포머파괴 소행성 절대 금지(포머=매라운드 QIC 수입)". 발타크강제 40판: 발타크석 84.0 vs 87.2(**−3.2, 룰 켠 쪽이 손해**) — 셀프플레이에선 R6 전 소행성도 순가치인 판 존재(봇이 포머 QIC 스트림을 사람만큼 못 굴리는 한계 가능). 룰-데이터 충돌 케이스: 코드 보존, 실게임 관찰로 재판정 권장.
- 2026-07-13 **채택(ON, 사용자 확정 — 셀프플레이 반대에도)**: `balTakAsteroidR6Only` — 보류 철회. 사용자 산수로 지배 확정: 포머 보존 = 매라 1QIC 수입 vs 소행성 광산 = 매라 1Ore 수입, QIC>Ore(가치+전략 프리미엄)라 R6 전 파괴는 항상 손해. 발타크석 −3.2는 봇이 QIC 스트림을 못 굴리는 측정 한계(humanResearchPrior 전례: 도메인 산수 > 셀프플레이). 실게임서 발타크 점수 악화 시 재보고.
- 2026-07-13 **채택(ON)**: `chainReachDefer` — 사용자 관찰(엠바스): Nav2+가이아→가이아 체인이면 2채를 0Q로 짓는데 3거리 가이아2에 2QIC 선지불. 1-ply가 '새 광산=새 앵커'를 못 봄 → 0Q 건설 가능한 Z가 있고 Z→T가 (지식4+면 Nav-up 가정) 사거리 내면 T로의 QIC점프 유보. 40판 VP+1.71(무해), 논리적 지배(같은 목적지를 0Q로 도달 가능하면 유보가 항상 우위). 2Q+가이아 지표는 희소해 40판 노이즈 — 실게임 관찰 대상.
- 2026-07-13 **채택(ON)**: `balTakGaiaFirst` — 사용자 룰: 발타크는 가이아 트랙 항상 우선(포머=매라 QIC), 예외는 아카 자금 2O 부족 시 테라 L1뿐. v1 발타크강제 40판: **발타크석 87.2 vs 80.2(+7.0)** (전체 −2.63은 비발타크 노이즈 — 종족 게이트 플래그 판정 전례). v2(사용자 정교화: L4까지·R4까지·아카 예외)로 확장 적용, v2 확인 런 예정.
- 2026-07-13 **기각 2건**: `firstTs6cBan`(VP−3.70 — 첫 TS 6C 예외는 인접 없는 봇의 유일한 엔진 경로, 사람 95% 할인 데이터는 '사람은 배치가 좋아 예외가 불필요'란 뜻이지 예외 제거 근거가 아님) / `bigMissionBigFirst`(VP−4.65, 의회/아카 +0.04 무발동 — 좁은 조건 + 강제 타이밍 역효과). balTakGaiaFirst v2는 확인 유지(발타크 평균 86).
- 2026-07-14 **채택(ON)**: `researchYieldBuild` — 반사실 복기 1회전(122결정, 스냅샷 32)의 최대 진짜-후회 패턴: 연구 직접-return(earlyDigResearch·expansionEngineOpen)이 강한 건설을 선점(의회 +36VP·포머 배치 +27VP 실측). 의회/아카 가능·성숙 포머·포머 배치 가능 시 연구 양보. 40판 승률57.5% VP−0.60(중립), **가이아포밍 +0.41(+48%)** — 반사실 지목 전환 그대로 발생. ★반사실 하네스(counterfactual.ts)의 첫 채택 성과.
- 2026-07-14 **기각**: `fedWhenOffered` — 반사실 2회전 연방 후회(10회/17.7VP) 기반 재검. 40판 VP−5.83(p=0.095) + **연방 +0.01(증가 없음)** = buildOrderPlanner(−4.14, 연방 0증가) 실패 형상 재확인. ★방법론 교훈: 반사실이 '라운드 첫 수'만 비교해 실제 봇이 같은 라운드 나중 턴에 한 행동(연방 등)을 누락 후회로 오인 — 순서 차이 ≠ 누락. 하네스에 '라운드 내 전체 행동' 대조 필요. 연방 셀프플레이 실험 금지 조항 재확정(반사실 근거로도 뚫리지 않음 — 10번째 무효).
- 2026-07-14 **[반사실 복기 파이프라인 3회전 종합]** — 하네스(counterfactual.ts) 구축·정련 완료: ①라운드 스냅샷(AI_ROUND_SNAPSHOTS) ②대안 강제 터미널 재생 ③실제수 대조 ④순서차이/누락 분리(fedWhenOffered 교훈) ⑤N회 반복 검증(CF_SAMPLES). 성과: `researchYieldBuild` 채택(가이아포밍 +48%). 교훈 2건: (a)단일 재생 후회는 ±10 이상 부풀려짐 — N=3 검증 필수(35→14 수축 실측) (b)'첫 수' 비교는 라운드 내 순서차이를 누락으로 오인. N=3 생존 안정 케이스는 소수(geodens R2 연구강제 vs 비할인 강한 TS +31) — 직접-return의 양보 목록을 케이스 1개로 과적합하지 않고 보류. 다음 확장: 사람 게임 스냅샷으로 사람 수를 기준선 삼는 버전(사람로그 참조 강화).
- 2026-07-14 **무죄 확정: `asteroidCandOpen` 행 혐의 기각 (가짜 행 = 동시 h2h CPU 경합)** — 머지 스모크 6/16 타임아웃의 bisect(OFF→0/16)로 범인 지목했었으나 교란 확인: 트랜스크립트 타임스탬프 재구성 결과 그 스모크(08:03 시작)가 r1fix 검증 런(08:09 종료)과 ~6분 겹쳐 12워커가 24코어 경합 → 타임아웃 6건 전부 겹침 구간 시작 게임(워커 첫 게임 5 + w6 초기), HANG 덤프도 pending 없이 R5/R6 정상 진행 중(= 8분 컷에 걸린 느린 게임). 겹침 종료 후 게임 10판 전부 90초대 완주. 플래그 ON 단독 재실행 0/16 (원래 p=0.375였다면 확률 0.05%). 트랩 계측(8개 핫함수 호출카운터)도 무발화 — 코드 루프 없음. ★교훈 2건: (a) h2h 겹쳐 돌리면 8분 타임아웃이 행으로 오탐 — bisect 전에 반드시 단독 실행 재현부터 (b) bisect는 '그 사이 다른 변경/환경이 없었는가'를 먼저 검증. 배포 차단 해제.
- 2026-07-14 **채택(ON, 무해+도메인)**: `lateTilePref` — 사용자 관찰: 봇이 R6에 big-4str(큰건물 4파워)를 1O1Q보다 먼저 집기 시작. 원인 = 선호가 아니라 ★R5+에서 big-4str·act-4p·1o-1q 전부 0점 동점 → 목록 순서(엄격 >)가 픽 결정하던 사고, lastRoundFedFree(7/13)가 R6 업글 체인→타일 획득을 부활시키며 노출 증가. 수정: R5+ 서열 부여(1O1Q +20 즉시자원 > act-4p +10 마지막 1회 사용 > big-4str 0 유지 — 연방 파워 잠재는 보존). 40판 +7.16 p=0.031 → ★120판 −0.70±2.02 완전회귀(**winner's curse 11호** — R6 정크타일 서열이 +7VP일 리 없다는 사전 직관대로). 중립+도메인 자명이라 채택.
- 2026-07-14 **채택(ON, 무해+도메인)**: `bonusPassZeroMalus` — 사용자 관찰(재론): 봇이 큰건물 0개인데 4pw+큰건물 부스터를 2C+1Q보다 먼저 집음. 원인 = bonusTileHumanW(7/5)의 파워 ×2.5가 4pw 부스터를 정적 10점 > 2c-1q 7점으로 만든 과교정(사람 분포는 12%/13% 균형 = 상황 따라) + 주말 채택들(아카/PI 증가)이 노출 확대. 수정: 패스VP 대상 0개면 −4(큰건물 1개부터 +4VP로 자연 복귀; 0랩 1k-lab 등 일괄 교정). 40판 VP −0.57±3.45 중립 — 산술 확정 메커니즘 + 사람 분포 방향 채택. ※교훈: 앞서 이 관찰을 기술타일로 오독해 lateTilePref를 먼저 만들었음(그것대로 무해 채택) — 관찰 재확인 습관.
- 2026-07-14 **채택(ON)**: `ivitsFedPowerFix` — 사용자 관찰: 이비츠가 연방 전에 3pw 정리 — 이비츠 위성은 QIC 지불이라 토큰이 안 소모되는데, 연방 전 bowl3 회수 2종(fedSpendBowl3 idle 배출·bowl3CashoutOre 소모분 회수)이 타 종족 전제(spent−p1−p2)로 전 bowl3를 idle 오판 → 액션 연료를 1P→1C 헐값 배출. 연방 경로만 면제(인공물 6토큰·포머 배치는 이비츠도 실소모라 유지, 1704 게이트는 기존 면제 확인). 이비츠강제 40판: 이비츠석 Δ+2.45, 전체 −0.50 노이즈 — 근거 부재 제거 + 정방향 채택.
- 2026-07-14 **기각(OFF 보존)**: `terraDynamicScore` — 사용자 관찰(Terra2/Nav2인데 계속 Nav만): 원인은 점수 비대칭(Nav만 '새로 닿는 행성 ×15' 동적 가점, Terra는 정적 (6-lvl)×12 — 삽 절감이 여는 행성 가치가 점수에 부재)로 진단 정확. Nav 대칭 이식(사거리+2 내 1-3스텝 행성 × 절감광석 × 6, 캡90) 40판: VP −2.01, ★행동 역방향(광산 −0.46·가이아광산 −0.43) — terra 연구가 건설 액션을 대체(기회비용 > 절감). 셀프플레이 메타에선 '지금 짓기 > 싸게 만들고 나중에 짓기'(midTerraformGuard·번축과 동일 교훈: 확장 관련 손규칙은 직접 건설 우선). 비대칭 자체는 실재 — vs사람 재검 여지(1:3 A/B) + 가점 축소(×3, 캡45) 재시도 여지 기록. 코드 보존 OFF.
- 2026-07-14 **채택(ON, 무해+도메인)**: `endgameTrackVp` — 사용자 관찰: R6 3정큐 트랙 선택이 Terra/Eco L2(→L3 +4VP 확정) 대신 과학 L0→L1(가치 0)을 고름. 원인 = 과학 기본점수 (6-lvl)×22가 라운드 무관 고정(R6에 수입 0판인데 132점)이라 종료임계 가중 60(4VP×15)을 이김. 수정: R6 한정 VP_UNIT 15→33(L2→3 = 132+ > 과학 132) — 3정큐 트랙 선택과 R6 일반 4K 연구(L3 완주 +4VP > 잔여 1.33VP) 동시 교정. 40판 VP −0.20 중립, 연구진행 +0.21▲ R5+L3 +0.05▲. ※lateResearchMerit(차단식, −4.11 기각)과 대조 — 차단이 아니라 서열 교정이라 부작용 없음.
- 2026-07-14 **기각(OFF 보존, 유의 음수)**: `firaksEngineRush` — 사람 빌드오더 실측(파이락 11석): 성공 공식 = R1-2 랩+의회 → 다운 5-6회 → 186-220점 (미완성 33-73점, 올인형). 봇 갭 = 랩 R1.5인데 PI R3.7·다운 0.94. 랩→PI 직접-return 시도 40판: 전체 −6.40 p=0.032·파이락석 −6.90, ★목표 행동 역행(PI R2이내 1/20 vs OFF 3/20) — 직접-return은 후보 존재가 전제인데 진짜 병목은 자금(4O6C 후보 미생성). 사람은 R1 TS→랩→PI 3연속(9O14C)을 변환·수입 배분으로 조달. 랩 강제만 발동해 순서 꼬임(r1RebelCommit 계열 강제커밋 실패 4호). ★다음 각도 = 자금 계획: 파이락 R1-2에 PI 목적 자원 뱅킹(변환 억제·크레딧 보존) 또는 upgradeOreConvert 갭 상한 확대(≤2→≤3) — 후보를 먼저 만들어야 직접-return이든 MCTS든 잡음.
- 2026-07-14 **기각(OFF 보존) + ★파이락 조기PI 축 종결**: `firaksPiFunding` v2(자금 게이트 개방 — PI 광석갭 변환상한 2→4, 후보만 추가·선택 MCTS) 40판: 파이락석 Δ−10.80(p=0.14). v1 직접-return −6.90과 합쳐 2연속 큰 음수 — 셀프플레이 메타에선 조기 PI 자금(파워 12/QIC 소진)이 확장·엔진을 잠식해 사람 공식(R1-2 랩+PI→다운 5-6회→200점)이 재현 안 됨. 사람 성공은 리치 경제(사람게임 환경) 전제일 가능성 — 재검토는 hasHumanOpp 한정(humanPowerRace 패턴)으로만. 다운그레이드 엔진 자체(firaksDowngrade)는 유지.
- 2026-07-14 **버그수정(사용자 발견)**: 프리액션 언두 로그 잔류 — undo_free_action이 상태는 스냅샷으로 되돌리면서 gameLog는 라이브를 그대로 재부착("표시 차이 허용" 처리)해, 리셋 반복 시 'Used Tech Action(4파워)' 등 되돌린 액션 로그가 3중 누적. 스냅샷에 이미 있던 gameLogSeq로 꼬리 트림(reset_turn과 동일 방식) — 상태·로그 일치.
- 2026-07-14 **채택(ON)**: `firaksEcoPlan` v3 — 사용자 재반박 적중("0파워여도 R2 의회 가능 — R1 랩+경제 2칸, 담라 수입으로 의회"): 변환 조달(v1 직접-return −6.9, v2 갭확대 −10.8 기각)이 아니라 ★수입 조달이 정답. 파이락+의회 미보유+R≤2+Eco<L2면 경제 연구 +80 — 같은 점수 함수라 랩 무료타일 트랙 선택에도 적용(Eco L2 = +1O2C2P/R → R2 의회 자금). 파이락강제 40판: 파이락석 +0.85 무해, ★행동 전부 정방향(PI R2이내 0→3/20, PI평균 R5.25→4.45, 다운 0.95→1.40(+47%), Eco 2.60→3.25). 사람 수준(PI R1.7·다운 5.6)까진 거리 有 — 다음 단계 = 테라 라인 분기(랩 타일이 테라 트랙 위면 테라 우선, 사용자 제안 '타일 위치 보고 선택').
- 2026-07-14 **채택(ON, 사람게임 한정)**: `taklonsBrainHuman` — 사용자 재제기("브레인 활용 못함, 프리액션 후보 부재"): 사람 비교 실측(각 6석)이 7/2 셀프플레이 5차 기각("브레인 아껴라")을 반증 — ★사람 B+T 번 6.2/석·VP 153 vs 봇 0.2/석·VP 58. 원인 = 셀프플레이 충전 기근에선 지출 브레인이 재충전 안 돼 순환 경제 측정 불가(humanPowerRace 동형 사각지대), 사람 게임은 리치 풍부(8.7VP 지불)라 성립. 기존 부품 재활용: ①taklonsBrainBurn(bowl2→3 번 후보) ②taklonsSpendIdleBrain(턴종료 idle 지출 — 5차에서 브레인놀림 1.80→0.00 훅 검증됨)를 hasHumanOpp 게이트로 묶고, R6 지출 제외(종료정산 브레인=3C > 1O). 셀프플레이 no-op(측정 무오염) — 판정 = 사용자 1:3 실측(지표: 타클론 B+T/석 0.2→?, 브레인놀림, 타클론 VP).
- 2026-07-14 **전환(사람게임 한정 ON)**: `shipEntryFloor` — 사용자 목표("못해도 2척, 왠만하면 3척") + 실게임 실측(7/12~ 1척 고정 봇 급증: 12석 중 4석, 원인 = 사람이 공유 우주선 액션 선점 → 봇 첫 배 '미사용' 고착 → 적재방지 게이트 연쇄 차단 + rebelEntryFareGuard 중첩). 수정 = 2척 미만 시 적재방지 게이트 면제 + R4+ 요금가드 면제. 셀프플레이 40판 +6.89 p=0.034 → ★120판 −1.36·승률 39.3% p=0.021 유의 음수(★winner's curse 12호 — 유의→유의 반전 최초). 셀프플레이(입장 2.3·병목 부재)에선 억지 2척이 낭비, 병목은 사람게임 전용 → hasHumanOpp 게이트로 전환(humanPowerRace 동형, 측정 무오염). 판정 = 1:3 실측(1척 고정 봇 비율 4/12→?).
- 2026-07-14 **타클론 브레인 최종 정리(사용자 방향 교정 → 6차 실측 → 수렴)**: 사용자 "사람게임 게이팅은 잘못된 방향" → 올바른 형태(번+사용 커플링, 7/2 기록의 미시도 해법) `taklonsBrainCombo`를 전 게임으로 구현·검증: 타클론강제 40판 ★타클론석 −9.00, 파워액션 −0.58(번 +0.19 발동에도 — bowl2 토큰 소모가 충전 기근에서 역효과). ★6차 전부 셀프플레이 음수 = 이 축은 셀프플레이 검증 원리적 불가 확정. 수렴: 커플링 콤보를 사람게임 한정(hasHumanOpp)으로 채택 + 어제 taklonsBrainHuman 조잡 번들(비커플링 번·맹목 1O 지출) 철회 — 사람게임엔 잘 형성된 커플링만 남음. 판정 = 1:3 실측(B+T/석 0.2→?, 타클론 VP). ※셀프플레이 랭킹으로는 타클론 브레인 수정 검증 불가(사용자 인지 필요).
- 2026-07-14 **기각(OFF 보존) + 수정지점 재규명**: `twilightVpLate` — 사용자 관찰(12VP 보상을 너무 일찍 가져감)을 트와 재수령 점수에 적용(순수VP형 R4까지 ×18→×8) → 40판 −6.13 승률 30.6% p=0.020 유의 음수. ★원인 = 수정 지점 오인: 재수령(3Q 지불)은 12VP가 최고 효율이라 할인하면 저가치 자원 재수령(3Q에 2O 등)으로 유도돼 손해. 사용자 관찰의 실체는 아마 ★연방 '형성 시' 보상 선택(planner getRewardScore)이 12VP를 조기 선택하는 것 — 다음 검증: 사람 vs 봇의 12VP 보상 선택 라운드 분포 실측 후 형성 쪽만 수정.
- 2026-07-14 **채택(ON, 전 게임) ★타클론 7차 만의 첫 양수**: `taklonsPowerFirst` — 사용자 처방("순환 예측이 안 되면 일단 파워액션부터라도": 브레인+1PW 두고 교역소 짓고 담턴 파워액션하는 순서 손해 관찰). 브레인 bowl3 + 가치 파워액션(점수≥0) 존재 + R≤5 + 연방후보 없음이면 파워액션 선실행 — 자기 경제 순서-중립 + 공유 슬롯 선점만 이득이라 셀프플레이도 성립(지출 강제 6차 음수와 구조가 다름). 타클론강제 40판: ★타클론석 +6.85, 파워액션 +0.52▲, 전체 +3.16 승률 64%. ★교훈: 이 축의 실패는 '지출'이 아니라 '순서'가 레버였음 — 지출 강제(6차)는 자산을 버리는 것, 순서 교정은 공짜.
- 2026-07-14 **채택(ON, 사용자 처방 — 검증 불요 낭비 룰)**: `rangeBonusFarOnly` — "3거리 쓰고 바로 옆 1nav에 건설 = 낭비". 활성화 게이트(rangeBoosterUnlocksTarget)는 있었으나 활성 후 건설 선택에 근거리 타일이 남아 MCTS가 근거리를 고르면 서버가 부스터를 무조건 소모. 후보 단계에서 '부스터 OFF 후보군에 없는 타깃'(=부스터가 실제로 여는 곳)만 남김. 먼 타깃이 소멸한 경우엔 기존 폴백 유지(턴 안 막힘). 빌드/포머/입장 모두 커버.
- 2026-07-14 **채택(ON): `rebel3qLadder` v2** — R1 QIC 분석(사람 125석 vs 봇 192석)의 후속: 사람은 3Q타일 R1부터 매라운드 구매(55/52/49/41/41/40), 봇은 R4-5 몰림·R1 0회. 병목 = 조립 사다리(기존 브리지 q==2·K≥4 한정). v1(엔진 모드 q<3·K≥5 포함): 발동 88회 중 74% 미완성 변환 → 2K 드레인, 광산 −0.75·VP −0.82 → 축소. v2(완성 브리지만: q2·K≥2 / q1·K≥2·번4P): 40판 승률 50%·VP +1.19(무해), ★행동 전부 발생 — 기술타일 +0.35·고급 +0.13·Nav+1 +0.50·우주선액션 +0.44·구매 +28%/게임·조기화(R1 0→3), 광산 하락 반감(−0.36)·R1-2 광산 +0.05. 교훈: 사람 엔진 이식은 '이번 라운드 완성 경로'가 보일 때만 — 무조건부 자원변환 엔진은 확장을 밀어냄(bundle-ban과 동계열: 자원은 남으면 딴 데로 샘).
- 2026-07-14 **채택(사람게임 한정)**: `oppAdjacencyValue` — 상대 인접 건설 가치 상향(광산/TS 20→45, 기타 10→22; 미래 TS할인 예약 + 리치 스트림 + 모방넷 dOpp −0.85 근거). 40판 승률 52.5% / VP −1.69±3.70 = 방향 상충 노이즈 → 사전 등록("리치 가치는 셀프플레이 측정 불가, do-no-harm + 실게임 판정")대로 creditCapGuard 패턴 사람게임 게이트로 채택. 셀프플레이 지표 불변, 실게임 리치 수신/봇점수로 추적.
- 2026-07-14 **채택(ON): `fedAbsorbBuildPenalty`** — 사용자 관찰(다카니안이 PI 트리거 +1K+2C 노리고 연방 포함/인접 칸에 막 지음)의 2탄. 업글 fedPenalty 450 대비 건설엔 감점이 없던 비대칭 교정: noBuildAdjFed 필터(대안 있을 때만) 무력 시 흡수 건설에 감점(내부 −300/인접 −150, R6 해제). 40판 승률 50% / VP +4.08±3.50(p=0.245) + 행동 정합(광산 −0.13, 우주선액션 +0.80·Nav+1 +0.46로 자원 전환, 연방 +0.03 무손상). 1탄(noBuildAdjFed dist0 보강)은 7eec220에서 무검증 룰 채택.
- 2026-07-14 **채택(ON): `firaksPiFunding`** — 파이락 PI 자금 v2(v1 firaksEngineRush 직접-return −6.40 기각 후 사전 등록 각도). R≤2+랩 보유 시 PI 광석 갭 변환(3P→1O/1Q→1O) 상한 2→4로 후보만 존재시키고 선택은 MCTS. 파이락 고정좌석 paired 40판: **파이락석 ON 79.7 vs OFF 76.0 = +3.65**(전체 +0.24 중립 — 비파이락 노이즈 상쇄, 종족 게이트 판정 전례), 행동 정합: PI 평균 R3.7→R3.22, R2이내 3%→12.5%(혼합 기준). 다음 단 = firaksMineHold(사람 R1 광산 0.29/석 실측, 사용자 처방 "광산 적당히") A/B.
- 2026-07-15 **채택(ON): `firaksMineHold`** — 사용자 처방("연구소+의회 자금 최적화, 광산 적당히") + 사람 실측(R1 광산 0.29/석, 6/7이 0개 — 자금 전부 TS→랩→PI). PI 전 R≤2 광산 후보 일괄 −260(금지 아님). firaksPiFunding(ON) 위 paired 40판: **파이락석 ON 87.8 vs OFF 83.8 = +3.90**(파이락 라인 연속 +3.65/+3.90 일관), 광산 −0.46(의도), R2이내 PI 12.5%→22.5%(원래 3%, 사람 86%). 경제연구 2칸 강제는 사람 신호 약해(1/7) 미구현 — R2이내 갭이 남으면 다음 각도(크레딧 조달 or 경제 2칸).
- 2026-07-15 **기각(반증, 코드 되돌림): firaksMineHold v2 (QIC 점프 near-ban)** — 가설: R1-2 QIC 점프 광산 66/115가 파이락 PI 자금 증발의 주범 → QIC 점프만 −700 near-ban. paired 40판 firaks석 ON(v2) 85.6 vs OFF 87.5 = **−1.9**(v1 flat −260의 +3.90 대비 후퇴). −700 과도 억제로 파이락 수동화. 가설 반증 → v1(flat −260) 그대로 유지. 타임아웃 6건은 kill+재실행 워커 첫게임 경합(가짜행, hang-classes 5부류) — 실 hang 아님. ★교훈: QIC 점프 광산이 낭비라는 직관은 파이락 자금 문제의 원인이 아니었음. 진짜 병목 = 표준 랩(3O5C) 39 vs 싼 배기술 랩(2O3P) 1 — 사람은 트와일라잇 TS→랩(2O3P)으로 자금 아껴 R1 PI. 50%+ 달성엔 '싼 배기술 랩 우선' 레버 필요(별도 세션).
- 2026-07-15 **기각(행동 미변경 = 노이즈, 코드 되돌림): firaksCheapLab** — 트와일라잇 #2(TS→랩 2O3P) 싼 랩 직접-return. paired 40판 firaks석 ON 88.1 vs OFF 79.6 = +8.50(p≈0.200)로 커 보였으나 **로그상 발동 0회** → +8.50은 순수 변동성(레버가 안 움직임). 원인: 파이락 R1-2 트와 탑승 22%뿐 + 탑승 전 이미 표준 랩 건설(싼배기술:표준 = 1:35 불변). ★스킬 원칙 재확인("플래그가 실제 선택을 바꾸는지 검증, 안 바뀌면 무의미") + winner's curse 함정(발동 0인데 +8.5 = n=18 노이즈). **파이락 50%+ R2 PI의 진짜 벽 = 그리디 천장([[ai-greedy-ceiling]]): 사람의 트와 탑승→싼 랩→PI R1 오프닝은 다턴 계획이라 그리디가 자연 형성 못함.** 파이락 바닥은 이미 올림(firaksMineHold +3.90·firaksPiFunding +3.65 채택), 50%는 트와 조기탑승 강제(위험 큰 별도 세션) 필요.
- 2026-07-15 **채택(ON, 무해+도메인정답): `firaksPiPriority`** — 랩 보유 firaks R≤2에 PI 점수 +180(배 입장 150-450에 밀려 PI가 R3로 밀리던 것 교정). paired 40판 firaks석 −0.20(중립), R2이내 PI 여전히 ~10-12%. VP 무해 + 사용자 의도(PI 우선) 정합이라 채택하나 ★목표(R2 50%) 미달. **결론: 파이락 R2 PI 50%는 점수 넛지로 불가 — TS→랩→PI는 빠듯한 자금으로 3턴 순차 실행하는 다턴 오프닝이라 그리디 구조가 R2 완성 못함([[ai-greedy-ceiling]]). 오늘 4실험(mineHold v1채택/v2기각·PiFunding채택·cheapLab노이즈기각·PiPriority무해채택)으로 바닥은 올렸으나(파이락 평균 ~80→~88 좌석), 50%는 다턴 계획 능력(value-net/search, 사람 100게임 트리거 대기) 없이는 도달 불가. 강제 R1 오프닝 커밋은 5/5 실패 이력이라 비권장.**
- 2026-07-15 **채택(ON): `placementPolicy`** — 사용자 관찰("봇들이 다들 뜬금없이 혼자 있는 배치")의 데이터 기반 정답. 사람 22판(656 빌드결정) 학습 8-피처 선형 랭커(dOwn −3.57 밀집·dOpp −0.85 상대근접·newType −0.81 등), isolatedTSPenalty(−150 추측 휴리스틱)을 대체. 40판 VP **+1.94**(p=0.583, 승률 51.3%), 행동 정합: 광산 +0.43·연방 +0.16(밀집→연방 형성 쉬움)·연구진행 +0.81 전방향 양성. root 결정에만 적용(game.simulation 시 생략 → MCTS GC/hang 없음, 타임아웃 0 확인). 학습 랭커 > 추측 휴리스틱 업그레이드.
- 2026-07-15 **철회(winner's curse, OFF 복귀): `placementPolicy`** — 40판 VP +1.94로 채택했으나 blast radius 큰(전종족 모든 빌드) 변경이라 120판 확인 → 챌린저 OFF +2.05 = **ON −2.05로 방향 역전**(p=0.300 양쪽 노이즈, 진짜 효과 ≈0 음수쪽). 문서화된 winner's curse 11번째 사례. 스킬 원칙3(경계값 120판 확인) 정확히 작동. 고립 배치(사용자 관찰)는 기존 isolatedTSPenalty(−150 휴리스틱, R1-4)가 계속 담당. 학습 랭커는 VP 중립이라 순비용(root 전수 filter) 대비 이득 없음 → OFF. ★교훈: 40판 +2 내외는 blast radius 큰 변경일수록 120판 필수(작은 게이트 플래그와 달리 전역 변경은 회귀 표면이 큼).
- 2026-07-15 **확인 유지: `fedAbsorbBuildPenalty`** — 120판 확인 A/B(챔피언 ON vs 챌린저 OFF): ON 승률 55.9%, VP +1.27(40판 +4.08서 평균회귀했으나 **방향 유지·양수**, p=0.505). placementPolicy(전역, 부호 역전)와 대조 — 상황 게이트(비인접 대안 없을 때만) 플래그라 견고(게이트 플래그 생존 원칙 재확인). 유지.
- 2026-07-15 **확인 유지(do-no-harm): `rebel3qLadder`** — 120판 확인: ON 대비 −0.30(40판 +1.19서 제로 회귀, p=0.884 노이즈). placementPolicy 같은 큰 부호역전 아님(노이즈 내 중립) + 사람 3정큐 엔진(사용자 R1 QIC 주제) 행동정답이라 유지. 오늘 winner's curse 확인 스윕: placementPolicy 철회(전역, +1.94→−2.05 역전) / fedAbsorb 유지(게이트, +4.08→+1.27) / rebel3q 유지(중립+행동정답). 교훈 재확인: 전역 변경은 부호까지 뒤집히고, 상황·종족 게이트 플래그는 평균회귀해도 방향 유지.
- 2026-07-15 **채택(ON)**: `firaksLoopDrive` — 사용자 목표("매라운드 3O5C 재건하며 다운그레이드"): 다운 후보는 있었지만(1.75/판) 재건 연결이 없어 루프 단절이던 것. ①PI 보유 시 다운그레이드 최우선 직접-return(연방 다음 — 무비용 연구 전진이라 순서 강제 안전) ②다운 사용 후 랩 0 + 3O5C면 TS→랩 재건 직접-return(기술타일 재수확). 파이락강제 40판: ★다운 1.75→3.10(+77%)·랩건설 4.20·PI R2.85, 파이락석 +4.10(92.7). firaksEngineRush(-6.9, 후보 부재 강제)와 대조 — ★후보 실존 상태의 순서 강제(taklonsPowerFirst 동형)는 통함. 잔여 갭: 사람 다운 5.6 — R1-2 광석 조달(부스터 연동)이 다음 레버.
- 2026-07-15 **채택(ON)**: `firaksPiPriority` — 사용자 목표(PI 평균 R2 이하) 3단계: 실측상 랩 R1 완성(14/20)인데 R2에 자금이 모여도 MCTS가 의회를 미룸(지연군과 R2군의 유일한 차이). 의회 미보유+랩 보유+의회 후보 자금상 실존이면 직접-return(후보 실존 순서강제 3연승: taklonsPowerFirst·firaksLoopDrive 동형). 파이락강제 40판: ★파이락석 +7.00(93.0→100.0), PI R4.20→3.65·R2이내 2→5/20·다운 2.90→3.25. 파이락 3부작 누적: 좌석 ~78→100(+22). 잔여: PI 평균 R2 목표까지 자금 조달(R1 광석 부스터 연동)이 마지막 레버.
- 2026-07-15 **기각(OFF 보존) + 병목 재규명**: `researchFinishL3` — 사용자 관찰("R3에 경제 한 칸" 그루터기) 실측: 사람(80점+) L3+ 완주 4.0트랙 vs 봇 1.9트랙, 그루터기 원인 = (6-레벨) 새트랙 우대 공식으로 진단 → 완주 서열 교정(L1-2 +25, R2+ 신규 L0 −30) 40판: VP −4.46, ★행동 완전 무변화(L3+ 2.10=2.10, 그루터기 2.41≈2.48) — 후보점수 nudge 무시 함정 4호. ★실병목 재규명: 봇 총 연구 11.4 vs 사람 18.4레벨(−7레벨 ≈ 28K) — 완주 실패는 순서가 아니라 ★지식 총량. 지식 조달 스택(다운그레이드 +77%·아카 등 이번 주 채택분)의 하류 효과 재측정이 선행, 그래도 남으면 지식 소싱(2K액션·지식타일) 직접 레버.
- 2026-07-15 **기각(OFF 보존, 유의 음수)**: `upgradeBeforeFed` — 사용자 관찰(연방 후 그 안 건물 업글 = 순서 낭비): what-if(getBestFederationSpentTokensAfterUpgrade)로 위성 절약 확인 시 업글 선실행. 40판 승률 28.9% p=0.009·VP −4.28, 연방 +0.18(늘었는데 손해) — ★순서강제 클래스 첫 반례: what-if가 정확해도 '연방 1턴 지연' 리스크(보상 타일 소진·리치 타이밍·MCTS 재계획 변동)가 위성 절약을 이김. 연방은 가능한 즉시가 정답(기존 최우선 순서 유지).
- 2026-07-15 **채택(ON, 무해+보험)**: `ecoSciL5Late` — 사용자 관찰(경제/지식 꼭대기 R5 이전 등정 = L4 수입 정지 손해): 남은 징수 횟수 × 28점 차감. 40판 VP −0.21 중립 + ★재계측: 현 코드 셀프플레이의 eco/sci L5 등정 28건 전부 R5-6 = 조기 등정은 이미 부재(관찰은 배포본(≈2주 전 코드) 행동으로 추정 — 미푸시 57커밋 격차). 차감은 원리 정합 보험으로 ON(셀프플레이 no-op·실게임 방어). ※사용자 최근 관찰 다수가 미푸시분에서 이미 수정됐을 수 있음 — 푸시 후 재관찰 권장.
- 2026-07-15 **기각 확정(v1·v2 2연속 유의 음수) + 원인 규명**: `upgradeBeforeFed` — 사용자 재요청("순서만 다른데 지연 리스크가 왜 있나")으로 v2 정밀화(광산→TS 2O3C 한정 + what-if 절약≥1 확인, 발동 20게임): −5.28 p=0.044·승률 33.3%(v1 −4.28). ★순서가 중립이 아닌 이유 규명: 연방 즉시 형성 = 보상 자원 당라운드 가동 + 초록토큰 + rs8 5VP 즉시 vs 위성 절약 = 토큰 1-2개(봇 장부에선 비축 자원이라 실가치 낮음). '연방은 가능한 순간 즉시'가 측정상 정답 — 업글은 그 다음. 사용자 직관은 사람 파워순환(토큰 귀함) 전제. 축 종결.
- 2026-07-15 **채택(ON, v3) — 사용자 격리 지적 적중**: `upgradeBeforeFed` v3(의회/아카 한정) — v1 혼합(−4.28)·v2 광산→TS(−5.28 유의)로 축 종결했었으나 사용자 "의회나 아카가 체크 안 되는 것 같다" — 실제로 그 셀은 격리 측정된 적 없었음. v3(TS→의회/아카만 + what-if 절약≥1) 40판: ★+2.46 비음수, 발동 12회 전부 아카. ★셀 분해 교훈: 광산→TS(필러, 강제 시 템포 손실)와 아카/의회(어차피 하려던 고가치 엔진 행동 + 파워 2→3)는 같은 '업글 선실행'이라도 부호가 반대 — 혼합 측정이 진짜 신호를 가렸음. 사용자 재반박 적중 2연속(파이락 수입라인, 본 건).
- 2026-07-15 **전환(사람게임 한정 ON)**: `earlyGaiaQicCap` — 사용자 관찰(R1-2 QIC 2개 던져 가이아 건설) + 사람 실측(R1-2 가이아 광산 2QIC+ 사람 7%(포머 경유 70%) vs 봇 44%): 총 QIC≥2 가이아 직접 건설 후보 R1-2 미생성. 40판 ★+7.55 p=0.020(2Q+가이아 −73%, QIC가 우주선액션 +0.91·타일 +0.30으로 이동) → 120판 −2.29 회귀(★curse 13호, 행동은 유지) — 셀프플레이선 낭비 점프도 확장가치>차단(midTerraformGuard 계열 4차 확인). creditCapGuard 동일 프로필로 hasHumanOpp 한정 전환 — 판정 = 1:3 실측(봇 2QIC+ 가이아 44%→?).
- 2026-07-15 채택(무측정 룰수정): pickResearchTracks L4→5 후보에 isTrackLevel5Taken(선점=트랙당 1명) 체크 추가 — 서버 검증(gameState 1210행)과 정합. 선점된 트랙의 유령 L5 후보가 MCTS에 섞이던 대칭 버그(advClaimDrive v2 사고로 발견: 직접-return이 유령 후보를 뽑아 서버 거부→무진행→조기 패스, 총행동 −3.6).
- 2026-07-15 기각(OFF): advClaimDrive — R6 "초록+L4+미클레임 고급타일" 낭비(480석 중 222석 46% 실측)를 트리거 강제(리벨3Q·랩건설)+초록→L5 폴백으로 회수 시도. v1(adv만) 발화 2%로 무행동(−5.13 p=0.18 노이즈), v2(+L5) −4.45 p=0.29, v3(선점버그 수정 후) −7.36 p=0.041 유의 악화. 교훈: ①R5-6 트리거 희소가 진짜 병목(adv≥60 턴의 81%가 트리거 전무, 12게임 372턴 계측) — 후반 강제가 아니라 중반에 트리거 원료(TS·자원)를 남기는 문제 ②R6 직접-return은 MCTS가 이미 고려·기각한 선택을 강제해 손해("후보 실존+순서 강제"는 봇이 순서를 몰라서 못 할 때만 유효). 계측 인프라: ADVCLAIM_DIAG=1 env로 R5+ 초록보유 턴 게이트 분포 기록(data/advclaim-diag.jsonl) — 유지.
- 2026-07-15 기각(OFF): lateLowStepGate — 사용자 재보고("R3-4 경제 한칸 올리고 방치") 실측: 고립 경제1칸(1회·최종≤2) 봇 33% vs 사람 3%(쌍봉: 0 또는 L4-5), 전원 4K 유료. 유료 연구 후보 생성에서 경제/과학 도착레벨≤2 하드 제외(미션 예외 없음 — 사용자: +2VP는 트랙 불문). 행동은 확실히 바뀜(고립 13→8석)이나 VP 일관 소폭 음수: R4+ 40판 −1.80 / R4+ 120판 −1.59(승률 51.8%) / R5+ 축소 40판 −2.76. 해석: 봇의 4K 반사실이 '더 나은 소비'가 아니라 종료 잔여변환(~1VP 부스러기)이라 차단해도 VP로 전환 안 됨 — 저레벨 스텝도 징수 1회 수입+액션으로 근사 등가. 진짜 레버 = 커밋 후속(L3그루터기→L4/5 완주, 지식 총량 병목)이지 후반 차단이 아님. researchFinishL3(nudge 무시)→lateLowStepGate(하드 제외, 행동은 변함)로 메커니즘은 입증 — 남은 건 방향.
- 2026-07-15 채택(ON): qicMineLabGate — 사용자 관찰("R1에 1QIC 광산 안 지으면 연구소 지을 수 있는데 그걸 날림") 실측: R1-2 QIC 가이아 광산 봇 91% vs 사람 48%, 'QIC광산+R2까지 랩없음' 봇 13% vs 사람 4%. R1-2 & 랩0 & TS보유(랩 업글이 다음 수) 동안 QIC 소모 가이아 직건 후보 제외(랩 완성 후 해제, 포머 경유 무관). 40판 −0.53±3.93 p=0.89(중립) + 행동 개선: QIC광산+랩없음 21→14석, 첫 랩 R1.44→1.36. 무해+도메인 옳음+행동 확인 → 채택.
- 2026-07-15 채택(ON): geodensPiAfterAcademy — 사용자 관찰("기오덴 아카 지었으면 2번째 TS는 의회여야 하는데 연구소를 지음"). firaksPiPriority 동형(후보 실존+순서 강제): 아카 보유+의회 미보유+의회 후보 실존 시 직접-return. 기오덴 강제좌석 paired 40판: ON 99.8 vs OFF 97.35 Δ+2.45(p=0.74) — 양방향+무해+도메인 옳음.
- 2026-07-15 채택(무측정 룰수정): gain-2-steps 단독 파워액션 부스터 가드 — 사용자 관찰(타클론): 부스터 1TF 미사용인데 3P 삽 소진 시 5P 2삽 폴백으로 1스텝 땅 건설(스텝+파워 낭비). 1스텝 목표만 있고 부스터 1TF가 살아 있으면 5P 폴백 금지(공짜 삽이 지배). 콤보 경로는 tfBonusCombo·gain1 가드가 기존 커버 — 단독 경로의 구멍만 보수.
- 2026-07-15 채택(ON): darkTsFirst — 사용자 관찰("다카니안 R1 광산만 3-4개 짓고 패스"). 실측: R1 TS 확보율 다카니안 48% vs 타종족 94-100%, 평균 VP 최하위(74.5). 원인 = 7색 1스텝(2O 고정) 광산 후보 과다로 시작 7O 전소 → TS(2O6C) 불가 → 기술 엔진 미진입. R1-2 & TS 0 & TS 후보 실존 시 TS 직접-return(후보 실존+순서 강제). 다카니안 강제좌석 paired 40판: ON 74.5 vs OFF 67.9 Δ+6.55(p=0.21).
- 2026-07-15 채택(ON): powerBudgetRoundGuard — 사용자 관찰("R4인데 2파워 안 받고 회피, 너무 빨리"). powerAcceptBudget(누적 8VP+ 시 가치 ×0.6)이 이른 라운드까지 물들여 R4 2PW/1VP(기본모델 수락)를 뒤집던 것 → 할인을 활용 라운드 ≤2(≈R5+ 또는 패스 후)로 한정. 40판: VP −0.80±3.39 p=0.81(중립) 승률 60%. 무해+도메인 옳음.
- 2026-07-15 채택(무측정 룰수정): 우주선 연방 무료광산 보상 타깃 검사 — 사용자 관찰("3Step FreeMine 가져가고 건설 안 함"). ship-fed-3tf-mine/mine-free 보상 점수가 고정 가산(+260/220)이라 닿는 건설 타깃 0이어도 선택 → 죽은 보상. what-if(무료 플래그+삽 임시 세팅 → findBuildActionsWithPendingSteps 후보 실존)로 타깃 0이면 가산 제거, 다른 보상(VP/자원)이 이기게. 획득 후 처리(pendingTerraformSteps 강제 건설·비용 면제)는 기존 정상 — 선택 단계의 구멍만 보수.
- 2026-07-15 채택(ON): sciIncomeScale — 사용자 관찰("R6에 12K로 Eco1/Sci0 → Eco3+Sci2 패스" — Eco L4 +8VP가 정답인데 분산). 원인 = 경제는 남은징수 비례 스케일 적용됐는데 과학은 옛 고정 공식이라 R6 Sci0→1(가치0)이 132점 = Eco3→4 종료보너스와 동점. 과학 반복수입도 (6-레벨)×22×(남은징수/5)로 스케일(researchValueModel 하위). 40판 +0.55±3.72(중립-양) + 행동: R5+ L3도달 1.86→2.00, 연구 +0.15.
- 2026-07-15 채택(ON): lantidsFedCluster — 진단: lantids 최하위(68.9)의 주범 = 연방 1.32 vs 상위권 2.45(연방당 7-12VP). 기생 5.1개가 상대 클러스터 따라 흩어져 위성으로 못 묶임. 기생 후보에 '내 자산(건물+기생) dist≤2 근접' 가점(+40/개 캡120, 고립 −80) — 기각된 Push(상대밀집 우대)와 반대 각도. lantids 강제좌석 paired 40판: ON 78.6 vs OFF 69.35 Δ+9.25(p=0.20, 오늘 최대폭), 연방 1.32→1.45(혼합).
- 2026-07-15 채택(ON): lantidsParasiteWindow — 사용자 지시("근사 말고 롤아웃이 커버하게, 의미없으면 종료"). 기생의 발판/가교 가치를 근사 가점 대신 롤아웃이 직접 판정: MCTS 롤아웃 창이 상위 8(TOP_N)뿐이라 기생 후보 9위 밖이면 체인 탐색 기회 0 → 최고 기생 1개를 창 안(4위)으로 이동만(점수 인플레 없음). lantids paired 40판: ON 77.15 vs OFF 71.75 Δ+5.40. (미측정 근사 lantidsBridgeValue 블록은 폐기·삭제. 교훈: 후보점수는 추리기용이므로 '창 보장'이 근사 가점보다 정직한 개입.)
- 2026-07-16 채택(ON): fedLabKeepOut — 사용자 룰("연방할 때 랩 3개 묶으면 아카 업글 손해"). 연방 랩은 ①아카 +1pw가 완성 연방에서 증발 ②noFedTierUp이 연방 랩→아카를 R6까지 봉쇄(자체 필터의 역효과). 플래너 플랜 채점에 포함 랩당 −70 소프트 페널티(R6 미적용, 랩이 7파워 필수면 보상이 커버). 40판 +0.26±3.38(중립) — 무해+도메인 옳음.
- 2026-07-16 채택(ON): labSpread — 사용자 관찰("건설할 때 연구소 3개가 딱 붙는 경우가 너무 흔함" — fedLabKeepOut의 상류). 기존 랩 2+개와 dist≤2로 붙는 TS는 덜 붙는 대안 TS가 있으면 랩 후보에서 하드 제외. 40판 +4.22±2.95(p=0.15) 승률 65.8% + 행동: 연방 1.59→1.78 증가(분산→연방 수↑ 메커니즘 확인), 연구소 수 유지.
- 2026-07-16 기각(OFF): taklonsPiCommit — 실측(봇 타클론 PI 미건설 43% vs 사람 6/6, 타이밍은 R4.3≈R4.5 동등)에서 firaksPiPriority 동형 직접-return(R≤4 PI 최우선) 시도. 타클론 paired 40판: ON 79.85 vs OFF 81.55 Δ−1.70 — 행동은 변함(건설률↑)이나 VP 음수. 타클론 PI는 파이락(다운그레이드 엔진 개통)과 달리 강제 가치가 없음; 미건설 43%는 결핍이 아니라 상황 판단으로 재해석. 사람 표본 6석 = 약한 근거였음.
- 2026-07-16 보류(전역 기각): rolloutTopN=12 — nevlas 진단(연방 1.67 vs 상위 2.42, 건물·연구는 대등)에서 롤아웃 창 확대를 기존 플래그로 프로브. nevlas paired Δ+3.20(노이즈 범위) / 전체 −1.24(타 종족 중립-음수: 400ms가 12후보로 분산). lantidsParasiteWindow(+5.40)와 달리 굶는 후보 클래스가 특정 안 됨 — nevlas 연방 병목의 구체 원인(후보 부재? 위성? 토큰?) 규명이 선행. 다음 레버: nevlas 연방 후보 생성 시점 계측.
- 2026-07-16 기각(OFF): gaiaThroughput — 흐름 해부(resourcePolicyDiff: 좌석당 액션 사람 63 vs 봇 48, 가이아광산 3.08 vs 1.39, 연구 L1 그루터기 46%)에서 가이아 심화 가격(8/레벨 = eco·sci의 1/3)을 실가동 조건부로 수입트랙급 상향. 40판 −4.97±3.47 승률 40% + 행동 역방향(가이아광산 1.75→1.30, 연방↓) — 연구만 우대하면 4K가 새고 빌드 미연결(expansionResearch·resourceActionSetup 실패계보 재확인). 가이아 격차는 다턴 복리 문제라 연구 가격표로 못 풂. 교훈 갱신: 흐름 전선의 셀프플레이-측정가능 레버는 소진 상태 — 남은 길 ①geodensNewType형 '빌드타깃+실건설 연결' 설계 ②사람게임 한정 룰 ③학습(보드인코딩) 프로젝트.
- 2026-07-20 채택(ON): piGateWide — 94게임 리프로브 발견: 사람 PI 79건 중 봇 후보 부재 84.8%, 그 72%(39건)가 R1-3 게이트(사람은 전 종족이 광산 1~7 기반 조기 PI — 스자·기오덴 광산1, 매안·암바스 광산2 등). 기존 부분개방(r1PiOpen 종족제한·piGateOpen 광산4+)을 넘어 자금 실존 시 후보 무조건 개방(순수 후보 개방, 선택은 MCTS — acadGateOpen 동형). 40판 −2.99(노이즈) → 120판 −0.15±1.91(완전 중립) + 행동: PI 평균 R3.78→R2.50, 건설률 0.62→0.71. ★"게임 수가 쌓여야 보이는 발견"의 첫 사례(21게임 땐 종족별 땜질, 94게임에서 일반 패턴 확정).
- 2026-07-20 기록(미착수 레버): advTileBundleValue — 사람 실전 반례(2vmhjhjm 사보르 R6): 외각 고급타일(즉시 4VP)을 7VP 타일 대신 선택 = 옳은 수(4VP + 딸린 트랙전진 terra L2→3 종료보너스 +4 + 증발 예정 초록 처분 + 커버 대상이 효과소진 타일이라 비용 0 ≈ 8VP+ > 7). 우리 scoreAdvancedTechTile은 타일 VP만 보고 ①획득에 딸린 트랙전진 가치 ②초록 처분가치를 미반영 — advTileValueFloor(<12 차단)가 이 정답 수를 차단했을 것. 묶음 가치 반영 레버.
- 2026-07-20 채택(ON): fedR6ConvCap — 사용자 1순위("연방이 좋다고 보이면 온갖 손실 감수 — R6 시작하자마자 광석→토큰 12VP 연방"). 실측: R6 연방 중 광석→토큰 동반 사람 14%(평균 2.0개) vs 봇 58%(3.4개). R6 변환 상한 6~8→3(사람 캘리브레이션). 40판 +1.71±3.44 승률 52.5% + 행동: 챌린저 연방 1.75→1.51(비싼 연방 포기)인데 VP 양 = 광석 3+ 소각 연방은 순손해 방향 확인. 관련 미착수: ①번(burn)이 형성가능 연방의 위성 지불력을 죽이는 역방향 가드(fedTokenGuard) ②advTileBundleValue(고급타일 = 타일VP+딸린 전진+초록 처분 — 사보르 반례).
- 2026-07-20 채택(ON): fedLastCall v3 — 사용자 정책("연방은 다른 액션들 — 토큰 파워액션·랩·가이아L2 +3토큰·AI트랙 — 을 하다가 '더 할 것 없음 vs 지금이 마지막' 시점에 형성, 고급타일 여는 연방은 앞당김"). 구현: R5+ & 연방 외 생산 후보 존재 & !advUrgent(초록0+adv≥60) 시 연방 후보 보류 + 번 가드(보류 연방의 위성 수요 밑으로 토큰 태우는 후보 제외 — 사용자 예시 A). 반복: v1(가드 없음) R6 연방 0.26 증발 → v2(번 가드) R6 연방 0.40 회복·총연방 −0.17(중반 미루기 부작용) → v3(R5+ 한정) +0.97±3.16, 총연방 1.55≈1.56 비감소, 총행동 +2.4/석. ★번 가드 = 예시 A(3그릇 태우고 연방 못함)의 부분 해결 — 전 라운드 일반화(fedTokenGuard)는 미착수.
- 2026-07-20 채택(ON): piR1HumanOnly — 사용자 관찰("왜 발타크도 R1에 의회?"): piGateWide가 R1까지 전면 개방해 비정합 종족 R1 의회 폭증(480석 중 발타크 25·스자 26·암바스 17 — 사람 R1 의회는 PI-파워 종족 6개뿐). R1은 r1PiOpen(사람 실측 목록)만, 전면 개방은 R2+부터. 40판(38완주) +4.16±3.77 승률 65.8% + 행동: 비정합 R1 의회 13→0. piGateWide의 하루 만의 정밀화 — 셀프플레이 중립이어도 사람 정합 붕괴는 관찰로만 잡힘.
- 2026-07-20 기각(OFF): navTileNavScale — 사용자 관찰("Nav+1 타일은 이미 nav 2~3단계면 덜 필요"). 프로덕션 활성 경로(bot.ts 평평한 +90/+45, shipTechEntryValue는 기각됨)에서 Nav+1 타일 점수를 nav 연구레벨로 감쇠(nav≥2 ×0.85, nav≥4 ×0.6). 120판 승률 42.5%(51:69) VP −1.75±2.13 p=0.41 — 유의차 없으나 부호 음수+승률<5할이라 "무해" 아님. ★행동 검증 실패: 의도는 Nav+1 획득↓인데 도전자가 오히려 ↑(1.13→1.32) — Nav+1은 주로 nav 낮은 초반에 선택돼 감쇠 조건(nav≥2)이 의도 케이스에 거의 안 걸리고, 걸릴 땐 봇 약점인 reach 투자를 깎아 음수로 샘. 직관은 합리적이나 실전 발화 케이스가 어긋남. 코드 되돌림. (참고: r1ShipPriority Nav+1 우대는 미변경 유지 — reach 약점 직격이라 채택된 방향.)
- 2026-07-22 채택(ON): fedCredit6CStarve — 사용자 관찰("극심한 크레딧 기아인데 광석 연방 선택"). fedRewardResourceAware(06-19 채택)가 돈 남을 때 6C 감점만 있고 기아 방향이 없어 2O(+2)가 항상 승 — 돈 여유(잔고+수익×min(3,남은R)) ≤18이면 6C +30, ≤10이면 +50(위성/업글 닫기 두 경로 모두 getRewardScore 경유라 단일 수정). 120판 격리OK: 승률 53.4%(63:55, p=0.461), VP +2.71±1.89(p=0.150) — 비음수+방향 양성+도메인 옳음. 행동믹스 특이사항 없음(연방 1.45→1.55). 참고: 보상 선택은 MCTS 아닌 getRewardScore 직접 결정이라 조건 발화 시 행동 변경은 구조적으로 보장(발화 빈도 계측은 안 함).
- 2026-07-22 채택(ON): r1PiCalib103 — 103게임 리캘리브레이션(사람 로그 100+ 돌파 리프로브). 사람 R1 의회 50건 집계: 당시 광산수 거의 전부 0~1(시작광산→TS→PI 체인이라 구조적으로 1) → 기존 r1PiOpen의 mineCount>=2 게이트가 사람 패턴을 사실상 전부 차단(piR1HumanOnly가 연 문이 실제론 안 열림). 광산 게이트 제거 + 실측 종족 확장(darkanians 3·itars 3건 추가, bescods 1건은 노이즈 제외). 리프로브 행동검증: PI 후보 커버 22→49건(갭 75.8%→46.2%), R1 종족게이트 잔여 30→3건. 120판 격리OK: 승률 50.8%(60:58), VP +2.76±2.01(p=0.170), PI평균R 3.36→3.19(조기화) — 비음수+사람정합 → 채택. 다음 미착수: 아카데미 갭 1위 '자원부족(6O6C) 29건' = 사람은 파워→광석 변환 후 아카 건설, 봇은 잔고만 봐 후보 부재(upgradeConvertCombo의 파워→광석 확장 레버).
- 2026-07-22 기각(OFF): acadFundV2 — 103게임 리프로브 아카 갭 1위(자원부족 6O6C 29건: 광석갭3 캡초과·광석+크레딧 혼합갭) 겨냥, 혼합 변환 조달(3P→1O·1Q→1O + 1P→1C·1O→1C) 후보 개방. 리프로브 커버 82→86(+4, 부분폐쇄 — 잔여는 번 선행 필요·타클론 제외분). 120판 격리OK: 승률 46.6%(55:63), VP −2.99±2.22(p=0.178), 아카/의회 Δ−0.04 = 의도 행동 미증가 + VP 음수 방향 → 기각(코드는 플래그 OFF 보존). 교훈: 사람 정합 후보라도 셀프플레이에서 변환 연료(bowl3·QIC)가 사람만큼 안 쌓이면 오발동 비용만 남음 — 남은 25건은 '번 선행 조달'(위험) 또는 사람게임 한정 룰 후보.
