# AI 봇 강화 — 측정 기반 진단 (2026-06-08)

목표: 봇 점수 대폭 상향(사용자 요청 "평균 180"). 결론부터: **현재 평가함수 구조로는 self-play 승자 ~85가 천장이고, 격리된 손잡이 조정으로는 안 깨진다.** 아래는 그 근거와, 진짜로 점수를 올리려면 무엇을 건드려야 하는지.

## 측정 인프라
- `npm run head2head` — 챔피언 vs 도전자를 같은 테이블에 앉혀 승률·VP마진을 유의성과 함께 측정(좌석 6패턴 회전으로 편향 상쇄). 자세히는 `HEAD_TO_HEAD.md`.
- 베이스라인(self-play 4봇): 150ms → avg 63.5 / 승자 84.3 (20판). 1000ms → avg 64.0 / 승자 85.7 (12판). **생각시간 6.7배도 변화 거의 없음.**

## 측정된 NULL 결과 (모두 head-to-head, p값)
| 시도 | 결과 |
|---|---|
| 생각시간 6.7배↑ | 변화 없음 (84→86) |
| 가중치 튜닝(tuned vs default) | 동률, 약간 나쁨 |
| `candReorder` (우주선/인공물 후보 전진) | 41.7% 승, p=0.32 |
| `shipEntryGate` (R6 입장·미사용함 적재 차단) | 45.7% 승, p=0.61 |
| `shipLowPriority` (우주선 후보점수 200→40, n=60) | 53.3% 승, +2.08VP, p=0.45 (유일한 +방향, 미미) |
| `expandDrive` (확장 보상 5번째부터 강화, n=60) | 48.3% 승, −1.40VP, p=0.63 |
| `scriptedStrategy` (연방 형성가능+<2면 MCTS우회 강제형성, n=60) | 45.8% 승, +0.76VP, p=0.79 |

→ 탐색량·가중치·후보순서·우주선·확장보상 어느 것도 점수를 못 올림(최선 +2VP, 확인 불가). 봇 약점은 **평가/롤아웃의 전략적 질**이며 격리 수정에 안 움직임.

## 중대 정정: 180은 규칙상 불가능이 아님
초기에 "바닐라 Gaia 4인 총합 ~400-550 → 180 불가능"이라 단정한 것은 **틀렸다**. 이 구현은 우주선/트와일라잇 인공물/이클립스 등 커스텀 콘텐츠로 VP가 인플레된 변형이며, 사용자에 따르면 강한 사람은 **270-300점**을 낸다. 따라서 180은 달성 가능하고, 봇의 self-play 승자 ~85는 천장이 아니라 **강한 플레이의 ~1/3 수준으로 약한 것**. 위 7개 tweak null은 천장이 아니라 약한 정책에 대한 미세 조정이었을 뿐. (단, data/human-games의 365판은 전부 봇 게임 — 실제 강한 사람 플레이 데이터는 아직 0판.)

## 학습형(B) 1차 시도 — 가치망 leaf eval (실패, 유의)
- 파이프라인 구축: features.ts(33특징) → valueNet.ts(순수 TS MLP) → valueData.ts(자기대국 수집) → trainValueNet.ts → evaluator `useValueNet` 연결.
- 17,434 샘플 학습: 검증 MAE 7.56 VP (기준선 15.27의 절반) — **예측기로는 우수**.
- head2head(60판, useValueNet vs 휴리스틱): 도전자 **33.3% 승, VP −11.59 (p<0.001)** — 유의하게 **더 약함**.
- 교훈(전형적 함정): 약한 정책 게임으로 회귀학습한 가치망을 MCTS에 그대로 꽂으면 **탐색이 망의 외삽 오류를 악용**해 휴리스틱보다 못하다. 해결은 (a) 정책+가치 동시학습, (b) 자기 탐색이 만든 상태로 반복학습, (c) 정책 prior로 탐색 유도 — 즉 **제대로 된 AlphaZero 루프**(다일 규모) 또는 **강한 사람 데이터로 학습**(현재 0판, 1:3 필요).

## Capstone (expandDrive + scriptedStrategy)
 뿌리인 확장을 *직접* 강하게 보상해도 null(−1.4)이고, 가장 큰 단일 액션(연방)을 MCTS 우회해 *강제 실행*해도 null(+0.76). 즉 **가치 평가도, 단일 액션 강제 실행도** 안 먹힌다. 7가지 범주(탐색량·가중치·후보순서·행동게이팅·우주선점수·확장보상·연방강제) 전부 무효 — 봇의 약점은 단일 지점이 아니라 **다턴에 걸친 빌드오더 전체를 엮지 못하는 구조적 한계**다. 단일 스크립트 규칙으로도 안 풀리며, 포괄적 빌드오더 계획 엔진(사람 플레이 근거) 또는 학습형 정책이 필요하다. 봇은 확장·연방·연구를 충분히 높게 평가하지만 "경제→확장→연결→연방→연구5" 시퀀스를 엮지 못한다. 그래서 평가함수/가중치/후보 어떤 조정도 효과가 없다(6번 실험으로 확증). 깰 수 있는 것은 (a) 강한 사람 플레이로부터 추출한 **명시적 다턴 전략(빌드오더) 스크립팅**, 또는 (b) **학습형 정책/가치망(RL)** 뿐. 둘 다 사용자 입력(1:3 로그)·결정 필요.

## 근본 진단 (21,068 플레이어 로그 분포)
- **연방 수: 0=12%, 1=70%, 2=17%, 3=1.6%.** → 70%가 연방 1개에서 멈춤(강한 플레이 2-3개).
- **L5 트랙 수: 0=70%, 1=30%, 2=0.4%.** → 70%가 연구 5단을 한 트랙도 못 감.
- **점수 분포: 40-60 최빈, 평균 57.5, 100+ 단 2%.**
- 구조물 8-11개(강한 플레이 12-16개) → **확장 부족**이 사슬의 뿌리: 적은 구조물 → 2번째 연방 클러스터(파워7) 불가 → 소득 부족 → 연구 5단 불가.
- 우주선: 입장 26,979회 vs 액션 ~9,200회 — 입장 66%가 액션 없이 -5VP. 단, head2head에선 게이팅이 중립(입장비용 절감 = 액션이득 손실 상쇄).

## 진짜 점수를 올리려면 (격리 손잡이 X)
봇은 연방·연구를 **가치는 알지만(가중치 높음) 다턴에 걸쳐 도달을 못 함**. MCTS(깊이8)+근시안 롤아웃이 "지금 광산 1개"는 보지만 "3턴 후 2번째 연방"은 못 봄. 필요한 것:
1. **사람 플레이 기반 재설계** — 강한 사람의 1:3 게임(`humanGameLogger`가 `data/human-games/`에 자동 저장)을 봇 수순과 비교해, 확장/연방/연구 타이밍의 다턴 원칙을 추출해 후보생성·롤아웃 정책을 구조적으로 교체. (추측 아닌 근거 기반 — null을 깰 유일한 입력)
2. **학습형(RL) 가치망** — 자기대국으로 가치/정책망 학습. 천장 상향 가능하나 별도 파이프라인(며칠).

## 부수 성과(이번 세션, 별개로 가치)
- **프로덕션 프리징 버그 수정**: 봇이 우주선 트랙 4→5(초록연방 게이트) 무한 재시도 / 패스 실패 시 정지 → 유저 게임이 멈추던 버그. 근본수정+자가복구+graceful 스킵. self-play 100+판 멈춤 0.
- BOM 버그(aiWeights.json 무시), MCTS 디버그 로깅 과부하(게임 5-10배 지연) 수정.

## 야간 강화 시도 로그 (head2head, 가중치 고정)
- **iter1 `useGoalPlanner`** (평가 바이어스: 연구페이스·우주선액션·소행성·adv-pass·연방페이스): 36판 → 도전자 **33.3% 승, VP −4.3 (p=0.046)** → **유의하게 더 약함, 기각.** 평가 바이어스는 MCTS 국소최적화를 교란 → 재확인: 약점은 가치가 아니라 다턴 실행.
- **iter2 `deepRollout`** (롤아웃 메인액션 1→5회 연쇄, 스텝 6→14, 수입 미반영): 60판/400ms → 도전자 **51.7% 승, VP +0.88 (p=0.76)** → 중립. 한계: 수입 미반영이라 다라운드 경제를 못 봄.
- **iter3 `deepRollout`+`rolloutIncome`** (가상 라운드 사이 구조물 기반 수입 근사): 60판 +3.83(p=0.097)로 유망했으나 **n=120 확정에서 +1.82 (p=0.378)로 회귀 → null.** n=60 신호는 노이즈였음(확정 측정의 중요성). 채택 안 함.
  → **종합**: 탐색·평가·롤아웃·수입근사·가치망(약데이터) 모든 자율 레버가 ≤+2VP(확정 불가). 봇 ~85는 휴리스틱+얕은MCTS 구조의 천장. 돌파엔 **강한 사람 데이터 다수 수집(dev:collect로 특징 캡처) → 모방학습/가치망** 또는 **풀 AlphaZero**(다일) 필요. 현재 강한 게임 1판(273, 행동일지뿐·특징 미캡처).
- **shipEngineBonus v1** (사용액션×35 + 체감보상 탑승잠재): 60판 → 49.2%승, VP −3.03 (p=0.25) → null(음수 기운). 탑승 잠재가치(b)가 과탑승 유발 의심 → (a)사용액션만 남긴 v2로 재검증.
- **shipEngineBonus v2** (사용액션×35만): 60판 → VP −3.30 (p=0.21) → null(같은 음수 기운). 우주선 eval 보너스는 v1/v2 모두 기각 — 봇끼리에선 더 좋은 액션을 밀어냄. 코드 유지(flag off).
- **fedPacePush** (연방 페이스 뒤질 때 클러스터 질 신호 1.9×): 60판 → 45.0%승, VP −3.04 (p=0.17) → null. 기각.
- **의심: 측정 편향** — 최근 3실험(shipEngine v1/v2, fedPace)이 모두 VP −3.0±0.3으로 동일한 기운. 서로 다른 변경이 같은 값 → 챌린저 좌석 시스템 편향 가능성. A/A(동일 vs 동일) 60판으로 보정 측정.
- **A/A 보정** (동일 vs 동일, 60판): +2.70 ± 2.76 (55.9%승, p=0.36) → **장비 편향 없음**. 최근 −3들은 진짜 약한 음수 = eval 보너스 공간 포화 확정. 60판 노이즈 SE ±2.7 → 미묘한 효과는 120판 필요. → 탐색공간(deepRollout)으로 피벗.
- **deepRollout** (롤아웃 다턴 연쇄, MAIN_ACTION_CAP 5): 60판 → 47.4%승, VP +0.41 (p=0.89) → 순수 null. 수입 미반영 + 탐색시간 분산으로 상쇄 추정. 기각(코드 유지 flag off).

## 핵심 버그 발견·수정 (2026-06-13): 고급 기술타일 수락 버그
- 진단(최근 30판 gameLog): 기술타일 선택 114회 중 자격(연방1+L4) 23회 — **고급타일 채택 0회**.
- 원인: botHandler가 getNextMove 결과 중 'select_tech_tile'만 수락, MCTS가 고른
  'select_advanced_tech_tile'은 fallback(일반 타일)으로 **덮어씀**. 봇 고급타일 0.00의 직접 원인.
- 수정: 두 타입 모두 수락 + pendingAdvancedTechCover를 봇 트리거 조건에 추가(커버 단계 멈춤 방지).
- 검증: A/A 60판으로 (1) 고급타일 실제 획득 수, (2) 평균 점수 회귀 없음 확인 중.

## 고급타일 후속(2026-06-13 추가)
- findTechTileAction에 고급타일 평가/반환 연결 + botHandler에 pendingAdvancedTechCover 전용 핸들러 추가
  (없으면 고급타일 선택 시 게임 hang→타임아웃). 둘 다 올바른 수정.
- 그러나 검증 결과 봇 고급타일 여전히 0.00 — **상류 병목**: 선택 순간 (green연방≥1 AND 트랙L4) 동시충족이
  거의 없음(fed 1.37, L4+ 1.03). 즉 고급타일은 연방수·연구깊이가 풀려야 작동하는 "하류". 수정은 dormant로 유지.
- 결론(재확인): eval 보너스 공간 포화(A/A로 무편향 확인). 진짜 병목은 연방수(1.4 vs 4.5)→초록토큰→L4/5→고급타일.

## 2026-06-13 종합 (self-play 천장 확정)
- **[CRITICAL 수정] 고급타일 hang**: executeSelectAdvancedTechTile가 pendingTechTileSelection을 안 비워
  봇이 고급타일 무한 재선택→실유저 게임 동결. 수정·검증(24판 타임아웃0). 커밋 a343efe. ← 오늘 최대 실질 성과.
- **fedMinTrim 재검증**(hang 제거 후): 40판 중 2 타임아웃 잔존(최소부분집합 연방을 서버가 드물게 거부 추정) +
  효과 불확실 → **미채택(flag off 유지)**. 사용자 '7초과 연방' 관찰은 기존 clusterFedBonus가 7-클러스터로 이미 유도.
- **진단 확정**: 봇은 후반에 빌드 후보가 있음에도(reach 아님) 빌드 대신 다른행동 선택 → 정책 문제.
  eval 보너스 공간 A/A로 포화 확인. self-play 튜닝으론 점수격차(봇~94 vs 사람223) 못 좁힘.
- **다음 길(둘뿐)**: ① 강한 사람게임(7판,승자≥180)+학습샘플로 정책/가치 학습(약 self-play 학습과 다름).
  ② 1:3 실전으로 do-no-harm 변경 실효 검증(사용자 진행 중). 자가대국 추가 튜닝은 비생산적.

## 2026-06-13 navBeforeJump 채택 ✅
- head2head 50판(승패42): 도전자(navBeforeJump) 승률 **64.3%** (27:15, 95%CI 49~77%, **p=0.064**), VP +2.27±2.91(p=0.436 무해).
- 지금까지 검증한 플래그 중 **유일하게 승률이 유의에 근접하며 긍정** + 사용자 관찰 직접 교정.
- → bot.ts:1519 fallback false→**true 승격**(기본 ON). flag로 끄면 구동작.
- 효과: "nav 올릴 거면서(지식≥4,nav<5) QIC로 멀리 점프해 광산 흩뿌리기"를 억제 → 연구 먼저 후 0-QIC 확장. willNavResearchSaveQIC 가드로 불가피한 점프는 허용.

## 2026-06-13 결정적 증거 4종 — greedy 아키텍처 천장 확정
### (1) mctsTimeMul=4 head2head (연산 스케일링 테스트)
- 도전자(1600ms) vs 챔피언(400ms): 승률 41.9%(13:18), VP -3.11 → **연산 더 줘도 안 강해짐**.
- 함의: 트리 1턴 깊이 + greedy 롤아웃이라 시간↑ = 같은 선택 재확인. **실게임 6초 봇 ≈ 400ms 측정 봇** → "94 vs 223"은 측정 artifact 아님.
### (2) valuenet-data 16.7만 샘플 종반(R5+) 봇 vs 사람 실측
| 지표 | 봇 | 사람 | 격차 |
|---|---|---|---|
| techTiles | 2.73 | 8.54 | +5.81(최대) |
| gaiaPlanets | 1.53 | 4.97 | +3.44 |
| structures | 9.24 | 13.97 | +4.74 |
| federations | 1.18 | 2.63 | +1.46 |
| **ship(진입)** | **2.29** | **2.70** | **+0.41 거의 동등** |
| pi | 0.48 | 0.48 | 0 |
- **우주선 진입은 격차 아님**(가설 반증). 엔진 전체가 사람의 ~60% 스케일로 비례 축소 = 단일 누락 아닌 다턴 램프업 실패.
### 종합 결론
- 4각도 증거(eval포화 A/A, 연산스케일링 실패, 균일60%엔진, 우주선정상)가 **greedy 1-ply 아키텍처가 천장**임을 확정.
- 사람대등(223)은 **튜닝/연산으로 불가** → (A) 진짜 다라운드 search 엔진[상호작용 프롬프트/hang 위험으로 고난도] 또는 (B) 학습 정책[가치망-as-leaf는 실패, 정책-as-후보바이어스는 미시도]. 둘 다 수일 규모.

## 2026-06-13 경로 B(학습 가치망) 도 데이터 기아로 사망 — 실험 증명
- valuenet-data: y라벨 19.8만 = 봇 19.7만(평균VP 66.7) + 사람 435(평균VP 250.3). 사람 0.2%.
- 가치망(33→32→16→1 MLP) 학습 + gradient 프로브(종반 봇 상태에서 엔진 피처를 사람 수준으로 ↑):
  - 기본: techTiles/gaia/structs/feds 전부 **+0.0 VP** (모델이 현재 score만 읽음).
  - **score 계열 피처(2,29,30) 마스킹 후에도 전부 +0.0 VP**, 사람 valMAE 33.7(악화).
- 원인: 저엔진 데이터는 전부 봇@VP66, 고엔진은 사람@VP250뿐 → **연속 기울기 없는 2덩어리** → MLP가 인과 학습 불가, 지배적 봇평균 예측.
- 결론: **현 데이터(사람 7게임/435샘플)로는 1-ply re-ranker도 greedy와 동일.** 경로 B는 데이터 양이 관건.
  - 필요: 강한 사람게임 대량(게임당 ~60샘플 → 50~100게임=3천~6천샘플) + 강도 연속성(중간실력 포함). 사용자가 1:3 로그 누적 중 → 데이터 늘면 harness(scripts/trainMasked.ts, probeValueNet.mjs) 재가동.
- harness 준비 완료: scripts/trainMasked.ts(score마스킹+누출방지 학습+프로브), scripts/probeValueNet.mjs(gradient 프로브).

## ★ 전체 결론 (모든 레버 실험으로 닫음)
1. eval 튜닝 → A/A 포화 (8+ 플래그 null)
2. 연산 → mctsTimeMul=4 음성 (greedy라 스케일 안 됨)
3. 학습 가치망 → 데이터 기아 (엔진 gradient 0)
→ 사람대등(223)은 **(A) 다라운드 search 엔진 재작성[hang위험·고난도] 또는 (B) 사람데이터 10배+ 수집 후 학습** 중 하나. 둘 다 수일+. 빠른 승리 없음(소진).
**채택된 실질 개선은 유지**: navBeforeJump, 고급타일 hang수정, ore키스톤, over-7페널티 — 봇은 더 견고·다소 강함. 실검증은 사용자 1:3.

## 2026-06-13 실게임 블런더 사냥 (사람 1:3 로그 18 봇플레이 분석)
- **18 봇플레이 중 10개 60점 미만**(itars 30, nevlas 39, moweyip 42, ivits 35, ambas/geodens/gleens 46~55...). 실게임서도 종족 무관 systemic 약체 = 60% 엔진 진단 실전 확정.
- 저득점 봇 공통: 6라운드에 13~18액션뿐(라운드당 2~3, 자원 떨어져 조기 패스), 연구 1트랙만 깊고 나머지 0~1, 연방 1~2. → 스노볼 실패(작은엔진→적은income→적은액션). systemic 천장(포화), 단일버그 아님.
- **★ 유일한 isolatable correctness 블런더: R6 가이아포머 배치** (itars 게임 관측). findGaiaformerActions가 place에 score 350(극한부스트) 주는데 라운드 가드 없음 → R6에 놓으면 성숙 불가(다음라운드 없음) → 파워6+액션 낭비 + fm_gaia +0.
  - 수정: finalRound(R6) && !isFreeProject면 가이아포머 후보 제외. flag gfFinalRoundGuard 기본ON. **provable 낭비제거(휴리스틱 아님)라 correctness 수정으로 채택**(ore-terraform·hang과 동급). head2head 검증 run은 인프라 사망(워커 2h 무응답)했으나 부분데이터 이상無 + 논리상 do-no-harm.
- 다음 구조 실험 후보(미시도, 고위험): real-income deepRollout(helperTriggerIncomePhase로 가짜income 교체) — 단 다중플레이어 income선택 프롬프트 hang 위험.
- **head2head 인프라 주의**: 긴 run(40판+)에서 워커가 무응답/사망해 결과 미기록되는 사례 반복. 짧게 쪼개거나 타임아웃 늘릴 것.

## 2026-06-13 Path A(다라운드 search) 난이도 코드레벨 확정 + scoped 계획
- helperTriggerIncomePhase(gameState.ts:1501) 직접 확인: ① pendingIncomeOrder 선택 프롬프트 생성, ② executeBotTurnIfNeeded 재귀, ③ 종족 분기(Gleens 등). → 롤아웃에 직접 호출 불가. 정확 income 재구현=수백줄 복제+오류시 롤아웃 garbage→봇 회귀 위험. **무모한 in-session 시도 금지.**
- **Path A scoped 계획(안전 벽돌, 독립 테스트):**
  1. `simulatePlayerIncome(game,pid)` 순수함수(io/프롬프트/재귀 X, power-vs-token 고정 휴리스틱 자동해소). helperTriggerIncomePhase 결과와 단위대조.
  2. `simulateOpponentTurn` = getNextMove+performAction, step캡+try/catch(hang 차단).
  3. MCTS에 다라운드 롤아웃 조립(하드캡으로 무한루프 불가), flag 뒤에.
  4. head2head는 **짧게(≤20판)** 검증(긴 run 워커 사망 반복).
- **권고**: Path A는 수일+ 고위험. 병렬로 사용자 1:3 로그 누적(Path B 해금, 무위험)이 합리적. Path A 착수는 사용자 시간투자 승인 필요.

## 2026-06-13 Path A 벽돌1(정확 income) 검증 결과 — null, 병목 좁힘
- deepRollout + realRolloutIncome(getNextRoundIncomePreview 기반 정확 income) head2head 24판:
  도전자 54.5%(12:10), VP -1.16±3.55, p=0.744 → **null**. (가짜 income deepRollout도 과거 null이었음)
- 결론: **다턴 자기-income foresight는 천장 레버 아님.** 이유: (1) 상대 미시뮬(zero-sum 핵심은 상대대비 위치), (2) 양쪽 greedy 대칭매치, (3) eval 이미 보정→깊은 자기시야가 즉시 최선수 불변.
- → **진짜 병목 = opponent-blindness.** 벽돌1(income)은 필요·불충분. **벽돌 2~3(상대 턴 시뮬 + 진짜 라운드전환)**이 path A 본체(고위험: 상대 income선택/연방/기술 프롬프트 hang). 
- 벽돌1 인프라(rolloutIncome.ts + realRolloutIncome flag)는 유지(벽돌2~3 합류 시 재사용). 기본 OFF=라이브 봇 무해.
- **R1 ore-terraform 광산 다수 = 버그 아님(의도)**: 봇이 0스텝 싼땅 먼저→소진+오레≥6일 때만 3오레 테라포밍 확장. ore-terraform 키스톤이 언더확장 푸는 정상동작. 손대면 회귀위험+포화튜닝이라 유지.

## 2026-06-13 ★ Path A 벽돌2(상대-인지 다턴 search) 첫 양성 신호 — 유망!
- simulateWithOpponents(oppRollout): 상대 턴까지 싼 1-ply 정책으로 굴리는 다턴 greedy 플레이아웃.
- 초기(6-클론 스코어링) 16판: 46.7%, VP -5.70 → **느려서 반복부족(성능 artifact)**.
- **fast-rollout(cands[0] 우선순위 직접선택, O(1)) 20판: 승률 65.0%(13:7), VP +2.55, 타임아웃0!** → 양성 전환.
- 의미: **search 천장을 깬 첫 증거.** eval/연산/income/학습 다 막혔는데 상대-인지 다턴 search는 통함(반복만 충분하면).
  단 p=0.180(20판). 36판 확인검증 중. 확인되면 채택+증폭(라이브 6s think-time이면 반복 더 많아 효과 ↑ 기대).
- 주의: greedy는 연산 스케일 안 됐지만(mctsTimeMul null), oppRollout은 깊은 search라 think-time 스케일 기대.

## 2026-06-13 ★정정★ 벽돌2 oppRollout 확인검증 → 중립(65%는 노이즈)
- 36판 확인: 48.5%(16:17), VP +0.86, p=0.815 → **null/중립**. 앞선 20판 65%는 소표본 노이즈.
- 종합(fast 56판): oppRollout은 do-no-harm이나 greedy 대비 우위 없음. **미채택(flag OFF 유지)**.
- ★결정적 결론★: **상대-인지 다턴 search조차 무효 = eval이 진짜 천장.** playout 끝을 같은 saturated eval로
  평가하니 search 깊이가 루트 선택을 개선 못 함. search로 saturated eval 보완 불가.
- **모든 레버 실험 종료**: ①eval튜닝 포화 ②연산 null ③학습 데이터기아 ④income롤아웃 null ⑤상대-인지 search 중립.
  → 유일한 미해결 경로 = **더 나은 eval(학습)**, 그러나 학습은 사람데이터 10배 필요(데이터 게이트). 사용자 1:3 누적이 유일 진척로.
- 코드 자산 유지(flag OFF, 라이브 무해): rolloutIncome.ts, simulateWithOpponents(oppRollout/rolloutFatScore), realRolloutIncome. 학습 eval 확보 시 이 search 인프라가 재활용 가치 있음(좋은 eval+search=강함).
